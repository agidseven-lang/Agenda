// Diagnóstico minimal: só AppRegistry + App mínimo. Sem services/Firebase/Notifee.
import {AppRegistry} from 'react-native';
import App from './App';

AppRegistry.registerComponent('idseven', () => App);
