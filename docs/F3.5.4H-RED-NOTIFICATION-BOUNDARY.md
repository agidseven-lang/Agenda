# F3.5.4H — DIAGNÓSTICO OBRIGATÓRIO (antes de editar)
## Correção do momento de disparo da notificação vermelha T-10 — Desktop 1.0.199

Base: tag **v1.0.198** = `009f2c6ef2064dbdd18f5a2995669128df13a9ce` (repo sem avanço; main `00c62a2`).
Branch: `desktop/f354h-red-notification-boundary-1.0.199`. Reprodução determinística:
`desktop/scripts/f354h-repro.mjs` (scheduler REAL de dist/main com relógio fake — log acima).

## A) AUDITORIA (32 itens — arquivo:linha na base 009f2c6)

1. **dueAt**: `cardsRules.js:31 cardFinishMs` = `dtMs(t.dueDate||t.due, t.dueTime||'23:59')` — re-PARSE
   por máquina; **NENHUM campo canônico em ms é persistido** (renderer: saveTask 8708,
   saveCardsBatch 8786, saveCardsEdit 8867 gravam SÓ strings dueDate/dueTime).
2. **yellowAt**: `cardsRules.js:44` `warnAt = finishMs − CARDS_WARN_MS(30min)` ✓ fórmula correta.
3. **redAt**: `cardsRules.js:44` `redAt = finishMs − CARDS_RED_MS(10min)` ✓ fórmula correta.
4. **Disparo amarelo**: `cardsRules.js:69-72` (`cardsEmissionsFor`, state 'warning', severity warning,
   dedup `cards_warning:taskId:finishMs:rN`).
5. **Disparo vermelho**: `cardsRules.js:73-76` (state 'overdue', severity critical, corpo "Faltam 10
   minutos…", dedup `cards_overdue:taskId:finishMs:rN`).
6. **Evento canônico vermelho**: `sla_cards_overdue` (isolado dos eventos designerSla `sla_overdue`).
7. **DedupKey vermelha**: `cards_overdue:<taskId>:<finishMs>:r<cardDeadlineRev>` — SEM uid (o seen é
   por usuário via `createUserSlaSeen(uid)`, uid implícito na store; a diretiva exige uid NA CHAVE).
8. **Entrega no main**: `main.ts:119 deliverNotification` (toast premium janela ativa / bg-window /
   nativa; dedup do HUB `_notifSeen` pós-aceite).
9. **Renderer solicita?** NÃO — `index.html:4322 notifScan(){}` VAZIO; `notifScanSla` definido sem
   chamador (QA-only, F3.4.3); produtor ÚNICO no main. `api.notify` só existiria via notifEmit sem
   chamadores de SLA.
10. **Detecção de T-10**: `slaScheduler.ts:160-188` — boundary único `min(marcos futuros)` de
    `slaRules.nextBoundaryMs` + `cardsRules.cardsNextBoundaryMs`, `delay=max(250,min(b−now+120,60s))`.
11. **Boot dentro de T-10**: `reconcile("boot")` → `emitDue` → estado atual 'overdue' → vermelha 1×
    (catch-up correto DENTRO da janela) ✓.
12. **Boot antes de T-10**: estado 'warning'/'running' → nunca vermelha ✓ (provado cenário A).
13. **Reconexão dentro de T-10**: snapshot → reconcile → idem 11 ✓.
14. **Reconexão antes**: idem 12 ✓.
15. **Conversão do prazo**: `slaRules.js:29 dtMs` = `new Date(y,m-1,d,hh,mm).getTime()` — calendário
    LOCAL da máquina LEITORA.
16. **Parse de data BR textual?** NÃO (só ISO 'YYYY-MM-DD' + 'HH:MM').
17. **Confusão com createdAt/updatedAt/assignedAt/startedAt?** NÃO (usa dueDate/dueTime próprios).
18. **redAt usando yellowAt?** NÃO (constantes distintas 30/10; provado no cenário A).
19. **Comparação invertida?** NÃO (`now>=redAt` / `now>=warnAt` — ordem correta).
20. **Math.abs?** NÃO existe no caminho temporal.
21. **Arredondamento antecipando?** NÃO (comparações em ms crus; Math.round só em textos).
22. **TZ local × UTC misturados?** **SIM — CAUSA-RAIZ 1**: o INSTANTE do prazo é derivado POR MÁQUINA
    de strings wall-clock; fusos/hora-de-parede divergentes deslocam TODOS os marcos (cenário C:
    Recife × UTC ⇒ redAt diverge **180min**; a máquina errada dispara a vermelha ANTES da janela
    real, com o visual do owner correto — o sintoma físico exato).
23. **Offset 2×?** NÃO — `main.ts:92-99 slaNow` aplica `st.offsetMs` UMA vez (gate synced/degraded/
    stale; local_fallback ⇒ 0), espelho exato do renderer.
24. **Relógio local num ponto e canônico noutro?** O scheduler usa SEMPRE `opts.now=slaNow`; o parse
    do PRAZO usa o calendário local (item 22) — a mistura real é prazo-por-máquina × agora-global.
25. **Alarme antigo após mudança de prazo?** NÃO — timers re-armados a cada snapshot; estado derivado
    do doc ATUAL; chaves antigas nunca mais geradas (provado também no cenário de edição da suíte).
26. **Mudança de prazo cancela redAt antigo?** SIM (item 25 + `cardDeadlineRev` na chave).
27. **Login/restart trata alerta não consumido como entregável?** SIM — e SEM TETO: **CAUSA-RAIZ 2**
    (cenário B): boot APÓS dueAt entrega `sla_cards_overdue` retroativa ("Faltam 10 minutos" falso),
    pois `cardsDisplayState:45` dá 'overdue' para `now>=redAt` SEM `now<dueAt`.
28. **Catch-up ignora limites da janela?** SIM para o limite SUPERIOR (item 27). Limite inferior ok.
29. **Dois produtores da vermelha?** NÃO (renderer sem chamador — item 9; único: slaScheduler).
30. **Renderer+main em paralelo?** NÃO (idem).
31. **Monitor SLA chama o produtor nativo?** NÃO (F3.5.4G gate 17 provou; monitor é só visual).
32. **Por que os testes anteriores não pegaram?** As suítes F3.5.2/4F/4G validavam janelas/dedup em
    UMA máquina/fuso com o relógio do teste; nenhum teste variava TZ entre "quem grava" e "quem lê"
    (não havia fonte canônica a comparar) e nenhum cobria boot pós-dueAt do produtor de cards.

## B) HIPÓTESES (provar/descartar)

REFUTADAS pela leitura+repro: redAt com 30min; redAt por createdAt/início; comparação invertida;
Math.abs; arredondamento; offset 2×; stale alarm; amarelo renderizado como vermelho (severities e
eventos distintos, cenário A); vermelho na inicialização ANTES da janela (cenário A/boot T-40);
dedup sem dueAt/rev (chave TEM ambos); renderer∥main; boundary errado; setTimeout negativo
(delay≥250ms); data inválida→0 (finishMs=0 ⇒ 'none').
**CONFIRMADAS**: catch-up sem validação do limite superior (cenário B); timezone por máquina sem
timestamp canônico persistido (cenário C — o disparo antecipado físico).

## C) CORREÇÃO CIRÚRGICA (só lógica de disparo; layout 1.0.198 congelado)

1. **Fonte canônica persistida**: `saveCardsBatch` e `saveCardsEdit` passam a gravar TAMBÉM
   `dueAt` (= `dtMs(dueDate,dueTime)` na máquina de QUEM DEFINE o prazo — a Social) e `startAt`.
   Compat total: docs antigos sem `dueAt` seguem no comportamento atual (fallback parse).
2. **Leitura canônica**: `cardFinishMs` prioriza `Number(t.dueAt)>0` antes do parse; o visual
   (`kbv2SlaLiveData`, ramo cards) prioriza o MESMO campo — monitor e nativa passam a compartilhar
   o mesmo instante absoluto em TODAS as máquinas.
3. **Gate temporal puro central** (`cardsRules.shouldFireRedNotification({nowMs,dueAtMs,completed,
   recipientUid,currentAssigneeUid,alreadyConsumed})`): true SÓ com dueAt válido, não-concluída,
   uid==designer atual, `now>=dueAt−10min`, **`now<dueAt`**, não consumida. `cardsEmissionsFor`
   emite a vermelha SOMENTE via gate (mata o retro do cenário B); amarela ganha o teto simétrico
   `now<redAt` já implícito no estado. Pós-dueAt: NENHUM evento de cards (não existe alerta de
   atraso aprovado para cards; o crítico/bloqueio designerSla é regra separada e intacta).
4. **DedupKey com uid** (mandato): `cards_overdue:<taskId>:<uid>:<dueAtMs>:r<rev>` (e amarela
   idem), mantendo compat de upgrade: o payload carrega `legacyDedupKeys` com a chave antiga e o
   `emitList` (slaScheduler) pula se QUALQUER chave (nova ou legada) já foi vista — quem já recebeu
   na 1.0.198 NÃO recebe de novo ao atualizar.
5. **Boundary/catch-up**: inalterados na forma (timer único, delay≥250ms); com (1)+(2) os marcos
   ficam idênticos entre máquinas; com (3) o catch-up respeita [redAt, dueAt).
   Cenários offline: A(volta antes de redAt)=nada; B(volta em T-10)=1 catch-up; C(volta pós-dueAt)=
   nada — cobertos na suíte fake-timer.
6. **Arquivos**: `cardsRules.js` (+gate/chaves/canônico), `slaScheduler.ts` (emitList: consulta
   aditiva de legacyDedupKeys), `index.html` (2 saves gravam dueAt/startAt; kbv2SlaLiveData lê
   dueAt) — main.ts/notifier*/preloads/tray/monitor/sino/card intocados. slaScheduler.ts+cardsRules.js
   saem da Categoria B byte-idêntica e ganham DIFF CONTROLADO próprio no build (substituição de
   gate, nunca remoção).

**Risco residual documentado**: com clockSync em `local_fallback` (sem rede desde o boot) numa
máquina com RELÓGIO errado, o "agora" degrada para o local — política aprovada (R4B/F3.4.3,
espelhada no visual) e fora do escopo desta fase; com sync (login/periódico/resume), o skew de
relógio é corrigido e o teste multimáquina prova decisão pelo canônico.
