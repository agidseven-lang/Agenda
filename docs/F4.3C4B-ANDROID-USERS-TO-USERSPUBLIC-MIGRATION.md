# F4.3C4B — Migração Android: `users` (privada) → `usersPublic` (projeção segura)

> Branch isolado `android/f43c4b-userspublic-migration` (base `android/f43c1-local-security-hardening`
> @ `b414568b49a121b2ab3bfd0aa976705206e34bf1`). **SEM alterar a Desktop 1.0.191, SEM alterar Firestore
> Rules, SEM deploy, SEM APK/AAB, SEM tocar produção/dados reais.** Identidade preservada:
> `br.com.idseven.agenda` · versionName `1.0.0` · versionCode `129`.

## 1. Motivo
O ruleset ATIVO em produção (capturado byte-exato na F4.3C4A, run `30412227718`, ruleset
`projects/agenda-id-seven/rulesets/809204d1-ee1b-444f-9b1b-2d6e0cb15132`, SHA-256
`527d4a3c…cb7ee`, canônico `f39a405a…4668`) **fecha a coleção `users` ao cliente**
(read/create/update/delete = false) e abre a projeção **`usersPublic`** (read true / escrita
fechada — mantida SÓ pela trigger `syncUsersPublic`, Admin SDK). O roster do Android ainda ouvia
`users` cru (PII/credenciais) — sob as Rules vivas esse listener já é permission-denied. Esta fase
migra o runtime Android para a projeção segura + endpoints server-side, alinhado à alternativa
D→A da F4.3C4 (pré-requisito da F4.3C4C/da promoção futura das Rules-alvo).

## 2. Auditoria — acessos `users` encontrados (FASE 1, read-only)
Runtime inteiro = **exatamente 2 pontos** (nenhum write; nenhuma constante/wrapper oculto):

| # | Arquivo | Operação | Campos usados | Finalidade | Destino |
|---|---|---|---|---|---|
| R1 | `data/UsersRepo.kt:14` | listener coleção inteira `users` | name,role,color,photo(8 aliases),status,admin,createdAt,**email,phone,fcmTokens** | roster de TODAS as telas (Equipe/chat/responsável/avatares/RBAC/perfil/diag FCM) | **A** usersPublic (roster) + **B** getUserSelf (admin self) + **C** getUserContact (email/phone self) + **D** ACK do registerFcmToken (diag aparelho) + **E** createdAt (fallback neutro) |
| R2 | `core/ReminderAlarmActivity.kt:146` | `users/{uid}.get()` (já gated) | photo,name | avatar do lembrete premium | **A** usersPublic |

Referências restantes em `app/src/main` = **comentários históricos** (código removido em
F4.2B/F4.2C/F4.3C1) — ignorados pelos gates (comment-aware) e sem execução. `getUserContact` para
TERCEIROS: **nenhuma tela usa** (classe C só para o contato do PRÓPRIO usuário na tela Conta).
Queries do diretório: sem where/orderBy/limit (listener de coleção inteira; doc por id) ⇒
**nenhum índice composto novo** (nada a criar/deploy).

## 3. Contratos comprovados
- **usersPublic** (fonte `projectUserPublicOut`, `origin/app/main:functions/index.js:2415-2427`;
  backfill f33AH com o MESMO allowlist; comentário do ruleset vivo): **{id,name,role,admin,status,
  photo,color}** — allowlist puro, nunca PII/credenciais; escrita de cliente FECHADA (Rules vivas).
  A trigger **NÃO filtra inativos** ⇒ o cliente esconde `pendente/removido/excluido` (regra que o
  Android já tinha em `isActive()`; nunca filtro positivo `=="ativo"` — docs legados sem status são
  ativos). **Nota:** o doc carrega `admin` informativo; o **modelo Android `UserPublic` NÃO o
  consome** (autorização por papel é server-side; `admin` do próprio usuário vem do getUserSelf).
- **getUserSelf** (`functions/index.js:1450`): Bearer da sessão → `self:{id,name,role,admin,status,
  photo,color,reminderMinutes,hasFcmToken,fcmTokensCount}`; nunca pass/salt/fcmTokens/email/phone.
  Wrapper Android já existia (`ServerAuthRepository.self`).
- **getUserContact** (`functions/index.js:1381`): Bearer + `{uid}`; autorização **self-or-admin NO
  SERVIDOR** (403 antes do read); resposta allowlist **`{phone,email}`**. Wrapper Android **criado
  nesta fase** (`postContact` em AuthApi/AuthApiClient; `ServerAuthRepository.getUserContact`;
  `ContactOutcome`). URL BuildConfig `AUTH_CONTACT_URL` na MESMA forma canônica cloudfunctions.net
  dos endpoints F4.2C/F4.3C1 (padrão comprovado em produção; prova física do endpoint fica para o
  checkpoint F4.3C4E — falha ⇒ "Não informado", fail-closed, sem fallback ao Firestore).

## 4. O que mudou (arquivos)
**Novos:** `domain/UserPublic.kt` (modelo público SEM admin/PII + isActive()); `data/UserPublicMapper.kt`
(fromFields/fromDoc total-safe + toUserLite — ponte para o modelo visual SEM redesenhar telas);
`data/UsersPublicRepo.kt` (listener `usersPublic` **atrás do Session Gate**, fail-closed);
`app/src/test/.../F43C4BUsersPublicMigrationTest.kt` (30 testes).
**Removido:** `data/UsersRepo.kt` (listener em `users`).
**Editados:** `core/HomeViewModel.kt` (roster→UsersPublicRepo); `core/ReminderAlarmActivity.kt`
(`users`→`usersPublic`, executor gated preservado); `data/SessionStore.kt` (`UserSession.admin`);
`core/AppRoot.kt` (propaga `profile.admin` do getUserSelf); `core/MainScaffold.kt`
(`canManage = session.admin`; overlay do admin SÓ no próprio usuário); `core/Fcm.kt` (persiste o
**ACK do servidor** do registerFcmToken p/ o diagnóstico); `features/profile/ProfileScreen.kt`
(E-mail/WhatsApp do self via getUserContact; "Este aparelho na conta" via ACK);
`data/auth/{AuthApi,AuthApiClient,ServerAuthRepository,AuthContract}.kt` (+getUserContact);
`app/build.gradle` (+`AUTH_CONTACT_URL`); fixtures de teste (+postContact no FakeAuthApi).

## 5. Session Gate (obrigatório) e fail-closed
Todo acesso a `usersPublic` ocorre SOMENTE após `AuthenticatedFirestoreExecutor.resolve()` ==
`Authenticated` — que exige `FirebaseSessionGate.ensure() == Ok` **e** `FirebaseAuth.currentUser.uid
== session.uid` (dupla checagem). Gate negado (SessionMissing/SessionExpired/FirebaseAuthFailure/
UidMismatch/NetworkFailure) ⇒ o flow **falha** (`UiList.Error` na UI) **sem armar listener**, sem
fallback anônimo, sem fallback para `users`, sem modo legado. Mesmo o ruleset vivo permitindo
leitura anônima de usersPublic, o Android NÃO depende de acesso anônimo — já compatível com as
Rules-alvo F4.3C3 (`request.auth` obrigatório).

## 6. Decisões de contrato registradas (sem mascarar ausência)
1. **admin**: modelo público NÃO carrega admin (teste 14). Self-admin = `SecureSession.profile.admin`
   (getUserSelf), plumbado por `UserSession.admin`. **Delta visual aceito e registrado:** o selo
   "Admin" na tela Equipe passa a aparecer com precisão só para o PRÓPRIO usuário; para os demais
   membros o selo não é exibido (fallback neutro autorizado — não se consome campo proibido).
2. **email/phone (self, tela Conta)**: via getUserContact; indisponível ⇒ "Não informado".
3. **createdAt ("Criada em")**: não existe em NENHUM contrato seguro ⇒ a linha deixa de aparecer
   (fallback neutro já codificado; perda registrada — extensão futura do getUserSelf, se desejada).
4. **fcmTokens (diag "Este aparelho na conta")**: substituído pelo **ACK real do servidor** no
   último registerFcmToken (prefs gravadas SÓ após Success; limpas no logout). Sem ACK ⇒ "Ausente —
   toque em Reenviar" (estado honesto; nunca lê tokens de lugar algum).

## 7. Cadastro / FCM / F4.3C1 preservados
`AuthRepo.register()` segue **RegistrationUnavailable** (sem write em users/usersPublic; cadastro é
administrativo). FCM: registerFcmToken/removeMyFcmToken/revogação no logout/deviceId inalterados;
token nunca em usersPublic, nunca logado (só `take(12)…` pré-existente). Revogação-antes-da-limpeza,
`AuthenticatedFirestoreExecutor`, Session Gate, logs sanitizados e limpeza de sessão: intactos
(re-provados pela suíte F43C1 + testes 24-26).

## 8. Testes
- **30 testes F4.3C4B** (`F43C4BUsersPublicMigrationTest`, ordem da autorização): gate estático
  1-6 (nenhum repo/Activity/Service/Receiver/ViewModel/main-inteiro toca `users`; comment-aware);
  destinos 7-9; modelo sem pass/salt/fcmTokens/sessionVersion/admin 10-14; mapper total-safe 15-17;
  inativos 18; Session Gate fail-closed 19-22 (Firestore NUNCA tocado em negação — provado com
  provider que explode); sem fallback 23; register bloqueado 24; logout/FCM 25; ReminderAlarmActivity
  26; logs 27-28; navegação 29; troca de usuário sem resíduo 30.
- **Regressão**: TODAS as suítes `br.com.idseven.agenda.auth.*` (F4.2B 33 + F4.2C + F4.3C1 35)
  rodam no MESMO filtro do CI; `compileDebugSources` + `lintDebug` com gate de regressão.
- **Execução**: o sandbox local NÃO tem Android SDK (dl.google.com bloqueado) ⇒ compilação/testes/
  lint são provados no CI (mesmo modelo da F4.3C1). **Testes instrumentados** (13 cenários: login
  sintético→gate Ok→diretório→responsável→tarefa/evento/chat→logout→troca de usuário→usersPublic
  indisponível sem fallback) ficam **PROJETADOS e BLOQUEADOS** — exigem Emulator Suite + device;
  **não declarados aprovados** (executar no checkpoint F4.3C4E com fixtures sintéticas/projeto demo).
  O checkpoint visual/funcional (telas com fixtures) também fica para antes de QUALQUER build oficial.

## 9. Gate estático (prova contínua)
(a) JUnit t01-t06 (fonte, comment-aware, `app/src/main` inteiro; testes/androidTest fora do escopo);
(b) passo dedicado do CI (python, comment-aware) que FALHA se `app/src/main` contiver
`collection("users")`/`document("users/`. Referências permitidas só em testes negativos e
documentação.

## 10. CI verify-only
`.github/workflows/f43c4b-android-verify.yml` (em `main`; `workflow_dispatch`; `contents: read`;
checkout do branch de feature): identidade congelada (129/1.0.0/br.com.idseven.agenda) → isolamento
(allowlist `android-native-beta/**` + `docs/F4.3C4B-*.md`; Desktop/Worker/functions/Rules/captura
viva/portal diff-empty) → **gate estático no-users** → JDK17 + Android SDK + google-services (secret
já existente) → `compileDebugSources` → `testDebugUnitTest --tests "br.com.idseven.agenda.auth.*"`
→ `lintDebug` + gate de regressão. **Sem assinatura, sem assembleRelease/bundleRelease, sem deploy,
sem produção, sem APK/AAB publicado.**

## 11. Limitações registradas
- Endpoint `getUserContact`: contrato comprovado no código canônico deployado; **prova física
  (HTTP vivo) pendente** (F4.3C4E) — falha degrada para "Não informado" (fail-closed).
- Deltas visuais aceitos: selo Admin de terceiros (§6.1) e linha "Criada em" (§6.3).
- Cache offline do SDK Firestore pode reter o roster ANTIGO (com PII) de instalações anteriores até
  expirar/substituir; após a migração o cache passa a conter apenas campos públicos. Limpeza ativa
  do cache ficaria para fase futura (fora do escopo cirúrgico desta).
- Testes instrumentados: projetados, NÃO executados (sem ambiente) — ver §8.

## 12. Isolamento / componentes congelados
Alterações SOMENTE em `android-native-beta/**` + `docs/F4.3C4B-*.md` (+ workflow verify-only em
`main`). **Diff-empty** comprovado (gate do CI + verificação local) em: `desktop/` (1.0.191 Golden
Master intacta — Card Premium, notificações, Cronograma, Roteiro, tray, updater), `functions/`
(backend F4.3C2 não tocado/não deployado), Firestore Rules (nenhuma alteração/deploy; captura viva
`ops/firestore-rules-live-captures/2026-07-29/` e baseline intocadas), `cloudflare-worker.js`/
Worker J6/portal, `scripts/`, `presence-service/`. **Nenhum deploy; nenhum dado real; nenhum
usuário real; nenhum APK/AAB; assinatura intocada; APK oficial (Artifact 8701571306) preservado;
Desktop 1.0.191 permanece Latest.** Rollback: descartar o branch (nada publicado).

## 13. Próximos gates (nenhum inicia automaticamente)
CI verde ⇒ **PRONTO PARA F4.3C4C** (protótipo isolado da Desktop futura). Antes de QUALQUER build
oficial Android: checkpoint visual/funcional com fixtures sintéticas + testes instrumentados
(F4.3C4E) + prova física do getUserContact. Promoção das Rules-alvo continua bloqueada até A
(Desktop autenticada) — sequência F4.3C4C→H da arquitetura F4.3C4.
