# F3.5.4G — DIAGNÓSTICO OBRIGATÓRIO (antes de editar)
## Reposicionamento do SLA para o monitor superior + restauração do sino — Desktop 1.0.198

Base auditada: tag **v1.0.197** = `14e9fb952b64e56d56528e63c6ac54499ab4b71d` (repositório NÃO avançou:
main `00c62a2`, tags v1.0.194–197 intactas, nenhuma branch f354g pré-existente).
Branch isolada: `desktop/f354g-sla-top-monitor-bell-fix-1.0.198`. `main` não será tocada.

Referências de linha = `desktop/src/renderer/index.html` @ 14e9fb95.

---

## A) AUDITORIA OBRIGATÓRIA (15 itens)

1. **Onde o SLA foi inserido no card na F3.5.4F**: dentro de `kbv2Card`, entre o topo
   (`.kbv2-card-top`) e o perfil (`.kbv2-card-profile`) — bloco `{ const _lv=kbv2SlaLiveData(t); … }`
   (linhas ~6220–6245), que injeta `<div class="kbv2-sla-live …" data-sla-live …>` com
   `avatar(_resp2,22)`, nome e contador. CSS da banda: bloco `<style id="f354f-sla-live-styles">`
   (linhas 2393–2411). É exatamente o que o owner REPROVOU.
2. **Função que injeta a banda**: o próprio `kbv2Card` (bloco acima), alimentado por
   `kbv2SlaLiveData(t)` (linha 6145 — marcos canônicos T-30/T-10 p/ `edicao_cards` e modo designer)
   e `f354fSlaLiveState(marks,now)` (linha 6163 — estado puro amarelo/vermelho/atraso).
   O ticker de 1s atualiza via `f354fLiveEls()`/`f354fSlaLiveUpdate()` (linhas 3769–3784).
3. **Função que renderiza o monitor superior**: o componente apontado pelo owner é o widget
   `#sla-monitor` (classe `.slamon`), renderizado por `slaMonRender()` (linha 3532): chip
   `slamon-chip` com kicker **"Monitor SLA — ao vivo"**, status (`slaMonStatusText`, linha 3483 —
   **"Tudo em dia"** quando `d.total==0`), segmentos (`slaMonSegs`) e dropdown `slamon-pop`
   (`slaMonDropInner`, linha 3466). Anti-flicker por assinatura `data-sig` (`slaMonSig`, 3504) +
   atualização em lugar (`slaMonUpdateCounters`, 3507).
4. **Como o monitor determina "Tudo em dia"**: `slaMonData()` (linha 3452) deriva SÓ de
   `designerSla` (`slaPanelDerive` sobre `slaibVisible(u)`); `d.total==0` ⇒ status "Tudo em dia".
   **Ponto cego provado**: tarefas de Edição de Cards NÃO têm `designerSla` (comentário vivo na
   linha 3787) ⇒ as cobranças T-30/T-10 dos cards NUNCA entram no monitor hoje.
5. **Como o alerta será transferido do card para o monitor**: os helpers puros da F3.5.4F
   (`kbv2SlaLiveData` + `f354fSlaLiveState` + `f354fSpan`) CONTINUAM como fonte única; nasce uma
   camada aditiva `f354gMonAlerts()` que roda esses helpers sobre `slaibVisible(state.user)`
   (mesmo escopo de visibilidade do sino), filtra pelo contexto da aba e ordena
   vermelho>amarelo, prazo mais próximo primeiro. O chip do `#sla-monitor` ganha o Estado B
   (avatar MAIOR + nome + contador ao vivo); a banda do card é REMOVIDA por completo.
6. **Fonte de verdade do Designer cobrado**: `designerOf(t)` → uid canônico
   (`assigneeId`/`designerAssignment.designerId`) — nunca nome. A F3.5.4F já usava; preserva-se.
7. **Como o contexto da aba Designers é identificado**: `state.flowView==='designers'` +
   `state.designerBoard` = uid do Designer com quadro aberto (linhas 4399, 5234); quadros de
   pessoa usam `state.personBoard` (uid; linhas 4401, 5054). Ambos são LIMPOS em toda troca de
   aba/tab (linhas 8839–8844) — sinal confiável do contexto ativo.
8. **Como o monitor deve reagir à troca de aba**: cada troca chama `render()` → wrapper (linha
   9110) chama `slaibRefresh()` → `slaMonRender()`; a assinatura (`slaMonSig` estendida com a
   identidade do alerta de contexto) reconstrói o chip UMA vez na troca. Regra adotada
   (documentada): **contexto = `state.designerBoard` || `state.personBoard`; se nulo, modo GLOBAL
   = alerta mais urgente entre as tarefas visíveis ao usuário** (Designer vê só as suas, pois
   `slaibVisible` já aplica o papel).
9. **Como o sino foi quebrado — CAUSA-RAIZ (empírica)**: a F3.5.4F trocou o gate do tick de 1s
   (linha 3789) de `if(slaMonHasActive())` para `if(slaMonHasActive()||f354fLiveEls().length)`.
   `f354fLiveEls()` conta os nós `.kbv2-sla-live[data-sla-live]` — que EXISTEM (mesmo `hidden`)
   em TODO card de Edição de Cards com prazo. Resultado: em qualquer quadro de Edição de Cards o
   `slaTick()` roda TODO SEGUNDO, para sempre, e chama `slaibRender()` (sino) + alinhamentos.
   `slaibRender()` com o painel aberto chama `slaibRenderPanel()` (linha 3309), que
   **substitui o innerHTML de `.slaib-list` a cada segundo** — os nós sob o cursor são destruídos
   entre o mousedown e o mouseup e o clique NUNCA se completa; além disso `slaClusterAlign()`
   (linha 3383) REESCREVE a geometria do sino 3×/s. Na 1.0.196 o gate era só `slaMonHasActive()`
   — falso sem `designerSla` — e o sino ficava estável nesses quadros.
   **Prova (sonda `f354g-bell-probe.mjs`, Chromium, timers reais, fatia REAL do index.html)**:
   | medição (quadro Edição de Cards, sem designerSla) | 1.0.196 | 1.0.197 |
   |---|---|---|
   | chamadas de `slaibRender` em 3s | **0** | **3** (1/s) |
   | escritas de geometria no sino em 3s | 3 (boot) | **9** (3/s) |
   | nó do painel aberto sobrevive 1,4s | **sim** | **não** |
   Agravante latente (igual nas duas, mas re-aplicado a cada 1s na 197): com `#cornerAvatar` de
   rect zero (foto carregando/re-render), `slaClusterAlign` calcula `right=innerWidth+10` e
   **empurra o sino para fora da tela** (medido: `right:1290px`, onScreen:false).
10. **Elemento do topo interceptando o clique**: NENHUM em repouso — `elementFromPoint` no centro
    do sino devolve o próprio sino nas duas versões (sem overlay, sem pointer-events indevido).
    A interceptação é TEMPORAL (nó destruído no meio do clique) e POSICIONAL (sino re-posicionado
    todo segundo; fora da tela no caso de rect zero), não um overlay estático.
11. **Arquivos da F3.5.4F que afetaram o header**: só `index.html` — (a) gate do interval de 1s
    (linha 3789 — a causa); (b) `slaOpRowHtml` ganhou avatar nas linhas do dropdown do Monitor
    (linhas 3429–3436, inofensivo); (c) CSS `#sla-monitor .slaop-av` (linha 2409, escopado).
    `notifEvents.js`/`notifierA.ts` não tocam o sino (produtor/entrega de notificações).
12. **Como manter o app.asar compatível com os gates aprovados**: gates F3.5.4F do build são
    ROTACIONADOS cirurgicamente (regra F3.5.4C: substituir, nunca apagar): os gates de banda
    no card viram gates de REMOÇÃO da banda + gates do monitor; gates de edição/task_updated/
    congelados permanecem idênticos; suítes f354/f354e voltam ao estado 1.0.196 (a emenda do
    stub `kbv2SlaLiveData=null` deixa de ser necessária quando o card não referencia mais a
    banda — prova adicional de card limpo).
13. **Como provar que o card voltou limpo**: (a) assert de fonte: `kbv2Card` sem `kbv2-sla-live`;
    (b) render REAL de tarefa DENTRO da janela T-30 → HTML do card SEM banda e byte-comparável ao
    da 1.0.196 (mesma fixture, `git show e3a1a5b`); (c) screenshot antes(197)/depois(198);
    (d) gate no app.asar.
14. **Como provar que o monitor virou o ponto oficial do SLA**: suíte `f354g-sla-top-monitor`
    (20 asserts: estados A/B, amarelo/vermelho/atraso, avatar/nome/contador, isolamento por aba,
    reatribuição/conclusão/reabertura/prazo, scheduler único) + screenshots reais + gates no asar.
15. **Como provar que o sino voltou a funcionar**: suíte `f354g-bell-notification-panel`
    (15 asserts, Chromium interativo: clicável, abre/fecha, badge, itens, clique abre tarefa,
    sem erro de console, sem overlay, painel ESTÁVEL sob tick com alerta ativo — clique lento
    mousedown→1,1s→mouseup COMPLETA) + sonda comparativa 196×197×198.

---

## B) AUDITORIA OBRIGATÓRIA DO SINO (20 itens)

1. **Componente**: `#slaib-bell` — portal fixo no `<body>` (fora de `#app`), criado por
   `slaibRender()` (linha 3654); ícone `svg('bell')` + badge `#slaib-count`.
2. **Handler de clique**: `bell.addEventListener('click',slaibToggle)` no momento da criação
   (linha 3661) — listener direto no elemento (não delegado), nunca re-registrado.
3. **Estado aberto/fechado**: presença do overlay `#slaib-ov` no DOM (`slaibToggle`, linha 3330:
   existe→remove; não existe→cria).
4. **Painel vinculado**: `#slaib-ov` (z-index 4001) com `.slaib-panel` → `.slaib-hd`
   ("Alertas de SLA" + "Marcar todas como lidas") + `.slaib-list` (linhas via `slaibRenderPanel`).
5. **Dados**: `slaibCompute()` (linha 3173) — alertas por PRAZO FINAL derivados de `designerSla`
   (`slaPanelRow`); leitura/lidas em `slaibLoadRead/slaibSaveRead` (localStorage).
6. **Problema no renderer?** SIM — exclusivamente (gate do tick + rebuild do painel + realinhamento).
7. **Overlay/z-index?** NÃO — hit-test em repouso devolve o sino; nenhum overlay novo da F3.5.4F.
8. **pointer-events?** NÃO — nenhum `pointer-events` novo; banda vive DENTRO do card, longe do topo.
9. **Listener removido?** NÃO — binding intacto; o elemento do sino não é recriado pelo tick.
10. **stopPropagation?** NÃO — o handler global (linha 8893) só ganhou o branch `[data-cardedit]`,
    padrão idêntico aos vizinhos; `closeCardMenus()` continua no topo do handler.
11. **Render condicional?** PARCIAL — regra pré-existente (sino só em `isOperationalBoardContext()`,
    linha 3524; some em formulários/abas não-Kanban — comportamento aprovado da 1.0.196, intacto).
12. **Refatoração da F3.5.4F?** SIM — item A.9: o gate do interval de 1s é a regressão.
13. **Colisão com o novo monitor SLA?** A prevenir: o monitor F3.5.4G atualiza contador por
    `textContent` dirigido (assinatura estrutural), NUNCA reconstruindo o painel do sino; o gate
    novo é por DADOS (`f354gMonHasActive()`), não por DOM.
14. **Área clicável coberta?** NÃO em repouso (item B.7); TEMPORALMENTE sim na 197 (nós destruídos
    sob o cursor a cada 1s com painel aberto; sino fora da tela no caso rect-zero re-aplicado 1/s).
15. **Parou em aberto, fechado ou de listar?** O sino ABRE (provado); a falha é de INTERAÇÃO
    (itens/painel instáveis sob o storm de 1s; geometria reescrita 3×/s; borda rect-zero =
    sino fora da tela ⇒ "parou de funcionar" na percepção do owner).
16. **Badge/contador afetado?** Estruturalmente não (`#slaib-count` atualizado por textContent);
    sob o storm é recalculado 1×/s sem necessidade (custo, não corrupção).
17. **Clique no item abre a tarefa correta?** O caminho `[data-slaib-open]`→`slaibOpenTask`→
    `openDetails(taskId)` está intacto; o clique MORRE antes (item A.9) quando o rebuild cruza o
    gesto. Com painel estável, abre a tarefa correta (provado na sonda de controle).
18. **Aberto/minimizado/fechado no X**: arquitetura preservada (`backgroundThrottling:false` +
    flag de timers; produtor autoritativo no main) — a F3.5.4G não toca main/preload/tray.
19. **Erros de console ao clicar**: NENHUM nas duas versões (sonda com pageerror+console.error).
20. **Regressão do tray/notificações?** NÃO — main process byte-idêntico entre 196→197 nesses
    caminhos; F3.5.4G mantém congelados (gates de diff no build).

---

## C) PLANO CIRÚRGICO (F3.5.4G)

**C1 — Remover o SLA do card (P1)**: apagar o bloco da banda no `kbv2Card` (~6220–6245) e o CSS
`.kbv2-sla-live` (2393–2411 → substituído pelo CSS do monitor); apagar `f354fLiveEls`/
`f354fSlaLiveUpdate` e a menção no `slaTick`. `kbv2SlaLiveData`/`f354fSlaLiveState`/`f354fSpan`/
`f354fPad2`/constantes PERMANECEM (fonte única reutilizada pelo monitor). Card volta byte-limpo
(prova C13). Suítes f354/f354e voltam ao texto da 1.0.196.

**C2 — Monitor superior (P2)**: camada aditiva `f354gMonCtxUid()` (contexto: designerBoard||
personBoard||global) + `f354gMonAlerts()` (helpers F3.5.4F sobre `slaibVisible`, filtro de
contexto, ordenação vermelho>amarelo>prazo) + `f354gMonTop()`/`f354gMonHasActive()`.
`slaMonRender()` ganha Estado B premium no chip: avatar REAL 30px (foto ou iniciais+cor via
`avatar()`), nome (clamp 2 linhas + tooltip), contador tabular ao vivo ("Faltam 29m 43s" /
"09m 18s" / "04m 51s em atraso"), chip amber/red (classes sev existentes); "+N" quando há mais
cobranças no contexto; Estado A ("Tudo em dia") preservado. Dropdown ganha seção das cobranças
de cards (avatar+nome+tarefa+contador+Abrir tarefa) acima dos grupos designerSla. Assinatura
estendida (identidade do alerta) ⇒ troca de estado reconstrói 1×; tick de 1s atualiza SÓ
`textContent` (`f354gMonLiveUpdate`). Boundary: `slaMonNextBoundary` passa a considerar também
os marcos dos cards (warn/red/due) ⇒ transições instantâneas sem tick permanente.

**C3 — Restaurar o sino (P3)**: (a) gate do 1s volta a ser por DADOS:
`if(slaMonHasActive()||f354gMonHasActive()) slaTick();` (zero DOM-query; sem alerta ⇒ ZERO tick,
comportamento 1.0.196); (b) `slaibRenderPanel` ganha assinatura estrutural (chaves+sev+lidas):
estrutura igual ⇒ atualiza SÓ textos em lugar (spans `data-slaib-txt`), estrutura diferente ⇒
reconstrói 1× — painel aberto ESTÁVEL mesmo durante alerta real (conserta também o defeito
latente da 1.0.196); (c) `slaClusterAlign` endurecido: ignora rect de largura zero (mantém a
última posição válida; nunca manda o sino p/ fora da tela) e só escreve estilo quando o valor
muda.

**C4 — Testes/provas**: suítes novas `f354g-sla-top-monitor.test.mjs` (20) e
`f354g-bell-notification-panel.test.mjs` (15, Chromium interativo) + 5 cenários multiusuário;
emenda documentada da suíte f354f (banda→monitor; helpers preservados); f354/f354e restauradas;
sweep baseline; 15 screenshots reais + métricas; build gated com 20 gates no asar; publicação
automática 1.0.198; rollback = Latest→v1.0.197 (e v1.0.196 física preservada).

**Rollback da fase**: nada é publicado sem gates verdes; releases/tags 194–197 intocadas.
