# F3.5.5A — ACOMPANHAMENTO INTELIGENTE DE EXECUÇÃO — RELATÓRIO FINAL (51 itens)

Desktop **1.0.217** · baseline 1.0.216 · rollback imediato **1.0.216** · adicional **1.0.213**
Branch `desktop/f355a-adaptive-execution-checkins-1.0.217` · Backend `backend/f355a-execution-checkpoint-claims` → `app/main` @ 4a624a1

## A. Auditoria e arquitetura (1–8)
1. **Auditoria read-only pré-edição executada e commitada** antes de qualquer edição (docs/f355a/f355a-checkpoint-auditoria.md + ADENDOS 1/2/3): arquitetura real dos alertas (slaReminder orquestrador; taskIdleScheduler produtor legado; slaCfgOf por setor; campos canônicos de tempo; Rules vivas capturadas F4.3C4A).
2. **Nenhum scheduler novo competindo**: o orquestrador NÃO tem listener/timer/fila próprios — acoplado ao taskIdleScheduler por 4 hooks opcionais (mesmo listener Firestore, mesmo setTimeout auto-reagendável, mesma fila central enqueueCheckin). Prova: suíte f355a-orchestrator F1–F5 + código.
3. **Autoridade única por tarefa** (matriz etAuthority): OFF⇒legado 1.0.216; SHADOW⇒legado autoridade REAL; ACTIVE+elegível⇒adaptativo único (legacyGate suprime o legado POR TAREFA — `task.idle.suppressed_by_execution_authority`); ACTIVE+inelegível⇒legado. Nunca dois check-ins para a mesma tarefa (suite 38/38 + prova P20/P25).
4. **Modelo de dados**: `executionCheckins/<taskId|designerId|dl<v>|cp<idx>>` = histórico imutável SERVER-ONLY (catch-all das Rules bloqueia clientes; Admin SDK bypassa — ZERO mudança de Rules); projeção `tasks.executionTracking` {lastCheckin, timeline cap 40, observação cap 2000} escrita NA MESMA transação do respond.
5. **Chave idempotente determinística** taskId+designerId+deadlineVersion+checkpointIndex (nunca timestamp) — etKey nos dois lados (Desktop e servidor, byte-idêntico sha256 5e849c60…).
6. **9 estados** PLANNED/DUE/CLAIMED/DISPLAYED/RESPONDED/MISSED/SUPPRESSED/CANCELLED/SUPERSEDED (ET_STATES; transições validadas na suíte do planner).
7. **Privacidade estrita**: só sinais operacionais da Agenda (docs de tasks); nenhum monitoramento de teclado/mouse/programas/janela/tela/arquivos; sem linguagem de vigilância; cabeçalhos dos módulos declaram a proibição.
8. **deadlineVersion** mudado SOMENTE pela operação autoritativa de mudança de prazo (saveCardsEdit, irmão do cardDeadlineRev aprovado) — nunca pelo planner; momento da atribuição lido do dado REAL (designerAssignment.assignedAt; nunca inventado).

## B. Backend server-side (9–16) — CORREÇÃO ARQUITETURAL incorporada
9. **claimExecutionCheckpoint** (functions/, aditivo): POST session-gated (authVerifySessionToken; uid SÓ da sessão), valida Designer atribuído (assigneeId do doc lido NA transação), taskId/checkinId/deadlineVersion/checkpointIndex; transação Firestore ATÔMICA com RELÓGIO DO SERVIDOR.
10. **Concessão apenas** {doc ausente | lease vencido no relógio do servidor | retomada idempotente do mesmo device}; **negação** {LEASED_BY_OTHER | RESPONDED/MISSED/CANCELLED/SUPERSEDED/SUPPRESSED | inelegível | deadlineVersion mudou | Designer mudou | zona SLA (defesa conservadora — cliente só aperta)}.
11. **Persistência com tempos do servidor**: state CLAIMED, claimedAt, leaseExpiresAt(=servidor+duração), claimedBySessionHash (sha256 do Bearer; nunca o token), claimedByDeviceHash, claimVersion, updatedAt. Relógio local NUNCA é autoridade (prova ±10min D1/D2 da suíte + P24).
12. **respondExecutionCheckpoint** transacional: claim vigente (mesmo Designer; mesmo device ou lease vencido), checkpoint aberto, deadlineVersion atual, sem resposta anterior, transição válida; RESPONDED ou MISSED (kind=missed pelo detentor); already_responded IDEMPOTENTE; histórico nunca sobrescrito; projeção no task doc NA MESMA transação (invalidação multi-device via snapshot realtime já existente).
13. **Sem escrita offline de claim**: desktop OFFLINE ⇒ RETRY_ON_RECONNECT (zero claim local/alerta/som/backlog) — provas D1/D3 + P25.
14. **Harness offline 45/45** (contrato HTTP; corrida dual-device; retomada; takeover; matriz de negação; caps; privacidade de logs; domínio byte-idêntico pinado) + suítes herdadas f33R..f42c todas verdes.
15. **Deploy gated em produção ANTES da Desktop**: workflow pinado `f355a-execution-endpoints-deploy.yml` (main 76a72c4; commit 4a624a1 + blob 5dba32b5; alvo FIXO --only 2 functions) — dry-run 30934574821 VERDE; deploy REAL **30934894269 VERDE** com functions:list + sondas SEM credencial (GET=405; POST-sem-Bearer=401) nos run.app oficiais.
16. **Retrocompatível e dormente**: nenhum endpoint/contrato existente mudou (loginUser/getUserSelf/issueFirebaseAuthToken/registerFcmToken byte-presentes; gate no deploy); ZERO mudança de Rules; nenhum consumidor até a 1.0.217 (que sai com flag OFF).

## C. Planner determinístico (17–24)
17. **Matriz explicável por duração** (16 durações testadas): <30min 0; 30–60min 0 quando o corte SLA não deixa distância segura (com warn 30+margem 10) e 1 (~35%) quando o setor permite; 1h–1h30 1 (~30%); 1h30–3h 25/60; 3h–8h 20/50/75; 8h–24h 25/55/75; 24h–3d 25/50/75 máx 1/dia; >3d base 20/50/75 + extras só reativos (máx 1/dia; máx 6/tarefa). Limite diário VENCE percentuais (documentado).
18. **Zona de proteção do SLA real**: earliestSlaAlertAt = prazo − warningMinutes DO SETOR (slaCfgOf real: cronograma 30, edicao_midia 40…) − margem configurável (nunca hardcode T-30/T-10); na zona: PLANNED⇒CANCELLED, DUE/CLAIMED/DISPLAYED⇒SUPERSEDED; laranja nunca soa/promove; vermelho/amarelo NUNCA esperam.
19. **Prioridade** VERMELHO→AMARELO→LARANJA→COMUM preservada: laranja subordinado na fila central (SLA prioridade absoluta — código aprovado F3.5.4Q intacto + prova P15).
20. **Janela de execução** = startAt explícito → momento da atribuição → nunca inventada (sem janela ⇒ sem plano, razão janela_invalida).
21. **Supressão/adiamento com razão sanitizada**: task_finished/sent_to_review/blocked_formal/waiting_dependency/pause_until_active/sla_zone/outside_schedule/recent_meaningful_activity/recent_response/just_started/displayed_on_other_device/duplicate_snapshot (etShouldSuppress; ctx real).
22. **Sem backlog** pós-offline/restart: etStillRelevant descarta vencidos (>6h/pós-corte) ⇒ SUPERSEDED local; reavaliação na reconexão (D3).
23. **Risco determinístico e explicável** (etRisk: fatores nomeados que sobem/descem; low/medium/high) — sem score secreto.
24. **Jornada de trabalho**: sem config completa (dias/início/fim/timezone/intervalo/limite) ⇒ ACTIVE NÃO habilita — recusa com a mensagem LITERAL "Conclua a configuração da jornada antes de ativar os check-ins." (etActiveAllowed + IPC + UI); fora da jornada ⇒ reposiciona/superseda; sem horários inventados (timezone REAL da máquina).

## D. Desktop — janela central e respostas (25–33)
25. **Check-in laranja na MESMA janela central aprovada** (view própria .card.exec; título literal "CHECK-IN DE EXECUÇÃO"; pergunta literal "Você está executando esta tarefa neste momento?") — provas reais P2/P3.
26. **Som laranja PRÓPRIO** execution-checkin.wav (gerado localmente, empacotado; 2 notas discretas, mais suave que o vermelho e distinto do amarelo); WAVs de SLA aprovados byte-intactos (sha256 pinados); 1 som por checkpoint (re-render/restauração não duplicam — P4/P7/P17); nenhum som em SHADOW/suprimido/duplicado multi-device.
27. **Exibir SÓ após**: claim confirmado no servidor → checkpoint ainda elegível → entrou na fila central → virou ativo → ACK de render → SÓ ENTÃO som/tempo (timer pós-ACK — onExecutionRendered; P5).
28. **Respostas**: SIM (observação/progresso opcionais) e NÃO {Ainda não iniciei; Estou bloqueado; Aguardando material ou retorno (origem + pausar 1h/2h/4h); Concluí minha parte (confirmação explícita ⇒ flui p/ Revisão); Outro} — P11/P12 + suíte C1–C10.
29. **"Responder em 10 minutos"** 1x por checkpoint (configurável) — P18 + D1/D2 da suíte central.
30. **Sem resposta**: janela de resposta (configurável) SÓ pós-ACK ⇒ oculta SEM perder ⇒ UMA reapresentação ⇒ MISSED registrado server-side pelo detentor ⇒ SM notificado ⇒ risco↑.
31. **"Registrando…" 1.0.211 preservado**: freeze de bounds, troca atômica do rodapé, falha de rede mantém o texto digitado e permite retry, sem notificação falsa — P8/P10.
32. **Preempção pelo crítico**: laranja recolhe preservando o texto digitado; crítico exibe; laranja restaurado com o rascunho e SEM som novo — P15/P16/P17.
33. **Multi-device**: claim simultâneo ⇒ 1 CLAIMED + 1 LEASED_BY_OTHER (só o vencedor exibe; zero janela/som duplicados — P19/P20); resposta em um device INVALIDA quem exibe (projeção no task doc via snapshot; idempotente — P22); lease vencido permite takeover (P23); zero duplicação de histórico (chave única no servidor).

## E. SM, Detalhes, Configurações e observabilidade (34–41)
34. **SM em tempo real** pela notificação comum premium (deliverNotification; agrupamento por tarefa; 1 som no primeiro evento; deep-link; preview truncado 90 — texto integral SÓ nos Detalhes): eventTypes NOVOS execution_checkin_note / execution_not_started / execution_blocked / execution_waiting_dependency / execution_checkin_missed / execution_part_completed (nunca reutiliza sla_*; classifyReminderLevel não intercepta).
35. **Não notifica**: "Sim" sem observação; suprimidos; SHADOW (nada é escrito); reconexão/dedup (chave+estado); o Designer autor não se auto-notifica; destinatário = SM/Admin (papel server-verified).
36. **Detalhes — "ACOMPANHAMENTO DA EXECUÇÃO"**: timeline por checkpoint (respostas, observações INTEGRAIS, origem de espera, perdas) — nunca visível ao cliente (Worker não seleciona executionTracking; provado por grep na suíte).
37. **Configurações → Acompanhamento de execução**: modo OFF/SHADOW/ACTIVE, dias/horários da jornada, máx/dia, intervalo mínimo, tempo de resposta, snooze, observação obrigatória nas negativas; escrita global SÓ Admin (server-verified); não-Admin somente leitura.
38. **Feature flag default OFF pós-update** (ET_CFG_DEFAULTS mode "off"; gate no código e no app.asar); owner ativa manualmente (SHADOW primeiro; depois ACTIVE p/ um Designer de teste via lista designers) — NUNCA ativa sozinha.
39. **SHADOW totalmente silencioso**: zero janela/som/fila/notificação/sino/timeline/risco real/resposta fictícia — só plano computado + would-display + observabilidade sanitizada (suite B1–B5).
40. **Observabilidade sanitizada** execution_tracking.* (mode, appVersion, timestamp, taskIdHash, designerIdHash, checkpointIndex/Count, deadlineVersion, durationBucket, suppressionReason, responseType) — nunca nome/cliente/título/observação/UID cru/e-mail/token/caminho (assert B4).
41. **Segurança**: token de sessão confinado ao auth-core (postAuthed; 401 não derruba sessão); deviceHash = sha256(userData) truncado (nunca caminho em claro); endpoint protegido por autenticação+autorização (não só Rules); nenhum segredo impresso.

## F. Testes, provas e regressão (42–46)
42. **Suítes novas**: planner 98/98 · orquestrador 38/38 · janela central 33/33 · integração 25/25 · backend 45/45 = **239 asserts** verdes.
43. **Provas REAIS Electron 25/25** (renderer empacotado em asar; janelas/IPC/preload reais; manifesto sha256 em docs/f355a-qa/) — inclui TODAS as provas da CORREÇÃO (dual-device, relógio do servidor, ±10min, offline, takeover, invalidação cruzada, zero duplicação).
44. **Regressão 1.0.216 canônica** verde: som imediato 37/37 · detalhes/status/notas 74/74 · cards compactos 53/53 · sessão 40/40 (pins de versão atualizados p/ 1.0.217 pelo precedente b12fde2); amarelo/vermelho e check-in azul preservados integralmente (congelados diff-vazio: notificationGrouping/slaScheduler/notifierA/notifEvents/slaRules/cardsRules/updaterService/bgNotify/taskIdle*/bgnotify.html/WAVs de SLA).
45. **Build CI gated** (desktop-build-f355a.yml): ancestria e509f43+e626d36; versão×3; tsc; sintaxe dos 3 renderers; 4 suítes novas + 4 de regressão; provas reais 25 no runner (xvfb); isolamento allowlist; congelados; autoridade única/OFF-default/URLs; gate EMPACOTADO (marcadores no app.asar + regressão empacotada) — run: 30937370051 (VERDE nos 2 jobs).
46. **Condições de não-publicação verificadas**: nenhum cenário de duplo check-in; SHADOW silencioso; ACTIVE bloqueado sem jornada; sem vazamento ao cliente; backend validado ANTES; todos os gates verdes.

## G. Publicação e operação (47–51)
47. **Release**: tag v1.0.217 publicada DIRETO no Latest pelo GitHub App "Agenda ID Seven Release Bot" com os MESMOS bytes da run 30937370051 (digests dos 2 artifacts + 5 hashes SHA-256 conferidos no workflow desktop-release-f355a.yml antes de publicar; 6 assets: EXE, MSI, latest.yml, blockmap, SHA256SUMS, VERSAO-DESKTOP.txt). SHA-256 EXE 4d07b5fe1a489176aa551f7432fc962582d1fffe2e501d0ef297163fb656bc8a · MSI d0b603c40ac14727f5531679cfd0aabacda9c15be990b2dea9df0aa269489782.
48. **Canal**: exclusivamente o updater interno (latest.yml no Latest); sem prerelease/canal de teste/feed alternativo/download manual; releases anteriores preservadas para rollback (v1.0.216 imediato; v1.0.213 adicional — verificadas pós-publicação).
49. **Como ativar (owner)**: (1) atualizar para 1.0.217 (tudo permanece EXATAMENTE como 1.0.216 — flag OFF); (2) na máquina do Admin: Configurações → Acompanhamento de execução → preencher a JORNADA (dias/início/fim) e limites → modo **Sombra** (observação silenciosa; nada aparece); (3) após validar a telemetria, modo **Ativo** com o campo designers restrito ao Designer de teste; NUNCA ativar globalmente de imediato.
50. **Rollback**: Latest→v1.0.216 (bytes preservados) a qualquer momento; endpoints do backend ficam dormentes (nenhum cliente os chama); nenhum dado destrutivo (executionCheckins/executionTracking são aditivos e ignorados pelas versões antigas).
51. **Declaração**: **GO TÉCNICO** da F3.5.5A — todos os gates automatizados verdes e provas reais executadas na cadeia Electron empacotada. A experiência física (som audível, janela no Windows real, multi-máquina física, jornada real) depende exclusivamente da validação física do owner. Nada além do descrito foi alterado.
