# F3.5.4E — Unificação DEFINITIVA do modelo de card (Desktop 1.0.196)

**Base**: tag `v1.0.195` @ `a4687480b32d7d55355ce0eba1ab19de78503425` (1.0.195 REPROVADA fisicamente por
inconsistência visual entre cards). Branch: `desktop/f354e-card-template-unification-1.0.196`.
*Nota de avanço do repositório*: a branch da 1.0.195 tem 2 commits ALÉM da tag (`81e8a4f`, `f5acb58`) —
são EXCLUSIVAMENTE o workflow de release da F3.5.4D (`.github/workflows/desktop-release-f354d.yml`);
zero código de produto. A base autorizada (tag) foi usada exatamente.

## AUDITORIA (25 itens — código real, ANTES de editar)

1. **Card "Em andamento"**: `kbv2Card(t,persp)` via `kbv2BoardHtml` — nos 5 quadros desktop (pessoa 4916*, Clientes 5053*, Designers 5084*, Social 5526*, Setores 5618). *(linhas da 1.0.194; +60 na 1.0.195)*
2. **Card "A Fazer"**: o MESMO `kbv2Card` **somente quando `body.desktop` está presente**. Abaixo da media query, o quadro de setor renderiza pelo ramo "Mobile (paridade APK)" — linha **5632**: `colTasks.map(taskCard)` → **tcv4**.
3. **Ambos passam por kbv2Card?** Só no ramo desktop. O dispatch é `if(isDesktop()){…kbv2BoardHtml…}` (5600) senão taskCard.
4. **Branch condicional dentro de kbv2Card**: nenhum por status; por DADOS sim — slot central: `edicao_cards`→`kbv2-card-notes` (6103); cron/vídeos→painel TEMAS; **demais tipos/sem conteúdo→NENHUM slot** (seção simplesmente ausente).
5. **Template legado taskCard**: EXISTE (5683) e está **VIVO** — caller real na 5632 (invisível ao grep `taskCard(` porque a chamada é `.map(taskCard)`, sem parêntese; o próprio comentário 5671 avisava "chamadas via .map(taskCard)").
6. **tcv4**: é o DOM do taskCard; CSS ativo para desktop nas linhas 526-646 (bandas `body.desktop .kanban .kcol…`) + estilos base mobile; helpers de runtime `isRealCard`/single-card (8734/8802) operam nele.
7-8. **Fallbacks edicao_midia/edicao_cards**: no kbv2 não trocam o shell, mas trocam o VISUAL do slot (notes SEM painel × temas COM painel).
9. **Fallback sem temas**: kbv2 não-cron sem legenda → slot central OMITIDO (card "curto").
10-11. **CSS por status/tipo**: por status não; por tipo sim — `.kbv2-card-note` é texto solto (2160-2163: sem borda/fundo/painel) enquanto `.kbv2-card-themes` tem caixa completa (2115-2119).
12. **Classe compacta no A Fazer**: não existe; a "compactação" percebida é o tcv4 (MQ) e/ou o slot ausente.
13. **Função por quadro**: kbv2 nos 5 (desktop); ramo mobile usa taskCard.
14. **DOM por status**: não (kbv2 idêntico).
15. **Quadro chamando renderizador antigo**: SIM — o ramo mobile é ALCANÇÁVEL NO APP DESKTOP (item 25).
16-17. **Realtime/snapshot**: `render()` reexecuta o MESMO dispatch; enquanto a MQ estiver abaixo de 1024px, cada snapshot re-renderiza o tcv4 (percebido como "modelo antigo voltando"); o listener `DESKTOP_MQ change` re-renderiza ao cruzar o limiar — os DOIS modelos aparecem na mesma sessão.
18-19. **Tarefa antiga/schema**: `cronOf` aceita `contents` legado e `cronContents` → mesmo shell; NENHUM gatilho por idade da tarefa.
20. **edicao_midia/edicao_cards ativam outro template?** Não no kbv2 (só o slot).
21. **Seletores/handlers dependentes**: delegação `[data-detail|move|cardmenu|resend|del]` (8604+ na 195), menu portal `.kbv2-more/.kbv2-menu`; drag (`isRealCard` tc/tcv4) SÓ existe no board tcv4.
22. **Suítes que aceitavam modelos diferentes**: nenhuma suíte pinava "renderizador único" nem cobria a matriz status×tipo do shell; o ramo mobile nunca foi coberto por gate.
23. **Por que F3.5.4B/C não detectaram**: (a) o grep de callers usou `taskCard(` — não captura `.map(taskCard)`; (b) a checagem "5 quadros usam kbv2BoardHtml" era verdadeira, mas só para o RAMO DESKTOP do dispatch; (c) o harness visual chama `kbv2Card` diretamente (bypassa `isDesktop()`/app shell), então nunca fotografou o tcv4.
24. **Por que os screenshots não mostraram**: cenas sempre ≥720px de largura útil (MQ alta), sem cenário "janela estreita/escala ≥125% com colsel"; e o card de Edição de Cards (LEGENDA solta) não foi posto LADO A LADO com o de temas na mesma coluna comparativa.
25. **CAUSA-RAIZ EXATA (tripla)**:
    - **(i) DOMINANTE — `isDesktop()` é só media query**: `const DESKTOP_MQ=matchMedia('(min-width:1024px)'); function isDesktop(){return DESKTOP_MQ.matches;}` (2559-2560). No APP ELECTRON, janela < 1024px CSS — janela restaurada pequena, ou tela em escala Windows 125%/150% (1366px físicos ÷ 1,5 = 911px CSS!) — derruba a MQ, remove `body.desktop` e TODOS os quadros caem no ramo mobile → `taskCard`/tcv4 legado convive com o kbv2 na mesma sessão.
    - **(ii) Slot central NÃO universal no kbv2**: LEGENDA como texto solto (sem painel) e tipos sem conteúdo SEM seção central — dois "sub-modelos" visuais dentro do próprio kbv2.
    - **(iii) Nome do responsável** em 1 linha com reticências (2072) — corta nomes longos.

## CORREÇÃO (cirúrgica)

A. `IS_ELECTRON_APP=!!window.api` e `isDesktop(){return IS_ELECTRON_APP||DESKTOP_MQ.matches}` — no app o shell desktop é SEMPRE usado, em qualquer janela/escala; navegador/APK preservados (MQ).
B. GUARDA no `taskCard`: primeira linha `if(isDesktop())return kbv2Card(t,persp);` — inalcançabilidade por CONSTRUÇÃO (defesa em profundidade; tcv4 mantido apenas para paridade mobile, sem risco).
C. PAINEL ÚNICO no slot central do kbv2: LEGENDA/OBSERVAÇÕES ganham a MESMA caixa do TEMAS (tokens idênticos: borda .07/raio 11/fundo .03/cabeçalho 10px/texto 13px/1.45, sem clamp, sem scroll, quebra natural) — via CSS de cascata (markup `kbv2-card-notes` PRESERVADO, pinado por suíte); EMPTY STATE discreto dentro do painel para tarefa sem temas e sem legenda (nenhum tipo fica sem a seção central); checklist com contagem também quando o array existe vazio.
D. Nome do responsável: até 2 linhas (clamp2 controlado) + tooltip integral (já existente).
E. Suíte nova `f354e-card-template-unification.test.mjs` (25 asserts da autorização: shell idêntico na matriz status×tipo×quadro, renderizador único, gates negativos tcv4) + gates de CI espelhados (fonte e app.asar).

Rollback: v1.0.195 e v1.0.194 preservadas (releases/tags intocadas); branch isolada; main intocada.
