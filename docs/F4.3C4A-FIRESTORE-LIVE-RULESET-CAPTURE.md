# F4.3C4A — Captura byte-exata (read-only) do ruleset Firestore ATIVO em produção

> **FASE ESTRITAMENTE READ-ONLY.** Nenhuma Rule alterada; nenhum ruleset/release criado ou
> publicado; nenhum deploy; nenhum documento Firestore acessado; nenhum dado real alterado.
> Desktop 1.0.191, Android, Card Premium, notificações, Worker J6 e portal **intocados**.

## 1. Finalidade
Capturar e preservar, de forma **histórica e imutável**, o ruleset Firestore **atualmente ativo em
produção** (byte-exato, identidade, metadados, hashes), para servir de **baseline canônica e
rollback** de qualquer fase futura. Isto **resolve** o item que a F4.3C3/F4.3C4 marcaram como
**"RULESET ATIVO NÃO COMPROVADO"** — agora está **COMPROVADO PELA CAPTURA**.

## 2. Método (fonte da verdade = API oficial, SOMENTE GET)
Workflow `.github/workflows/f43c4a-firestore-live-rules-capture.yml` (em `main`, `workflow_dispatch`,
`permissions: contents: read`), modelado no read-only audit já provado (`firestore-rules-live-audit.yml`):
1. **GET** `firebaserules.googleapis.com/v1/projects/agenda-id-seven/releases/cloud.firestore` → Release ativa → `rulesetName`.
2. **GET** `firebaserules.googleapis.com/v1/{rulesetName}` → `source.files[]` (conteúdo byte-exato).
3. **GET** `firestore.googleapis.com/v1/projects/agenda-id-seven/databases/(default)/collectionGroups/-/indexes` → índices (read-only).
4. **GET** da Release novamente → prova `before == after` (rulesetName + updateTime inalterados ⇒ **nenhuma publicação**).

Credencial: **service account já existente e autorizada** (secret de repositório, a mesma usada pelos
workflows de captura/auditoria já existentes) — **sem expor identidade/chave**; token de acesso
**mascarado** (`::add-mask::`); arquivo temporário da SA **removido ao fim**. **GET-only**; nenhum
`POST/PUT/PATCH/DELETE`; nenhum `firebase/gcloud/wrangler deploy`; nenhum `releases.create`/
`rulesets.create`/`patch`/`publish`/`update`/`delete`. **Run do CI:** `30412227718` (SUCCESS).

## 3. Release ativa + ruleset (COMPROVADO PELA CAPTURA)
| Campo | Valor |
|---|---|
| projectId | `agenda-id-seven` |
| service / releaseName | `cloud.firestore` / `projects/agenda-id-seven/releases/cloud.firestore` |
| release createTime / updateTime | `2026-05-19T01:31:45.839293Z` / **`2026-07-07T18:30:22.742581Z`** |
| **rulesetName (ATIVO)** | **`projects/agenda-id-seven/rulesets/809204d1-ee1b-444f-9b1b-2d6e0cb15132`** |
| ruleset createTime | `2026-07-07T18:30:22.453583Z` |
| arquivos | **1** — `firestore.rules.generated` |
| tamanho / linhas | **6131 bytes** / **161** |
| **SHA-256 (arquivo)** | **`527d4a3cb9091c374d54a0edf6b96ac8e05d2650f7d4e28aed2c438a671cb7ee`** |
| **SHA-256 canônico** | **`f39a405a0394c6d95e030dbe380e7c0ecb46674fa81c74677d49076e3c4f4668`** |
| método do canônico | por arquivo (ordenado por path): `sha256( utf8(path) ‖ 0x00 ‖ conteúdo ‖ 0x00 )` |
| release inalterada (before==after) | **true** (nenhuma publicação) |
| capturedAt / captureRunId | `2026-07-29T00:48:43.407Z` / `30412227718` |

### Preservação (histórica e imutável)
`ops/firestore-rules-live-captures/2026-07-29/`: `files/firestore.rules.generated`, `release-metadata.json`,
`ruleset-metadata.json`, `ruleset-manifest.json`, `SHA256SUMS`, `capture-verification-report.json`,
`indexes-readonly.json`, `baseline-vs-live.diff`. **A baseline antiga `ops/firestore-rules-baseline/`
NÃO foi substituída** (permanece byte-idêntica, SHA `0f621e2f…`).

> **Nota de integridade (verificável):** o conteúdo canônico veio do **GET da API oficial no CI run
> 30412227718** (artifact `8708827986`). O egress do sandbox bloqueia o download direto do artifact,
> então a cópia no repo foi reconstruída e **provada byte-idêntica por hash**: `sha256` do arquivo no
> repo = **`527d4a3c…` == valor computado no CI**, e canônico = **`f39a405a…` == CI**. Igualdade de
> hash ⇒ identidade de bytes (a cópia **é** o conteúdo retornado pela API). Os JSON de metadados são
> regenerados espelhando o builder do CI (descritivos; o byte-exato exigido é o do **ruleset**).

## 4. Índices (read-only)
GET dos índices **sucedeu** e retornou **0 índices compostos** (`indexes-readonly.json`, `indexCount: 0`).
Consistente com a auditoria F4.3C4 (nenhuma query rastreável exige índice composto; todas de campo único).

## 5. Estado das Rules vivas por coleção (COMPROVADO PELA CAPTURA)
| Coleção | read | create | update | delete |
|---|---|---|---|---|
| `users/{id}` | **`false`** | **`false`** | **`false`** | `false` |
| `events/{id}` | `true` | `size()<50` | `size()<50` | `true` |
| `tasks/{id}` | `true` | `size()<80` | `size()<80` | `true` |
| `chats/{chatId}` (+ `messages`) | `true` | size caps | size caps | `true` |
| `usersPublic/{uid}` | `true` | `false` | `false` | `false` |
| `passwordResetRequests` | `false` | schema-allowlist | `false` | `false` |
| `passwordResetCodes` | `false` | `false` | `false` | `false` |
| `notifLog` / `notifPrefs` | `isAdmin()` → efet. fechada (app sem Firebase Auth) | `false` | `false`/— | `false`/— |
| `/{document=**}` | `false` | `false` | `false` | `false` |

`request.auth` aparece **apenas** em `signedIn()`/`isAdmin()` (usados só nas leituras de `notifLog`/
`notifPrefs`). **Tudo que os clientes do app usam é anônimo** (`request.auth == null`): `usersPublic`
(read), `events`/`tasks` (read+write), `chats`/`messages` (read+write).

## 6. Comparação com baselines (classificação)
- **vs baseline comprovada (2026-06-23, `0f621e2f`, ruleset `2ebfb400`)** — **DIFERENTE** (COMPROVADO PELA CAPTURA + COMPARAÇÃO LOCAL; `baseline-vs-live.diff`):
  - `users`: `read/create/update` **`true`/`size()<60` → `false`** (agora **fechada** ao cliente).
  - **+ bloco `usersPublic`** (`read: if true`; escrita fechada).
  - + 3 linhas em branco no fim.
  - ⇒ **LIVE = baseline `2ebfb400` + usersPublic-read + users-read-close + users-create-update-close.** Isto **confirma pela captura** que os três patches (antes NÃO COMPROVADOS) **estão aplicados no LIVE**.
- **vs Rules-alvo F4.3C3 (deny-by-default)** — **DIFERENTE** (COMPARAÇÃO LOCAL): a live mantém `events/tasks/usersPublic` com leitura anônima e `events/tasks` com escrita anônima; a alvo exige `request.auth`. **Confirma F4.3C3/F4.3C4:** a Rules-alvo **não pode** ser implantada enquanto a Desktop 1.0.191 (anônima) estiver em produção.

## 7. Limitações / riscos restantes
- A cópia do **ruleset** no repo é **byte-exata e hash-provada** (`527d4a3c`/`f39a405a` == CI). Os **JSON de metadados** são regenerados espelhando o CI (descritivos; não são a fonte canônica de bytes do ruleset).
- Identidade da service account **não divulgada** (por segurança).
- **Sem acesso à produção a partir do sandbox** (egress bloqueado): a captura viva rodou no runner do GitHub (CI); o artifact não pôde ser baixado no sandbox (403 no proxy) — o conteúdo foi trazido via **log do CI** (base64) e **reconstruído com verificação por hash**.
- O **ruleset ativo agora é COMPROVADO** (supera o "NÃO COMPROVADO" da F4.3C3/F4.3C4). Uma re-captura será necessária antes de qualquer deploy futuro se a produção mudar.

## 8. Prova de ausência de deploy / de acesso a dados
`capture-verification-report.json`: `deployExecuted:false`, `productionDataAccessed:false`,
`productionDataModified:false`, `dataAccess:{documents:false,runQuery:false,batchGet:false,commit:false}`,
`gates.releaseUnchanged:true`. Endpoints usados: **somente** `firebaserules` (releases/ruleset, GET) +
`firestore …/indexes` (GET). **Nenhum** `/documents`, `:runQuery`, `:batchGet`, `:commit`.

## 9. Isolamento / componentes congelados
Alterações **somente** em `ops/firestore-rules-live-captures/2026-07-29/**` + `docs/F4.3C4A-*.md`
(no branch de feature) e no **workflow read-only de captura** em `main`. **Diff vazio** comprovado em:
`desktop/`, `android-native*/`, `functions/`, `cloudflare-worker.js`, `wrangler.toml`, portal,
Card Premium, notificações, `ops/firestore-rules-baseline/` (baseline antiga intocada).
- **Card Premium intacto** (nenhuma alteração/deploy em `cloudflare-worker.js`/Worker J6/`/share`/OG/tokens/links/portal).
- **Notificações intactas** (nenhuma alteração em `main.ts`/`cardsRules.js`/`slaRules.js`/`notifier.ts`/`bgNotify.ts`/`tray.ts`/`firebase.ts`/`updaterService.ts`/`slaScheduler.ts`).
- **Desktop 1.0.191 intacta** e permanece **Latest**. **Android intacto.** **Worker J6 intacto.**
- **APK oficial (Artifact ID 8701571306) preservado intacto** (esta fase não gera/toca APK/AAB).

## STATUS FINAL
### ✅ RULESET ATIVO CAPTURADO E COMPROVADO — AGUARDANDO F4.3C4B
`rulesetName 809204d1-…` · arquivo `527d4a3c…` · canônico `f39a405a…` · release inalterada · sem
deploy · sem acesso a dados · sem produção alterada. **STOP** — não inicio a alternativa A, não
promovo Rules, não inicio a F4.3C4B nem canário, sem autorização explícita do owner.
