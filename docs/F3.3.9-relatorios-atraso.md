# Agenda ID Seven — Desktop · Relatórios de Atraso + Exportação (F3.3.9)

> **APROVADA no reteste real.** Aba **nova, aditiva e 100% read-side** para relatórios de atraso
> (por designer, cliente, tipo, período), tarefas críticas, reincidências, histórico por tarefa e
> **exportação LOCAL CSV/JSON** — reaproveitando a base aprovada do Painel Executivo **F3.3.8 sem alterá-la**,
> e sem tocar Kanban, SLA Monitor, notificações, WhatsApp premium, Editar prazo, write-path nem IPC/`main.ts`.
> Guardada por teste executável: `desktop/scripts/f339-reports.test.mjs` (41 asserts) + a suíte de regressão verde.

---

## 1. Build aprovada
- **Versão:** `1.0.146-beta-board-rebuild` (EXE/MSI Windows).
- **Run:** `27888317172` — `[manual] Desktop Build (Windows)`, conclusion **success**.
- **Commit (ref) buildado:** `e6e866ecc50836b3a73c801a3686dbaa2e74b572` (FASE B).
- **SHA-256 EXE** (`Agenda-ID-Seven-Desktop-1.0.146-x64.exe`): `9f96201f177e5f69d43861b08d45f1ae2727faa67078660fd73cf7d3f2860636`
- **SHA-256 MSI** (`Agenda-ID-Seven-Desktop-1.0.146-x64.msi`): `9b8612ef72cd59f44b7bf1d5221474ef2e388ba9913c04ce899ee7554e48b0c9`
- **Renderer empacotado == auditado:** ✅ prova byte-a-byte (passo do build) verde.

---

## 2. O que é (arquitetura congelada)
- **Nova aba `relatorios`** ("Relatórios", ícone `bars`) — entrada de navegação **separada e aditiva**
  (`TABS` + rota `state.tab==='relatorios'` no `render()`). Não altera nenhuma tab aprovada.
- **Bloco sentinela `/* F3.3.9-REPORTS-CORE BEGIN/END */`** em `desktop/src/renderer/index.html`:
  - **REUSA, sem alterar, o `F3.3.8-EXEC-CORE`** (`slaExecAggregate`/`execApplyFilters`/`execTaskDesignerId`/`execSevHex`/`execEnsureStyle` + componentes `execKpi`/`execDesRow`/`execMiniList`/`execPill`).
  - `slaExecReports(tasks,users,now,filtros)` — agregador puro: KPIs + período (buckets/dia) + reincidentes (`execTaskLateCount` via `history`+overdue) + histórico (linha do tempo) + `taskRows`.
  - `reportsFilter` (filtros client-side) · `toCSV` (separador `;`, escape) · `execDownload` (Blob + `<a download>`).
- **Exportação LOCAL:** CSV/JSON via `Blob`/download no renderer. **SEM IPC/`main.ts`, SEM Firestore-write, SEM upload, SEM backend.**

## 3. Relatórios (10) · Filtros (7) · Exportação
- **Relatórios:** por designer · por cliente · por tipo · por período · tarefas críticas · reincidentes em atraso ·
  média de atraso · % entrega no prazo · ranking de risco (ordenação do "por designer") · histórico resumido por tarefa.
- **Filtros:** período · designer · cliente · tipo · status SLA · **só atrasados** · **conclusão (concluídas/não)**.
- **Exportação:** CSV (tarefas em atraso) · JSON (relatório estruturado completo) — arquivos locais (pasta Downloads).

## 4. Invariantes (read-only) — guardadas por `f339` (41) + regressão
- **Zero escrita / zero provider / zero IPC:** sem `.set/.update/setDoc/addDoc/deleteDoc`, sem `db.collection`/`firestore`, sem `fetch`, sem `ipcRenderer`/`require('electron')`/`desktopAPI` no bloco REPORTS-CORE.
- **Exportação 100% local** (`new Blob` + `.download`).
- **F3.3.8 EXEC-CORE preservado** (reuso, sem redefinir `slaExecAggregate`).
- **Não cria evento, não altera tarefa/status/prazo.** Criar/mover/editar continua só pelo Kanban.
- **Aditiva e removível:** apagar o bloco `REPORTS-CORE` + a entrada de nav `relatorios` volta ao estado anterior.
- **Regressão verde no fechamento:** `f339=41 · f338=52 · f337=73 · f336=148 · f335=46 · f334b=72 · f334=30 · f333=146 · f332-panel=71 · e2e=APROVADO · tsc=0 · renderer-syntax=OK`.

## 5. PROIBIDO mexer sem auditoria + autorização explícita
Não ligar qualquer escrita (Firestore/provider), não usar IPC/`main.ts` sem aviso de risco, não acoplar ao
write-path, não alterar o `F3.3.8-EXEC-CORE`/SLA Monitor/Kanban/card/Caixa de Temas/login/notificações/WhatsApp/
Editar prazo. Sem deploy/release. Mudanças no agregador exigem atualizar `f339` e provar não-regressão na suíte.

---

## 6. Histórico
- **F3.3.9-DESKTOP FASE A** — diagnóstico + plano + 4 mockups (overview/críticas/histórico/vazio) **aprovados**.
- **F3.3.9-DESKTOP FASE B** — implementação read-side (commit `e6e866e`) + `f339` (41); regressão verde; prints da implementação aprovados.
- **F3.3.9-DESKTOP build** — EXE/MSI `1.0.146-beta-board-rebuild` (run `27888317172`) **aprovada no reteste real**. Renderer empacotado == auditado. Sem deploy. **CONSOLIDADA.**
