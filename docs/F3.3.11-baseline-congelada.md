# F3.3.11 — Baseline Congelada (Consolidação Anti-Regressão)

> **Status:** APROVADO em teste real e **CONGELADO**. Esta página é a fonte da verdade do que
> NÃO pode ser alterado em fases futuras. Mudança em qualquer item abaixo = **regressão**.

## 1. O que foi aprovado nesta baseline
1. **Notificações premium em background (Desktop):** app aberto → toast premium in-app; app
   minimizado e app fechado no X/bandeja → **janela premium própria** (`backgroundNotificationWindow`)
   no canto inferior direito; Central de notificações; SLA laranja/vermelho/crítico.
2. **Card Premium WhatsApp:** Worker saudável; rota `/share/cronograma/<token>`; OG tags premium;
   `og:image` (1200×630, image/jpeg); **cache-bust `?v=<timestamp>`** no link enviado ao WhatsApp.

## 2. O que está CONGELADO (intocável)
| # | Item | Onde vive | Estado |
|---|---|---|---|
| 1 | Notificações premium background | `desktop/src/main/main.ts` (`deliverNotification`) | CONGELADO |
| 2 | `backgroundNotificationWindow` | `desktop/src/main/bgNotify.ts` + `src/preload/bgnotify-preload.ts` + `src/renderer/bgnotify.html` | CONGELADO |
| 3 | Notifier do MAIN (long-polling) | `desktop/src/main/notifier.ts` + `firebase.ts` | CONGELADO |
| 4 | Central de Notificações | `desktop/src/renderer/index.html` (`notifHistoryAppend`) | CONGELADO |
| 5 | SLA laranja/vermelho/crítico | `index.html` (`notifScanSla`) + SLA Monitor | INTOCÁVEL |
| 6 | Card Premium WhatsApp / OG / og:image | `cloudflare-worker.js` (`shareCardHtml`, `ogClientMeta`, `OG_IMG_PATH`) | INTOCÁVEL |
| 7 | Rota `/share/cronograma` e `/cliente/cronograma` | `cloudflare-worker.js` | INTOCÁVEL |
| 8 | Cache-bust `?v=` (link WhatsApp) | `index.html` (`withWhatsAppPreviewBust` + `buildClientMessage`) | CONGELADO |
| 9 | Kanban, Painel Executivo, Relatórios, Editar prazo | renderer | INTOCÁVEL |
| 10 | Android, Firebase, Cloudflare | — | NÃO TOCAR |

## 3. PROIBIDO em fases futuras
- **Notificação aprovada NÃO pode ser reimplementada** (nem o canal toast×bg-window, nem dedup, nem Central).
- **Card premium aprovado NÃO pode ser redesenhado** (OG/og:image/dimensões/twitter intactos).
- **Cache-bust `?v=` aprovado NÃO pode ser removido** do link enviado ao WhatsApp.
- **Worker NÃO deve ser alterado** para o problema de preview do WhatsApp (a solução é client-side: `?v=`).
- Não mexer em rota `/share/cronograma` nem `/cliente/cronograma`.

## 4. Builds / commits de referência
- **Commit cache-bust WhatsApp:** `629193c` (`share-card: cache-bust automático no link do WhatsApp`).
- **Commit background notifications:** `1ad514b` (`F3.3.10-BG: backgroundNotificationWindow`).
- **Worker (intocado nesta baseline):** último commit `6d46bce` (anterior a esta fase).
- **Build Desktop consolidado (notif bg + cache-bust)** — run #179 `27920765764`, commit `a062d7c`:
  - EXE `Agenda-ID-Seven-Desktop-1.0.146-x64.exe` — SHA-256 `a48e402974f4466ebc26a0fd2bfa8e66b78259b5072298c632acf8fa8575322d`
  - MSI `Agenda-ID-Seven-Desktop-1.0.146-x64.msi` — SHA-256 `ff5b11703ac8606eb0ad0f7165630d1eefa7aa2624b2535514a71db8c066c290`
  - Renderer empacotado == auditado (byte a byte) ✅
- **Prova de canal em Windows REAL (bg-window):** run `27919294351` (log: `deliver.bg channel="bg-window"`, `bg.rendered`, `fallbackNative:false`).
- **Prova viva do card premium (servidor):** run `27919943633` (token real → `PREMIUM_OG=YES`, og:image 200/jpeg).

## 5. Regras de regressão obrigatórias
- **Antes de qualquer fase futura, rodar `f3311-baseline-freeze.test.mjs`** (blindagem). Se falhar, a baseline foi violada → reverter.
- Rodar a suíte completa (`f33A`…`f33F`, `f33[2-9]`, `e2e-flow`) + `tsc` + renderer syntax.
- Diff de qualquer fase de manutenção deve manter os invariantes desta página.

## 6. Como testar NOTIFICAÇÕES sem quebrar
- **Não** alterar `main.ts`/`bgNotify.ts`/`notifier.ts`/`firebase.ts`.
- Prova de canal em Windows real, read-only, gated: workflow `desktop-notif-selftest.yml`
  (`IDSEVEN_SELFTEST=1`) → confirma `visível→toast`, `minimizado/bandeja→bg-window` no log
  `%APPDATA%\agenda-id-seven-desktop\idseven-notif-diag.log`. NÃO grava Firestore, NÃO faz deploy.
- Testes lógicos: `f33E` (main notifier), `f33D` (renderer background), `f33F` (bgNotify), `f33A` (Central).

## 7. Como testar o CARD PREMIUM sem mexer no Worker
- **Não** editar `cloudflare-worker.js`.
- Diagnóstico vivo read-only por CI: `wa-card-live-diag.yml` (curl com UA `facebookexternalhit`/`WhatsApp/2.0`)
  → confirma OG premium + og:image 200/jpeg para um token real. `wa-share-meta-probe.yml` confirma
  `og:image:width/height/secure_url`. `wa-og-image-probe.yml` confirma bytes/dimensões.
- Se o card aparecer genérico, é cache/scrape do WhatsApp/Meta → **reenviar (o link já leva `?v=`)**
  ou re-scrape no Facebook Sharing Debugger. **Nunca** alterar o Worker para isso.

## 8. Como validar o CACHE-BUST sem alterar rota/OG
- O cache-bust é **client-side**, só no link da mensagem: `withWhatsAppPreviewBust(buildShareClientUrl(token))`
  em `buildClientMessage`. Formato: `…/share/cronograma/<token>?v=<timestamp>` (ou `&v=` se já houver query).
- O Worker **ignora a querystring** (rota casa por `url.pathname`) → serve o MESMO card premium.
- O link **salvo** no Firestore (`clientReviewUrl`, via `buildPublicClientUrl`) **NÃO** leva `?v=`.
- Provas automatizadas: `e2e-flow-test.js` (checks `BUST0..5`, `BUST_MSG`, `BUST_NOCLI`, `CLEAN4`, `MEDIA_LINK`).

## 9. Próxima fase recomendada
- **Opcional (belt-and-suspenders), só com autorização:** motor de SLA **main-side** (independente do
  renderer) para o cenário de renderer congelado — hoje o SLA é detectado no renderer (com switches
  anti-throttle, provados) e renderizado pela janela premium. **Não** é bloqueante; a baseline atual
  está aprovada.
- Qualquer nova fase começa rodando `f3311-baseline-freeze.test.mjs` antes e depois.
