# Agenda ID Seven — Desktop · Painel Executivo de SLA & Produtividade (F3.3.8)

> **APROVADA no reteste real.** Esta fase entregou uma **tela nova, aditiva e 100% read-side**
> para controle executivo de SLA, produtividade, atrasos e desempenho dos designers — sem tocar
> no Kanban, no SLA Monitor, nas notificações Desktop, no WhatsApp premium, no Editar prazo nem
> no write-path. Guardada por teste executável: `desktop/scripts/f338-exec-panel.test.mjs` (52 asserts)
> + a suíte completa de regressão verde.

---

## 1. Build aprovada
- **Versão:** `1.0.146-beta-board-rebuild` (EXE/MSI Windows).
- **Run:** `27887475695` — `[manual] Desktop Build (Windows)`, conclusion **success**.
- **Commit (ref) buildado:** `701bcb200129ec918c049c1b1fe68307e7aaf165` (FASE B).
- **SHA-256 EXE** (`Agenda-ID-Seven-Desktop-1.0.146-x64.exe`): `b122947f042da96377754cee50a8ebef4745d6a5ff9f89c8e2b3116a6b280315`
- **SHA-256 MSI** (`Agenda-ID-Seven-Desktop-1.0.146-x64.msi`): `905a42e52225f074d6b4a6c6bc32ff0a9f00cfb5de24873f5545dbcb05ee2deb`
- **Renderer empacotado == auditado:** ✅ prova byte-a-byte (passo do build) verde.

---

## 2. O que é (arquitetura congelada)
- **Nova aba `exec`** ("Executivo", ícone `gauge`) — entrada de navegação **separada e aditiva**
  (`TABS` + rota `state.tab==='exec'` no `render()`). Não substitui/altera nenhuma tab aprovada.
- **Bloco sentinela `/* F3.3.8-EXEC-CORE BEGIN/END */`** em `desktop/src/renderer/index.html`:
  - `slaExecAggregate(tasks,users,now,filtros)` — **agregador puro/determinístico** (sem efeitos).
  - `execApplyFilters` · `execRiskScore` · `execWithinPeriod` · `renderExecPanel()` · `afterExecPanel()` · `EXEC_CSS`.
- **Fontes de dados:** `state.tasks`/`state.users`/`state.user` (snapshot já em memória). **Zero leitura nova do Firestore, zero escrita.**

## 3. Reuso das fontes CONGELADAS (sem alterá-las)
`resolveTaskDisplayState` · `isTaskCompleted` · `deriveCanonicalTaskState` · `slaPanelFinishMs`/`resolveCanonicalDeadline` ·
`slaMMSSfmt` · `designerAssignment` · `designerSla`. O painel **deriva** desses; não duplica limiares nem reescreve regra.

## 4. Métricas entregues
Total ativas · no prazo · alerta laranja · atrasadas · críticas · SLA por designer · % entrega no prazo por designer ·
atraso médio por designer · qtd por designer · carga atual · clientes com mais atrasos · tipos com mais atraso ·
ranking de risco · tarefas críticas recentes · próximos vencimentos · distribuição de SLA ·
filtros por período/designer/cliente/tipo/status SLA. Estados **vazio / denso / crítico** conforme mockups aprovados.

## 5. Invariantes (read-only) — guardadas por `f338` (52 asserts) + regressão
- **Zero escrita / zero provider:** sem `.set/.update/setDoc/addDoc/deleteDoc`, sem `db.collection`/`firestore`, sem `fetch` no bloco EXEC-CORE.
- **Não cria evento, não altera tarefa/status/prazo.** Criar/mover/editar continua **exclusivamente** pelo Kanban.
- **Aditiva e removível:** apagar o bloco `EXEC-CORE` + a entrada de nav `exec` volta ao estado anterior, sem efeito colateral.
- **Regressão verde no fechamento:** `f338=52 · f337=73 · f336=148 · f335=46 · f334b=72 · f334=30 · f333=146 · f332-panel=71 · e2e=APROVADO · tsc=0 · renderer-syntax=OK`.

## 6. PROIBIDO mexer sem auditoria + autorização explícita
Não alterar o painel de forma que ligue qualquer escrita (Firestore/provider), não acoplar ao write-path, não fundir
com o SLA Monitor aprovado, não alterar Kanban/card/Caixa de Temas/login/notificações/WhatsApp/Editar prazo.
Sem deploy/release. Mudanças no agregador exigem atualizar `f338` e provar não-regressão na suíte.

---

## 7. Histórico
- **F3.3.7-DESKTOP** — triagem read-only das próximas frentes Desktop (Painel Executivo recomendado e escolhido).
- **F3.3.8-DESKTOP FASE A** — arquitetura + 4 mockups (overview/denso/crítico/vazio) **aprovados** visual e tecnicamente.
- **F3.3.8-DESKTOP FASE B** — implementação read-side (commit `701bcb2`) + `f338` (52); regressão verde; prints da implementação aprovados.
- **F3.3.8-DESKTOP build** — EXE/MSI `1.0.146-beta-board-rebuild` (run `27887475695`) **aprovada no reteste real**. Renderer empacotado == auditado. Sem deploy. **CONSOLIDADA.**
