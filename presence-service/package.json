{
  "name": "idseven-presence-canary",
  "version": "0.1.0-canary",
  "private": true,
  "description": "Serviço CANÁRIO de presença em tempo real (Cloudflare Worker + Durable Object SQLite + WebSocket Hibernation). Totalmente isolado do Worker J6.",
  "type": "module",
  "scripts": {
    "test": "node test/ticket.test.mjs && node test/presence.test.mjs && node test/presenceHub-contract.test.mjs",
    "typecheck": "tsc --noEmit",
    "deploy": "wrangler deploy",
    "deploy:stage2": "wrangler deploy -c wrangler.stage2.toml",
    "secret:ticket": "wrangler secret put PRESENCE_TICKET_SECRET"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}
