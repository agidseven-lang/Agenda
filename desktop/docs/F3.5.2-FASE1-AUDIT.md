# F3.5.2 — FASE 1 (auditoria READ-ONLY) — Edição de Cards (Desktop)

**Entregue ANTES de qualquer edição.** Base congelada: Desktop **1.0.186**, tag `v1.0.186`, commit `e87ef4b59c4edc6c6fd6249fd7f4ac07b6413048` (confirmado = **Latest**, `immutable:true`). Branch isolado: `desktop/f352-edicao-de-cards` (criado de `e87ef4b`).

Hashes publicados 1.0.186 (rollback pronto): EXE `44fd70e…`, EXE.blockmap `fb7ffa8…`, MSI `babb6d0…`, latest.yml `19c7c98…`, SBOM `f92b135…`, SHA256SUMS `7e0703c…`.

**VEREDITO: `frozenTouchRequired = FALSE` · `pararReportar = FALSE`.** A implementação é 100% aditiva e isolável. NENHUM contrato congelado (Worker/portal/Card Premium/WhatsApp/Firestore Rules/Android/Functions/Cloud Run) precisa mudar. A condição de "PARAR E REPORTAR" do owner **não** é acionada.

---

## 1. Arquitetura atual (verificada file:line)

**Modelo de setores** — `desktop/src/renderer/index.html`
- `SECTORS` (2320-2328): `edicao_midia` ('Edição de mídia'), `cronograma`, `copywriting` (`descontinuado:true`, oculto p/ novas), `roteiro`, `programacao_posts`. `SECTOR_ALIAS` (2329). `secOf()` (2330).
- **Portão-mestre do fluxo-cliente/Card Premium** = `isClientSector(k)` (2335) = `k==='cronograma'||k==='roteiro'` — **e SOMENTE isso**. (Espelhado em `slaRules.js:326`.)
- `STATUS` (2336-2340): `afazer`/`andamento`/`revisao`/`concluido` — workflow interno canônico de 4 estados.
- `TEMPLATES` (2350-2372): `edicao_midia` usa itens por-vídeo (`videoTema`, `t.videos[{id,n,tema}]`); `cronograma`/`roteiro` usam blocos `contentCount` (tema+legenda) + `clientRequired`.

**Wizard "Nova tarefa"** — `index.html`
- Estado único `state.form` = `newForm()` (7867); `openNewTaskWizard()` (7874). `renderForm()` (7883) = casca genérica com `STEPS=['Setor','Dados','Briefing','Revisão']` (7884), dirigida por `f.sector`/`f.step` — herda o 5º setor automaticamente.
- `stepSector()` (7893-7897) itera `SECTORS.filter(!descontinuado)` — o 5º card aparece sozinho ao adicionar a entrada.
- `stepDados()` (7898-7931): ramos `isCron`/`isRoteiro`/`isMedia`. `stepBriefing()` (7941-7972): repetidores `contents`/`videos`/`items`. `stepReview()` (7984-8032). `afterForm()` (8048-8065) faz o bind `oninput → state.form` (preserva dados ao navegar). `stepNext()` valida por etapa (8340-8344).
- **Criação** = `saveTask()` (8076-8138): trava `_saving` (8080); `data` canônico (8096); `videoTema` (8100); `edicao_midia` grava `designerAssignment`+`designerSla` (8110-8120); **gate cliente** `isClientSector` → `cronStatus`/`clientFlowStatus` (8122-8123); **único** `db.collection('tasks').add(data)` (8125); `openSendClientModal` só se `sendAfter && isClientSector` (8131).

**Notificações / SLA** — `desktop/src/main/slaRules.js` + `slaScheduler.ts`
- `SECTOR_SLA` (83-87): cronograma 30/10 (critical), edicao_midia 40/20, roteiro `designerSla:false`. **Default** (`slaCfgDefault`, 88) = 30/10. `warningMinutes` = ANTES do prazo; `overdueGraceMinutes` = tolerância **APÓS** o prazo.
- `resolveTaskDisplayState()` (60-80): **o overdue/vermelho nasce NO prazo** (`now>=finishMs`, linha 72) — **incompatível** com "vermelha 10 min ANTES do prazo".
- `slaEmissionsFor()` (179-224): só emite ao designer responsável; **guarda de setor** para roteiro em **189** (`return []`); só entra em painel se `t.designerSla` semeado (`inPanel`, via 198).
- `slaScheduler.ts`: `emitDue()` (loop ~129, chamada `slaEmissionsFor` ~132); ingestão do listener é **sector-agnóstica** (grava todo doc, ~193). `nextBoundaryMs` (274) exige `designerSla`.

**Kanban/Minhas/buscas/contadores/Agenda**: derivam de `secOf().key`/`t.status`/`t.assigneeId`; a Agenda opera sobre `state.events` (separado de `tasks`) — prazos de tarefa **nunca** entram na Agenda hoje.

## 2. Design de isolamento (por CONSTRUÇÃO, aditivo)

`edicao_cards` = setor **NÃO-cliente** novo. O isolamento é garantido por **OMISSÃO**, não por bloqueio:
1. **NÃO** adicionar `edicao_cards` a `isClientSector` (2335/326) ⇒ estruturalmente: `canSendToClient=false`, `cronStatus`/`clientFlowStatus` nunca gravados, `openSendClientModal` nunca abre, `ensureReviewToken`/`ensureStableClientReviewToken`/`buildShareClientUrl`/`buildClientMessage` (48 occ.) nunca alcançados ⇒ **Card Premium/Worker/portal/WhatsApp/Rules intocados**.
2. **NÃO** semear `designerSla` nos cards + **adicionar `edicao_cards` à guarda `slaEmissionsFor` (189)** (precedente literal do roteiro) ⇒ o produtor SLA **compartilhado** nunca emite p/ cards; cronograma/edicao_midia/roteiro **byte-idênticos**. A máquina congelada `resolveTaskDisplayState` **não é reusada**.
3. **Perfil de notificação próprio, isolado** (funções novas `cardsDisplayState` + `cardsEmissionsFor` em `slaRules.js`, chamadas por **1 linha aditiva** em `slaScheduler.emitDue`): amarela em `[due−30, due−10)`, vermelha em `[due−10, due)` (a vermelha **substitui** a amarela), dedup própria `taskId+notificationType+dueAt+deadlineRevision`. Tarefa criada dentro da janela: T-30..T-11 → só amarela 1×; ≤T-10 → só vermelha 1×; sem avalanche.
4. **Criação N-docs isolada**: early-branch no topo de `saveTask` (após `_saving`) → `saveCardsBatch()` cria N docs **atômicos** via `db.batch()` (tudo-ou-nada, ≤500). O `add()` único (8125) e todos os ramos de setor existentes (8096-8123) ficam **byte-idênticos** (early-return antes deles). Cada card reusa o shape canônico (title=tema, sector, status:'afazer', assignee/assigneeId, dueDate/dueTime/due, by, createdAt, src) + `designerAssignment.designerId` (roteia ao quadro do designer) **SEM `designerSla`** + campos aditivos `legenda`/`obs`/`internal:true`/`batchId`.
5. **Rename 100% visual**: trocar SÓ a string `label` em `index.html:2321` ('Edição de mídia'→'Edição de vídeos'); `key:'edicao_midia'` preservada. Nenhuma lógica compara o label (tudo usa `secOf().key`). A cópia vestigial em `slaRules.js:20` nunca é exibida ⇒ não editar.

## 3. Arquivos a ALTERAR (mínimo)

| Arquivo | Região | O quê |
|---|---|---|
| `desktop/src/renderer/index.html` | 2321 | Rename label `Edição de mídia`→`Edição de vídeos` (key preservada) |
| `desktop/src/renderer/index.html` | após 2328 | +entrada `edicao_cards` em `SECTORS` |
| `desktop/src/renderer/index.html` | 2372 | +`TEMPLATES.edicao_cards` |
| `desktop/src/renderer/index.html` | 7867 | `newForm()` +`f.cards=[]` |
| `desktop/src/renderer/index.html` | 7898-7931 | `stepDados()` ramo `isCards` (designer obrig.; oculta prazo único) |
| `desktop/src/renderer/index.html` | 7941-7972 + 8318-8328 | `stepBriefing()` repetidor por-card + handlers add/del |
| `desktop/src/renderer/index.html` | 8048-8065 | `afterForm()` binds dos campos por-card |
| `desktop/src/renderer/index.html` | 7984-8032 | `stepReview()` ramo listando N cards |
| `desktop/src/renderer/index.html` | 8340-8344 | `stepNext()` validação Briefing p/ cards |
| `desktop/src/renderer/index.html` | 8076-8138 | early-branch → **nova** `saveCardsBatch()` (lote atômico) |
| `desktop/src/main/slaRules.js` | 189 | estender guarda p/ curto-circuitar `edicao_cards` no produtor compartilhado |
| `desktop/src/main/slaRules.js` | append + export | **novas** `cardsDisplayState`/`cardsEmissionsFor` (+`cardsNextBoundaryMs`) |
| `desktop/src/main/slaScheduler.ts` | `emitDue()` após ~133 | 1 linha aditiva chamando `cardsEmissionsFor` (+ boundary p/ wake preciso) |
| `desktop/package.json` + `package-lock.json` | 3 (+lock 3,9) | versão 1.0.186 → 1.0.187 |
| `desktop/scripts/` | novos `f352-*.test.mjs` | testes isolados (62 asserções) |
| `.github/workflows/` | **novos** build + release (1.0.187) | pinados; não editar os workflows congelados |

## 4. Arquivos/regiões BYTE-IDÊNTICOS (congelados)

`isClientSector` (2335) · `SECTOR_ALIAS` (2329) · `STATUS` (2336-2340) · `resolveTaskDisplayState` (renderer + slaRules 60-80) · `canSendToClient` · bloco `cronStatus`/`clientFlowStatus` (8122-8123) · `openSendClientModal` (8131) · `ensureReviewToken`/`ensureStableClientReviewToken`/`buildShareClientUrl`/`buildClientMessage` + todo o Card Premium/fluxo-cliente (~1460, 48 occ.) · `add()` único (8125) + ramos de setor (8096-8120) · `SECTOR_SLA` (83-87) · `notifier.ts` · `main.ts` · `updaterService.ts` · `tray.ts` · `auth*.ts`/`firebase.ts`/`clockSync.ts`/`bgNotify.ts`/`reminder.ts`/preload · **`worker/` inteiro** · Firestore Rules · Functions · Cloud Run · Android · workflows pinados existentes.

## 5. Matriz de impacto (componentes que o owner exige preservar)

Todos = **IMPACTO ZERO / byte-idêntico em comportamento**, porque `edicao_cards` é aditivo e não-cliente:

Card Premium (imagem/título/descrição), geração/persistência de links, Cronograma, Roteiro, aprovação de temas, aprovação final, solicitação de alteração, **notificações amarelas/vermelhas existentes** (guarda 189 + não-reuso da máquina congelada), **SLA dos setores existentes** (edicao_cards fora de `SECTOR_SLA`), Agenda (opera sobre `events`, não tocada), Kanban (deriva de `secOf().key`/status), tarefas existentes (net-new docs; sem migração), eventos, chats, tray, fechamento no X, atualizador interno, Worker J6, portal do cliente, Firestore Rules (create=signedIn, sector-agnóstico), Functions (`onTaskCreated` sector-agnóstico), Cloud Run (sem acoplamento), dados reais (sem backfill).

Notas comportamentais (reuso, não mudança de contrato): (a) cada card criado dispara `onTaskCreated → 1 push FCM` ao designer (igual ao edicao_midia hoje) = a "notificação imediata" de nova tarefa; (b) o rename passa a exibir tarefas edicao_midia existentes como "Edição de vídeos" (pedido explícito do owner, visual).

## 6. Prova de isolamento (resumo)

`grep 'edicao_cards'` em todo o repo = **0 ocorrências** ⇒ cada ponto de toque é entrada nova ou ramo `=== 'edicao_cards'`; nenhuma key de setor existente é comparada contra valor alterado. O fluxo-cliente é excluído por **omissão** em `isClientSector`; o SLA compartilhado por **omissão** em `SECTOR_SLA` + guarda em 189 + não-semear `designerSla`; a criação por early-return antes de todo o código legado. Detalhe de agendamento (wake preciso em T-30/T-10 sem `designerSla`): `cardsNextBoundaryMs` contribui os marcos ao `nextBoundary`, com o re-eval de segurança (~60s) como rede.

## 7. Testes (a criar, 62 asserções) + gates

Novos `desktop/scripts/f352-*.test.mjs`: isolamento SECTORS/`isClientSector` + rename (key preservada); shape/atomicidade do lote N-docs; produtor T-30/T-10 dos cards + **prova de que setores existentes ficam byte-idênticos**; region-invariance vs 1.0.186 (regiões congeladas). Suítes herdadas verdes (f343 SLA golden master, f344 flow golden master, updater state-machine, entrega visual, harness independente). Workflow de build roda cada teste como step explícito (não há auto-descoberta).

**Conclusão:** proceder à implementação isolada; publicar 1.0.187 apenas após todos os gates verdes; rollback para 1.0.186 pronto. Se, na implementação, qualquer etapa exigir tocar um contrato congelado → PARAR e apresentar estratégia alternativa isolada.
