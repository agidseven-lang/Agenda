# F3.5.2 — PRE-BUILD HARDENING · CHECKPOINT FINAL

Fase autorizada: **F3.5.2-PREBUILD-HARDENING** (7 gates obrigatórios antes de qualquer build da
Desktop 1.0.187). Esta execução **NÃO** cria workflow de build, **NÃO** gera EXE/MSI e **NÃO**
publica. STOP após o checkpoint.

- **Branch:** `desktop/f352-edicao-de-cards`
- **Base congelada:** Desktop **1.0.186** · tag `v1.0.186` · commit `e87ef4b59c4edc6c6fd6249fd7f4ac07b6413048`
- **Versão candidata:** **1.0.187** (`package.json` + `package-lock.json`)
- **tsc `-p tsconfig.json --noEmit`:** sem erros ✓

---

## 1) Os 7 gates de hardening

### Gate 1 — `f351f-rollforward-equivalence` restaurado byte-idêntico (histórico, não é gate da F3.5.2)
- Restaurado **byte-idêntico** à base 1.0.186 — `git diff e87ef4b -- …/f351f-rollforward-equivalence.test.mjs` = **vazio**.
- **Não** rodado como gate da F3.5.2; **não** deletado/modificado/reancorado/enfraquecido. Preservado como evidência histórica.
- Na candidata ele fica **vermelho** — comportamento **esperado**: prova que o *hotfix* 1.0.186 era roll-forward byte-idêntico da 1.0.184; a *feature* F3.5.2 diverge por construção. (Classificado no Gate 6.)

### Gate 2 — `cardsRules.js` OBRIGATÓRIO em produção (jamais no-op silencioso)
- `slaScheduler.ts`: removido o try/catch de fallback. Agora:
  `const cardsRules: any = opts.cardsRules || require("./cardsRules");` — **require RÍGIDO** em produção
  (`opts.cardsRules` só existe em teste, como mock explícito). Módulo ausente na build ⇒ **erro alto** no start do scheduler.
- **Prova negativa (Gate 3, check 5d):** extrair o `slaScheduler.js` do `app.asar` **sem** o irmão `cardsRules.js` ⇒ `startSlaScheduler` em modo produção **LANÇA** (`Cannot find module './cardsRules'`).
- Os per-task try/catch de cards (emit/boundary) **espelham byte-a-byte** o padrão já aprovado do `slaRules` (linhas de `slaEmissionsFor`/`nextBoundaryMs`): protegem o loop compartilhado de UMA task malformada sem crashar os demais setores — **não** transformam ausência de módulo em no-op (a ausência é capturada no require rígido). O produtor de SLA dos demais setores permanece **byte-idêntico**.

### Gate 3 — Empacotamento `app.asar` PROVADO (`copy-renderer.js` é crítico) — `CARDS RULES PACKAGING: VERIFIED`
- Novo gate `scripts/f352-cards-packaging.mjs` empacota um **`app.asar` real** (`@electron/asar`, o mesmo do electron-builder) e prova **20/20**:
  1. `cardsRules.js` existe na fonte; 2. `copy-renderer.js` o copia p/ `dist/main`; 3. está no `app.asar`;
  4. **SHA-256 idêntico** fonte==dist==asar (`a9d4cfa7…d0ee`); 5. o `slaScheduler` empacotado o **carrega em produção** e **entrega** um card em T-30 **+ ausência ⇒ LANÇA**; 6. `cardsEmissionsFor` exportado; 7. exatamente **1** `cardsRules.js` no asar (sem versão antiga/duplicada).
- Falha ⇒ `exit 1` ⇒ a build **não** gera EXE/MSI. `copy-renderer.js` está **oficialmente listado como alterado** (`HANDWRITTEN_JS = ["slaRules.js","cardsRules.js"]`).

### Gate 4 — Substituição AMARELA→VERMELHA pelo canal oficial (integrado, 3 janelas) — 37/37
- Novo `scripts/f352-cards-channel-substitution.test.mjs`: `slaScheduler.ts` **real** (produção, require de cardsRules real) roteando cada emissão pelo `deliverNotification` **real** de `main.ts` (harness `fixtures/f343/deliver-harness`).
- Por estado **ABERTA (toast)** · **MINIMIZADA (bg)** · **BANDEJA/oculta (bg)**, os 8 sub-checks: (1) T-30 amarela; (2) uma amarela; (3) T-10 vermelha; (4) vermelha pelo canal oficial; (5) vermelha substitui a amarela; (6) nunca as duas concorrentes; (7) amarela não reaparece; (8) clique abre a MESMA tarefa (bg/nativa **restaura+foca** + `notif-open detail/t1`).
- **Sem** segundo notifier/janela/listener/tray/scheduler: canais observados ⊆ `{toast, bg-window}`; cards usam o **mesmo** `emitList`; `cardsRules.js` é produtor **puro** (sem `BrowserWindow`/`Notification`/`Tray`/`webContents`/`ipcMain`/`onSnapshot`); `slaScheduler` mantém **um** `listen("tasks")`.

### Gate 5 — Destinatário correto (7 checks) — 20/20
- Novo `scripts/f352-cards-recipient.test.mjs`: (1) designer designado recebe; (2) outro designer não; (3) criador Social não (salvo se for também o responsável — contrato existente); (4) cliente **nunca**; (5) sem designer ⇒ ninguém; (6) troca de designer **invalida** o anterior (novo recebe); (7) troca de prazo mantém **só** o novo destinatário + o novo prazo (dedup recalcula). Rota pela porta pessoal `resolveNotificationTargets` (SLA pessoal — só o responsável).

### Gate 6 — Classificação individual das suítes vermelhas (base 1.0.186 empírica)
- Ver `docs/F3.5.2-GATE6-RED-SUITES-CLASSIFICATION.md`. Resumo: base **13** vermelhas · candidata **14** (delta **+1 = `f351f`**, previsto pelo Gate 1). **Nenhuma** vermelha funcional nova; **todas** as aplicáveis **verdes**; gates históricos **preservados**.

### Gate 7 — Este checkpoint + STOP.

---

## 2) Arquivos alterados vs base 1.0.186 (completo, inclui tooling de build)

**Nada fora de `desktop/` mudou** (Android/Worker/Functions/Rules intactos — verificado `git diff` vazio).

| Arquivo | Tipo | Papel |
|---|---|---|
| `desktop/src/renderer/index.html` | mod (+152) | rename "Edição de vídeos"; 5º setor `edicao_cards`; wizard cards; `saveCardsBatch` (lote atômico); dispatch no handler salvar; `renderForm` rótulo cards (não-cards byte-idêntico) |
| `desktop/src/main/cardsRules.js` | **novo** (99) | produtor ISOLADO T-30/T-10 (puro, CommonJS) |
| `desktop/src/main/slaScheduler.ts` | mod (+20) | require RÍGIDO de `cardsRules` (produção) + injeção p/ teste; cards pelo MESMO `emitList`/timer |
| `desktop/scripts/copy-renderer.js` | mod (build tooling) | `HANDWRITTEN_JS += cardsRules.js` (empacota o módulo em `dist/main`→`app.asar`) |
| `desktop/package.json` · `package-lock.json` | mod | versão 1.0.186 → **1.0.187** |
| `desktop/scripts/f352-cards-notify.test.mjs` | **novo** (53) | produtor isolado + isolamento SLA |
| `desktop/scripts/f352-cards-source-invariance.test.mjs` | **novo** (41) | invariância de fonte (Card Premium byte-idêntico, `isClientSector`, `saveTask`) |
| `desktop/scripts/f352-cards-packaging.mjs` | **novo** (Gate 3) | prova de empacotamento `app.asar` (20/20) |
| `desktop/scripts/f352-cards-channel-substitution.test.mjs` | **novo** (Gate 4) | amarela→vermelha, canal oficial, 3 janelas (37/37) |
| `desktop/scripts/f352-cards-recipient.test.mjs` | **novo** (Gate 5) | destinatário correto (20/20) |
| `desktop/scripts/f351f-rollforward-equivalence.test.mjs` | restaurado | byte-idêntico à base (Gate 1) |
| 7× `scripts/f3377a-r3/r4b, f343-main-scheduler, f343-operational-block, f345-…, f347-…` | mod (+1 cada) | injeção do mock explícito `cardsRules` (Gate 2 — sem require em teste) |
| `desktop/docs/F3.5.2-FASE1-AUDIT.md`, `…-GATE6-…md`, `…-CHECKPOINT.md` | docs | auditoria FASE 1 + entregáveis do hardening |

## 3) Invariantes verificados (checkpoint)

| Item | Estado | Evidência |
|---|---|---|
| `f351f` restaurado/intacto | ✓ | diff vs base **vazio** |
| `cardsRules.js` obrigatório em produção | ✓ | require rígido; prova negativa (asar sem módulo ⇒ throw) |
| Prova de `app.asar` | ✓ | `f352-cards-packaging` 20/20 · SHA `a9d4cfa7…d0ee` · `CARDS RULES PACKAGING: VERIFIED` |
| T-30 amarela / T-10 vermelha | ✓ | `f352-cards-notify` 53/0 + `f352-cards-channel-substitution` 37/0 |
| Substituição visual (3 janelas) | ✓ | `f352-cards-channel-substitution` 8×3 |
| Destinatário correto | ✓ | `f352-cards-recipient` 20/0 |
| Lote atômico (all-or-nothing) | ✓ | `saveCardsBatch` `db.batch()`+`commit()`; `f352-cards-source-invariance` |
| Sem tokens / Card Premium em cards | ✓ | `f352-cards-source-invariance` (sem clientReviewToken/shareToken/cronStatus/…); `isClientSector` inalterado |
| Suítes aplicáveis 100% verdes | ✓ | golden masters f343/f344/f345 + f33E + f347 + f3377a + 4× f352 (Gate 6) |
| Card Premium byte-idêntico | ✓ | `isClientSector`/`buildClientMessage`/`ensureStableClientReviewToken` byte-idênticos (source-invariance) |
| Notificações existentes preservadas | ✓ | `slaRules.js`, `main.ts`, `notifier.ts`, `tray.ts`, `bgNotify.ts` **byte-idênticos**; deliverNotification reutilizado |
| Android/Worker/Rules/Functions intactos | ✓ | `git diff` vs base **fora de desktop/** = vazio |

## 4) STOP — o que NÃO foi feito (por ordem do owner)

- **Não** criado workflow de build. **Não** gerado EXE/MSI. **Não** publicada a 1.0.187.
- Build/publicação stable+latest e a prova física ficam para a **próxima autorização** do owner.

---

## STATUS FINAL

# ✅ PRONTO PARA BUILD OFICIAL 1.0.187

Todos os 7 gates de hardening estão verdes; todas as suítes **aplicáveis** verdes; base 1.0.186 e todos
os contratos congelados **byte-idênticos**; produtor de cards **isolado** e **obrigatório** no
empacotamento; entrega **exclusivamente** pelo canal oficial. Aguardando a ordem literal do owner para
a fase de build/publicação.
