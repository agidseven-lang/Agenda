# F3.5.2 — GATE 6 · Classificação individual das suítes VERMELHAS

**Objetivo do gate (owner):** *não* declarar "todas as suítes herdadas verdes" enquanto houver
suítes vermelhas. Entregar, para **cada** suíte vermelha, uma tabela com: nome · falha · resultado
na base 1.0.186 · aplicável ou não à F3.5.2 · justificativa · ação. Toda suíte **aplicável** deve
estar **verde**. Gates históricos **não-aplicáveis** são **preservados** (não deletados, não
reancorados, não enfraquecidos). Proibido deletar teste para ficar verde ou mudar expectativa para
aceitar o código novo.

## Método (empírico e reproduzível)

- **Base 1.0.186** = worktree em `git e87ef4b59c4edc6c6fd6249fd7f4ac07b6413048` (tag v1.0.186), `npm run build`, suíte completa.
- **Candidata F3.5.2** = branch `desktop/f352-edicao-de-cards`, `npm run build`, suíte completa.
- Mesmo Node, mesmo `node_modules`, mesmo `dist/` construído nos dois lados.

| | Base 1.0.186 | Candidata F3.5.2 |
|---|---|---|
| Suítes totais | 63 | 67 (+4 novas `f352-*`) |
| PASS | 50 | 53 |
| FAIL | 13 | 14 |

**Delta de vermelhas = exatamente +1**, e é `f351f-rollforward-equivalence` — previsto pelo **Gate 1**
(prova de roll-forward 1.0.186==1.0.184; uma *feature* diverge do binário por construção). **Nenhuma**
suíte funcional pré-existente piorou de magnitude; **nenhuma** suíte aplicável ficou vermelha.

---

## Tabela individual — 14 suítes vermelhas na candidata

### A) Gates de INVARIÂNCIA DE REGIÃO / PRODUÇÃO (pinam o conjunto-de-mudança de uma FASE PASSADA)

Estes gates congelam "só este conjunto de arquivos/regiões mudou vs a baseline **daquela** fase".
Tornaram-se **vermelhos já na 1.0.186** (o código evoluiu através de f344→f347, F3.5.1, F4.2F, F3.5.1F).
São **evidência histórica**, não gates da F3.5.2. Uma feature autorizada que toca `index.html`/
`slaScheduler.ts` fica, por definição, "fora do conjunto autorizado do hotfix passado".

| Suíte | Falha (assert representativo) | Base 1.0.186 | Aplicável à F3.5.2? | Justificativa | Ação |
|---|---|---|---|---|---|
| `f343-hotfix-region-invariance` | `mudança FORA do conjunto autorizado do hotfix (F3.4.3)` | **FAIL** (idêntico) | **NÃO** | Pin do change-set do F3.4.3; já vermelho na base. F3.5.2 adiciona `edicao_cards` (aditivo) → fora da janela histórica. | **Preservar** intacto (histórico) |
| `f344-hotfix-region-invariance` | `mudança FORA do conjunto autorizado do hotfix F3.4.4` | **FAIL** (idêntico) | **NÃO** | Idem, fase F3.4.4. | **Preservar** intacto |
| `f345-hotfix-region-invariance` | `mudança FORA do conjunto autorizado do hotfix F3.4.5` | **FAIL** (idêntico) | **NÃO** | Idem, fase F3.4.5. | **Preservar** intacto |
| `f346-hotfix-region-invariance` | `mudança FORA do conjunto autorizado do hotfix F3.4.6` | **FAIL** (idêntico) | **NÃO** | Idem, fase F3.4.6. | **Preservar** intacto |
| `f347-hotfix-region-invariance` | `mudança FORA do conjunto autorizado do hotfix F3.4.7` | **FAIL** (idêntico) | **NÃO** | Idem, fase F3.4.7. | **Preservar** intacto |
| `f3374k-production-invariance` | `74K-invariance: PASS=26 FAIL=9` | **FAIL** `26/9` (idêntico) | **NÃO** | Pin de produção da fase 74K; já vermelho na base, **mesma magnitude** na candidata. | **Preservar** intacto |
| `f341a-region-invariance` | `+1: F renderForm byte-idêntica produção×candidata` | **FAIL** `PASS=39 FAIL=11` | **NÃO** | Pin do F3.4.1 (updater). Base já 11 falhas. Candidata = **12** (+1): a F3.5.2 altera **só** o rótulo do botão final do wizard (`renderForm`) para `Criar N tarefas` **no setor cards**. Para os demais setores o `_saveLbl` resolve para o literal **byte-idêntico** `(f.id?'Salvar alterações':'Salvar tarefa')` — render inalterado. É mudança aditiva-isolada, fora da janela histórica F3.4.1. | **Preservar** intacto (o +1 é a feature autorizada; **não** reancorar) |

**Prova do +1 de `f341a` (renderForm aditivo-isolado):** o único delta em `renderForm()` é
`const _saveLbl = _isCardsF ? ('Criar '+cardsQtyOf(f)+…) : (f.id?'Salvar alterações':'Salvar tarefa')`.
O ramo não-cards é o **mesmo literal** da base ⇒ saída byte-idêntica para cronograma/roteiro/
edicao_midia/programação; só `edicao_cards` recebe o rótulo de lote.

### B) Vermelhas FUNCIONAIS/AMBIENTAIS pré-existentes (idênticas na base — F3.5.2 não tocou)

Vermelhas **já na 1.0.186**, com **magnitude idêntica** na candidata. Causas: pins de **literal de
versão** (esperam `1.0.184`), pins de **CSS/estrutura** históricos, e gates **ambientais** (exigem
`.asar` empacotado / números de run específicos). Nenhuma tem relação com Edição de Cards.

| Suíte | Falha (assert) | Base 1.0.186 | Aplicável? | Justificativa | Ação |
|---|---|---|---|---|---|
| `f3373i6c18c-card-prewarm` | `A1 versão da candidata (linha 1.0.184 — F3.4.7)` | **FAIL** (idêntico) | **NÃO** | Pin de **literal de versão** que espera `1.0.184`; falha em qualquer versão ≠ 1.0.184 — já falhava na base (1.0.186) e na candidata (1.0.187). Não toca cards. | **Preservar** (pin histórico de versão) |
| `f3373i6c18h-stable-token` | `H3 [gate20] versão da candidata (linha 1.0.184)` | **FAIL** `29/1` (idêntico) | **NÃO** | Mesmo pin de versão `1.0.184`. | **Preservar** |
| `f3374d-restore-contract` | `R17 [77A] versão 1.0.184 (package.json, sem -QA)` | **FAIL** `16/1` (idêntico) | **NÃO** | Mesmo pin de versão `1.0.184` (fase 77A). | **Preservar** |
| `f3370-d3r10b-card-canonical-pwa-cutoff` | `H12b/H12c prova-de-versão + asar; K11 8 espaçadores 18px` | **FAIL** `151/154` (idêntico) | **NÃO** | Gates **ambientais** (extração de `.asar` empacotado + run numbers) + pin de **CSS** (espaçadores do config). Pré-existentes; não tocam cards. | **Preservar** |
| `f3373i6c14-settings-completion` | `E5 estrutura D9R2 (cfg-head/cfg-wrap + 8 espaçadores)` | **FAIL** `30/31` (idêntico) | **NÃO** | Pin de **estrutura/CSS** do painel de Configurações (mesma deriva D9R2). Cards não tocam Configurações. | **Preservar** |
| `f3373i6c11-auth-logout-tray-diag` | `B2 destroyTray no realQuit; D2 IPC auth-logout/self; F5 X→hide` | **FAIL** `17/20` (idêntico) | **NÃO** | Pins de **fonte de tray/auth-diag** de fase passada (i6c11). Cards não tocam tray/logout/auth. | **Preservar** |

### C) Roll-forward equivalence (previsto pelo Gate 1)

| Suíte | Falha | Base 1.0.186 | Aplicável? | Justificativa | Ação |
|---|---|---|---|---|---|
| `f351f-rollforward-equivalence` | `5 FALHA(S) — 1.0.186 NÃO é roll-forward byte-idêntico da 1.0.184` | **PASS** (`1.0.186 == 1.0.184`) | **NÃO** (a uma *feature*) | Este gate prova que o **hotfix** 1.0.186 foi um roll-forward byte-idêntico da 1.0.184. A F3.5.2 é uma **feature** que **intencionalmente** altera o renderer (setor `edicao_cards`, rename, wizard) ⇒ deixa de ser byte-idêntica à 1.0.184. **Único** verde→vermelho, **previsto e autorizado pelo Gate 1**. | **Preservar byte-idêntico** (restaurado no Gate 1); **não** rodar como gate da F3.5.2 |

---

## Suítes APLICÁVEIS — 100% VERDES na candidata

Golden masters e contratos que protegem os invariantes congelados da F3.5.2 — **todos verdes**:

| Suíte aplicável | Resultado (candidata) |
|---|---|
| `f343-sla-golden-master` (SLA valor-a-valor) | **PASS** |
| `f344-flow-golden-master` (fluxo/azul única) | **PASS** |
| `f344-repeated-move-contract` (pin byte de `saveTask`+`slaRules`) | **PASS** |
| `f345-sla-timestamp-golden-master` (clamp temporal) | **PASS** (43/0) |
| `f33E-main-notifier` (produtor único da azul) | **PASS** |
| `f343-main-scheduler` / `f343-operational-block` / `f343-delivery-channel` / `f343-preservation` | **PASS** |
| `f347-immediate-visual-delivery` / `f347-independent-behavior` | **PASS** (46/0 · 18/0) |
| `f3377a-r3-single-producer-clock` / `f3377a-r4b-http-date-clock` | **PASS** |
| `f352-cards-notify` (produtor isolado) | **PASS** (53/0) |
| `f352-cards-recipient` (destinatário) | **PASS** (20/0) |
| `f352-cards-channel-substitution` (amarela→vermelha, 3 janelas) | **PASS** (37/0) |
| `f352-cards-source-invariance` (Card Premium byte-idêntico, isClientSector, saveTask) | **PASS** (41/0) |
| `f352-cards-packaging` (empacotamento app.asar) | **PASS** — `CARDS RULES PACKAGING: VERIFIED` |

## Conclusão do Gate 6

- **F3.5.2 introduziu ZERO nova vermelha** entre as suítes funcionais pré-existentes (todas com
  **magnitude idêntica** base==candidata).
- Os gates de **invariância de região/produção** já eram **vermelhos na base 1.0.186** (histórico,
  não-aplicável). `f341a` ganhou **+1** delta de uma mudança **aditiva-isolada** em `renderForm`
  (não-cards byte-idêntico).
- `f351f` é o **único** verde→vermelho, **previsto pelo Gate 1** (feature diverge do roll-forward).
- **Todas** as suítes **aplicáveis** estão **verdes**. Nenhum teste foi deletado, reancorado,
  enfraquecido, nem teve expectativa alterada para aceitar o código novo.
