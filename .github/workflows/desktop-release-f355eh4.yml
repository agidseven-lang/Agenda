# =====================================================================
# F3.5.5E-H4 — Publicacao da Desktop 1.0.228 no CANAL DO ATUALIZADOR INTERNO (Latest)
# ---------------------------------------------------------------------
# Publica a 1.0.228 (F3.5.5E-H4 — HANDOFF COMPLETO DE NOTIFICACOES AGRUPADAS: elimina a
# UNICA excecao declarada da H3 — o card de GRUPO ativo no toast agora TAMBEM migra para a
# janela premium quando o Agenda perde o foco. Handoff TRANSACIONAL: coleta i:/g: SEM fechar
# nada -> entrega a premium pelos caminhos congelados F3.5.4O (p0 com groupKey recria o card
# data-group; updateBgGroup morfa contagem/itens/CTA) -> ACEITE pela prova de render REAL
# (bgnotify-rendered) -> COMMIT fecha a representacao interna. Sem aceite: 1 retry; 2a falha
# PRESERVA o interno (zero perda). Som 0x na migracao; zero duplicacao; dedupe/recibos/sino
# intocados; agrupamento NAO desmontado — controlador F3.5.4O, bgNotify.ts e bgnotify.html
# BYTE-CONGELADOS por gate). BASE: 1.0.227 (F3.5.5E-H3). BASELINE FISICA OFICIAL: 1.0.226
# (FISICAMENTE APROVADA). ROLLBACK IMEDIATO: v1.0.226. Adicional: v1.0.224. Fisico de
# seguranca: v1.0.223.
# USA EXCLUSIVAMENTE os BYTES do run de build F3.5.5E-H4 31208383212 (gated, verde
# nos 2 jobs; --publish never): NAO RECOMPILA — baixa os 2 artifacts e publica os
# MESMOS bytes. TUDO PINADO: run id, digests dos artifacts, 5 hashes SHA-256, tag
# v1.0.228 no head EXATO da branch.
# 6 assets: EXE, MSI, latest.yml, blockmap, SHA256SUMS, VERSAO-DESKTOP.txt.
# ESCRITAS (tag + release + Latest) SO via token temporario do GitHub App "Agenda ID Seven Release
# Bot" (expira sozinho); GITHUB_TOKEN permanece somente leitura; sem RELEASE_PAT.
# Roda NA PROPRIA branch de teste (main INTOCAVEL): dispara no push DESTE arquivo + workflow_dispatch.
# A publicacao NAO significa aprovacao fisica — o teste fisico do owner (grupo formado ->
# Alt+Tab/minimizar/X-tray -> grupo aparece POR CIMA com a contagem; sem som repetido; sem
# duplicacao) vem depois. A 1.0.226 permanece baseline oficial ate GO fisico.
# =====================================================================
name: "F3.5.5E-H4 Desktop Release — 1.0.228 no canal do updater (Handoff Completo de Notificações Agrupadas; sem build novo)"
"on":
  push:
    branches:
      - "desktop/f355eh4-grouped-notification-handoff-1.0.228"
    paths:
      - ".github/workflows/desktop-release-f355eh4.yml"
  workflow_dispatch: {}

permissions:
  contents: read
  actions: read

jobs:
  publish:
    runs-on: ubuntu-latest
    env:
      GH_TOKEN: ${{ github.token }}
      REPO: agidseven-lang/Agenda
      # ── PINAGEM COMPLETA (F3.5.5E-H4; run de build 31208383212 verde nos 2 jobs) ──
      RUN_ID: "31208383212"
      STABLE_SHA: "877795ef9a4f7e90a89b95ff3e70dcb70a78f459"
      TEST_BRANCH: "desktop/f355eh4-grouped-notification-handoff-1.0.228"
      BASE_227_SHA: "fce316f0eccba45760e0d2953fa6482ab3032401"
      BASE_226_SHA: "267f8c8593e6724ad6460ed8bbb42201ade09d0b"
      BASE_225_SHA: "24b76b51faf4d2778f025df79d6047563f089b48"
      BASE_224_SHA: "0fcad3e56d39320b8853807861417a0a5eca1e78"
      BASE_223_SHA: "217ed0622993acced1a20b72ba677e9d17ed2f03"
      BASE_222_SHA: "1eac3672ab07a56bfd5d692e9ecb24688ac2ee83"
      BASE_221_SHA: "98caad68e83230c2ada88ce1b35e709a0d62ab8c"
      BASE_220_SHA: "2d003adaeeec00499e95a9eac5fe01f48859e4ed"
      BASE_218_SHA: "1a945159dd563eba5c80657427519d0f22b4b673"
      BASE_216_SHA: "e509f43ae8f3130a9922791f89db0ba29aa7a410"
      TAG: "v1.0.228"
      TITLE: "Agenda ID Seven Desktop 1.0.228 — Handoff Completo de Notificações Agrupadas"
      ART_INST: "agenda-id-seven-desktop-1.0.228-installer"
      ART_INST_DIGEST: "sha256:83d015faf251043ee185994b125376a79b1278a9cb4d193cf264a43b41fd0d3d"
      ART_META: "agenda-id-seven-desktop-1.0.228-updatemeta"
      ART_META_DIGEST: "sha256:e0abd8f1443322df5e6f3a2137c6ebad5bd8a64ffef725a399b4e83ae2d42e27"
      EXE_NAME: "Agenda-ID-Seven-Desktop-1.0.228-x64.exe"
      MSI_NAME: "Agenda-ID-Seven-Desktop-1.0.228-x64.msi"
      BM_NAME: "Agenda-ID-Seven-Desktop-1.0.228-x64.exe.blockmap"
      LY_NAME: "latest.yml"
      EXP_EXE: "2da95928ecdca90e37bd2881897edfc20d7411388a2054c98cf37821f9d77464"
      EXP_MSI: "244a6c04af58f2568c9d072ae4d77bf81edad25323e76676dcc0db97cd577643"
      EXP_LY: "9a6c6b55d45c301d7ee7846e0fcaa37080814e5de0c9dbb1b39247541b354b17"
      EXP_BM: "c335ed1f2da4183a5ce114b133bb21f6914c86c638c466100e6a3f769bac20bc"
      EXP_SBOM: "0c94b58da6e5df0106374561e91f89553bc52a8a7385bf753fb71eaaca6c6b76"
      PREV_LATEST_TAG: "v1.0.227"
      PRES_226_TAG: "v1.0.226"
      PRES_225_TAG: "v1.0.225"
      PRES_224_TAG: "v1.0.224"
      PRES_223_TAG: "v1.0.223"
      PROD_ROLLBACK_TAG: "v1.0.218"
      PROD_ROLLBACK2_TAG: "v1.0.216"
    steps:
      - name: "Gate 0 — contexto: branch de teste correta (publicacao autorizada F3.5.5E-H4)"
        run: |
          set -euo pipefail
          [ "${{ github.ref_name }}" = "$TEST_BRANCH" ] || { echo "::error::ref '${{ github.ref_name }}' != $TEST_BRANCH — nada publicado"; exit 1; }
          echo "OK: rodando na branch de teste autorizada"

      - name: "Gate 0a — token temporario do Release Bot (GitHub App; expira sozinho)"
        id: release_bot
        uses: actions/create-github-app-token@v3
        with:
          client-id: ${{ vars.RELEASE_APP_CLIENT_ID }}
          private-key: ${{ secrets.RELEASE_APP_PRIVATE_KEY }}
          owner: ${{ github.repository_owner }}
          repositories: Agenda
          permission-contents: write
          permission-workflows: write

      - name: "Gate 0b — token do Release Bot ATIVO + escopo SO Agenda"
        env:
          BOT_TOKEN: ${{ steps.release_bot.outputs.token }}
        run: |
          set -euo pipefail
          [ -n "$BOT_TOKEN" ] || { echo "::error::token do Release Bot vazio — NADA publicado."; exit 1; }
          FN="$(GH_TOKEN="$BOT_TOKEN" gh api "repos/$REPO" --jq '.full_name' 2>/dev/null || true)"
          [ "$FN" = "$REPO" ] || { echo "::error::Release Bot sem acesso a $REPO."; exit 1; }
          J="$(GH_TOKEN="$BOT_TOKEN" gh api /installation/repositories)"
          N="$(echo "$J" | jq -r '.total_count')"; R="$(echo "$J" | jq -r '.repositories[0].full_name')"
          { [ "$N" = "1" ] && [ "$R" = "$REPO" ]; } || { echo "::error::escopo do Release Bot != {1 repo, $REPO} ($N,$R)."; exit 1; }
          echo "OK: Release Bot ativo, escopo restrito a $REPO"

      - name: "Gate 1 — run de build F3.5.5E-H4 completed/success e head EXATO"
        run: |
          set -euo pipefail
          J="$(gh api "repos/$REPO/actions/runs/$RUN_ID")"
          ST="$(echo "$J" | jq -r '.status + "|" + .conclusion')"
          HS="$(echo "$J" | jq -r '.head_sha')"
          BR="$(echo "$J" | jq -r '.head_branch')"
          echo "run $RUN_ID: $ST head=$HS branch=$BR"
          [ "$ST" = "completed|success" ] || { echo "::error::run de build invalido"; exit 1; }
          [ "$HS" = "$STABLE_SHA" ] || { echo "::error::head do run != STABLE_SHA"; exit 1; }
          [ "$BR" = "$TEST_BRANCH" ] || { echo "::error::branch do run != branch de teste"; exit 1; }

      - name: "Gate 2 — STABLE_SHA na linhagem do tip; commits alem dele SO o workflow de release"
        run: |
          set -euo pipefail
          H="$(gh api "repos/$REPO/git/ref/heads/$TEST_BRANCH" --jq '.object.sha')"
          CMP="$(gh api "repos/$REPO/compare/$STABLE_SHA...$H" --jq '.status')"
          echo "compare $STABLE_SHA...$H: $CMP"
          { [ "$CMP" = "identical" ] || [ "$CMP" = "ahead" ]; } || { echo "::error::STABLE_SHA fora da linhagem do tip ($CMP)"; exit 1; }
          gh api "repos/$REPO/compare/$STABLE_SHA...$H" --jq '.files[].filename' | tee /tmp/extra.txt || true
          if grep -vE '^\.github/workflows/desktop-release-f355eh4\.yml$' /tmp/extra.txt | grep -q .; then
            echo "::error::commits pos-candidato tocam arquivos alem do workflow de release"; exit 1; fi
          if grep -qE '^(desktop/src/|desktop/scripts/copy-renderer\.js|desktop/package\.json|desktop/package-lock\.json|desktop/tsconfig\.json|desktop/electron-builder)' /tmp/extra.txt; then
            echo "::error::insumo de build alterado apos STABLE_SHA — bytes publicados nao corresponderiam ao build"; exit 1; fi
          CMP27="$(gh api "repos/$REPO/compare/$BASE_227_SHA...$STABLE_SHA" --jq '.status')"
          { [ "$CMP27" = "ahead" ] || [ "$CMP27" = "identical" ]; } || { echo "::error::STABLE_SHA nao descende da base 1.0.227 ($CMP27)"; exit 1; }
          CMP26="$(gh api "repos/$REPO/compare/$BASE_226_SHA...$STABLE_SHA" --jq '.status')"
          { [ "$CMP26" = "ahead" ] || [ "$CMP26" = "identical" ]; } || { echo "::error::STABLE_SHA nao descende da baseline fisica 1.0.226 ($CMP26)"; exit 1; }
          CMP25="$(gh api "repos/$REPO/compare/$BASE_225_SHA...$STABLE_SHA" --jq '.status')"
          { [ "$CMP25" = "ahead" ] || [ "$CMP25" = "identical" ]; } || { echo "::error::STABLE_SHA nao descende da base 1.0.225 ($CMP25)"; exit 1; }
          CMP24="$(gh api "repos/$REPO/compare/$BASE_224_SHA...$STABLE_SHA" --jq '.status')"
          { [ "$CMP24" = "ahead" ] || [ "$CMP24" = "identical" ]; } || { echo "::error::STABLE_SHA nao descende da baseline fisica 1.0.224 ($CMP24)"; exit 1; }
          CMP23="$(gh api "repos/$REPO/compare/$BASE_223_SHA...$STABLE_SHA" --jq '.status')"
          { [ "$CMP23" = "ahead" ] || [ "$CMP23" = "identical" ]; } || { echo "::error::STABLE_SHA nao descende da 1.0.223 ($CMP23)"; exit 1; }
          CMP22="$(gh api "repos/$REPO/compare/$BASE_222_SHA...$STABLE_SHA" --jq '.status')"
          { [ "$CMP22" = "ahead" ] || [ "$CMP22" = "identical" ]; } || { echo "::error::STABLE_SHA nao descende da 1.0.222 ($CMP22)"; exit 1; }
          CMP21="$(gh api "repos/$REPO/compare/$BASE_221_SHA...$STABLE_SHA" --jq '.status')"
          { [ "$CMP21" = "ahead" ] || [ "$CMP21" = "identical" ]; } || { echo "::error::STABLE_SHA nao descende da 1.0.221 ($CMP21)"; exit 1; }
          CMP20="$(gh api "repos/$REPO/compare/$BASE_220_SHA...$STABLE_SHA" --jq '.status')"
          { [ "$CMP20" = "ahead" ] || [ "$CMP20" = "identical" ]; } || { echo "::error::STABLE_SHA nao descende da 1.0.220 ($CMP20)"; exit 1; }
          CMP0="$(gh api "repos/$REPO/compare/$BASE_218_SHA...$STABLE_SHA" --jq '.status')"
          { [ "$CMP0" = "ahead" ] || [ "$CMP0" = "identical" ]; } || { echo "::error::STABLE_SHA nao descende da baseline fisica segura 1.0.218 ($CMP0)"; exit 1; }
          CMP1="$(gh api "repos/$REPO/compare/$BASE_216_SHA...$STABLE_SHA" --jq '.status')"
          { [ "$CMP1" = "ahead" ] || [ "$CMP1" = "identical" ]; } || { echo "::error::STABLE_SHA nao descende da 1.0.216 ($CMP1)"; exit 1; }
          echo "OK: STABLE_SHA ancestral do tip; extras = so o workflow de release; bases 1.0.227/1.0.226/1.0.225/1.0.224/1.0.223/1.0.222/1.0.221/1.0.220/1.0.218/1.0.216 na linhagem"

      - name: "Gate 3 — release v1.0.228 NAO existe; tag so pode preexistir apontando p/ STABLE_SHA"
        run: |
          set -euo pipefail
          if gh release view "$TAG" -R "$REPO" >/dev/null 2>&1; then echo "::error::release $TAG ja existe"; exit 1; fi
          if gh api "repos/$REPO/git/ref/tags/$TAG" >/dev/null 2>&1; then
            TS="$(gh api "repos/$REPO/git/ref/tags/$TAG" --jq '.object.sha')"
            [ "$TS" = "$STABLE_SHA" ] || { echo "::error::tag $TAG aponta para outro sha ($TS)"; exit 1; }
          fi
          echo "OK: release/tag livres (ou tag ja em STABLE_SHA)"

      - name: "Gate 4 — artifacts: ids+digests conferem (mesmos bytes do run verde)"
        run: |
          set -euo pipefail
          json="$(gh api "repos/$REPO/actions/runs/$RUN_ID/artifacts" --paginate)"
          chk() { local d; d="$(echo "$json" | jq -r --arg n "$1" '.artifacts[]|select(.name==$n)|.digest')"; echo "$1: $d"; [ "$d" = "$2" ] || { echo "::error::digest $1 divergente"; exit 1; }; }
          chk "$ART_INST" "$ART_INST_DIGEST"
          chk "$ART_META" "$ART_META_DIGEST"

      - name: "Baixar artifacts (installer + updatemeta) — SEM recompilar"
        run: |
          set -euo pipefail
          gh run download "$RUN_ID" -R "$REPO" -n "$ART_INST" -D pacote
          gh run download "$RUN_ID" -R "$REPO" -n "$ART_META" -D pacote
          for f in "$EXE_NAME" "$MSI_NAME" "$LY_NAME" "$BM_NAME" SHA256SUMS SBOM-cyclonedx.json VERSAO-DESKTOP.txt; do
            test -f "pacote/$f" || { echo "::error::arquivo ausente: $f"; exit 1; }
          done
          if [ -f pacote/canary.yml ]; then echo "::error::canary.yml presente — canal ambiguo"; exit 1; fi

      - name: "Gate 5 — hashes SHA-256 + latest.yml (versao/EXE/sha512) + VERSAO-DESKTOP"
        run: |
          set -euo pipefail
          declare -A EXP=( ["$EXE_NAME"]="$EXP_EXE" ["$MSI_NAME"]="$EXP_MSI" ["$LY_NAME"]="$EXP_LY" ["$BM_NAME"]="$EXP_BM" ["SBOM-cyclonedx.json"]="$EXP_SBOM" )
          for f in "${!EXP[@]}"; do
            H="$(sha256sum "pacote/$f" | awk '{print $1}')"; echo "$f=$H"
            [ "$H" = "${EXP[$f]}" ] || { echo "::error::hash $f divergente"; exit 1; }
          done
          ( cd pacote && sha256sum -c SHA256SUMS ) || { echo "::error::SHA256SUMS nao confere"; exit 1; }
          grep -q "version: 1.0.228" "pacote/$LY_NAME" || { echo "::error::latest.yml version != 1.0.228"; exit 1; }
          grep -qE "^path: $EXE_NAME" "pacote/$LY_NAME" || { echo "::error::latest.yml path != EXE"; exit 1; }
          META_SHA512=$(grep -E '^sha512:' "pacote/$LY_NAME" | head -1 | sed -E 's/^sha512:[[:space:]]*//' | tr -d '\r')
          CALC_SHA512=$(openssl dgst -sha512 -binary "pacote/$EXE_NAME" | openssl base64 -A)
          [ "$META_SHA512" = "$CALC_SHA512" ] || { echo "::error::sha512 do latest.yml != EXE (updater rejeitaria)"; exit 1; }
          grep -q "versionName : 1.0.228" pacote/VERSAO-DESKTOP.txt || { echo "::error::VERSAO-DESKTOP sem 1.0.228"; exit 1; }
          grep -q "1.0.228-f355eh4-grouped-notification-handoff" pacote/VERSAO-DESKTOP.txt || { echo "::error::rotulo da candidata ausente"; exit 1; }
          echo "OK: 5 hashes + SHA256SUMS + latest.yml (1.0.228/EXE/sha512==EXE) + VERSAO-DESKTOP"

      - name: "Release notes"
        run: |
          cat > notas.md <<'NOTAS'
          Agenda ID Seven Desktop 1.0.228 — Handoff Completo de Notificações Agrupadas

          Atualização disponibilizada pelo ATUALIZADOR INTERNO do app (Configurações ->
          Atualizações -> Verificar atualizações -> Baixar -> Instalar e reiniciar).
          NENHUM download manual é necessário.

          CONTEXTO: elimina a ÚNICA exceção declarada da 1.0.227, por determinação do
          owner: o card de GRUPO ("N atualizações") ativo no toast interno NÃO migrava
          quando você trocava do Agenda para outro programa — qualquer notificação
          comum agrupável podia ficar escondida atrás da janela por até 15 segundos.
          O sino NÃO substitui notificação imediata. Agora NENHUM tipo de notificação
          interna ativa permanece escondida quando o Agenda perde o foco.

          O QUE MUDA vs 1.0.227 (F3.5.5E-H4) — SOMENTE o handoff do blur:
          - GRUPOS migram: individual, agrupada (2, 3, 4+ eventos), rajada, já
            apresentada ou chegando durante a troca de foco — TUDO migra para a
            janela premium por cima do outro programa, com a CONTAGEM, os itens,
            o cliente e o botão "Abrir ->" preservados;
          - handoff TRANSACIONAL: o card interno SÓ é removido depois que a janela
            premium PROVA que renderizou o card (aceite por prova de render real);
            se a entrega externa falhar, tenta de novo e, falhando, PRESERVA o
            card interno — nenhum evento é perdido, nunca;
          - som NUNCA repete na migração; o mesmo evento NUNCA fica visível nas
            duas superfícies ao mesmo tempo (mover, nunca duplicar);
          - trocar de janela e voltar rapidamente tem desfecho determinístico
            (ou nada migra, ou a migração completa limpa — sem estados híbridos).

          O QUE NÃO MUDA (CONGELADO por gate):
          - o VISUAL 1.0.226 aprovado (avatar flutuante 62px, cápsula com segmento
            "Abrir ->", radius 24, navy premium) — bgnotify.html byte-idêntico;
          - a PRÓPRIA janela premium (bgNotify.ts) e o CONTROLADOR do agrupamento
            (F3.5.4O) — byte-idênticos: o handoff usa somente os caminhos que o
            sistema já suportava; o agrupamento não foi desmontado;
          - reassert de topmost/posição, roteamento por FOCO real, anti-roubo de
            foco, som (1x), deduplicação, fila/caps, recibos, sino/histórico,
            deep-link, SLA/lembretes centrais, Acompanhamento, sessão, tray,
            autostart, updater, Cronograma/editor/colagem, portal, auth;
          - sessão BLOQUEADA continua indo pela Notification nativa do Windows.

          IMPORTANTE: esta versão DEPENDE DE VALIDAÇÃO FÍSICA do owner no Windows.
          Teste decisivo (mandato): A) com o Agenda focado, receber 2+ eventos da
          MESMA tarefa (forma o grupo "2 atualizações") e dar Alt+Tab -> o grupo
          aparece IMEDIATAMENTE por cima do outro programa com a contagem; B) o
          mesmo com 3 e 4 eventos e com rajada; C) minimizar logo após formar o
          grupo; D) X->tray com grupo ativo; E) som NÃO repete; nada duplica; o
          clique em "Abrir ->" abre a tarefa. A publicação não é aprovação física.
          Em caso de problema: ROLLBACK IMEDIATO v1.0.226 (baseline física);
          adicional v1.0.224; físico de segurança v1.0.223; extra v1.0.218;
          todas as versões anteriores permanecem publicadas.

          Procedência: run de build F3.5.5E-H4 31208383212 (success; --publish
          never; verify + Windows verdes: suíte da fase 53 — espelho de grupos +
          transação coleta->entrega->ACEITE->commit + retry/preservação + som 0x
          + commit no renderer + controlador F3.5.4O intocado — rodada TAMBÉM
          contra o app.asar; 21 provas Electron REAIS com o pipeline REAL do dist
          cobrindo os 18 cenários do mandato (grupo 2/3/4, rajada, update durante
          o handoff, blur/focus rápidos, Alt+Tab repetido, minimizado, X->tray,
          retry real com recuperação, preservação na falha dupla, zero som, zero
          duplicação, CTA); suíte da visibilidade H3 herdada 57 + referência 62 +
          bateria herdada completa + provas herdadas H3 22 + H2 23 + E 27 + D 27
          + C-H2 9 + C-H1 10+ciclos + smoke RTE 91 + B 20 + A-H1 15 + A 25;
          isolamento: delta SÓ na camada do handoff autorizada vs 1.0.227 +
          bgNotify.ts/controlador F3.5.4O/visual da referência DIFF-VAZIO;
          limitações de CI declaradas — z-order do DWM real, minimize sem window
          manager e Notification nativa ficam para a validação física)
          @ commit 877795ef9a4f7e90a89b95ff3e70dcb70a78f459
          (branch desktop/f355eh4-grouped-notification-handoff-1.0.228).
          Mesmos bytes; sem recompilar. Confira SHA-256 em SHA256SUMS.
          NOTAS

      - name: "Criar tag v1.0.228 pinada em STABLE_SHA — ESCRITA via Release Bot"
        env:
          GH_TOKEN: ${{ steps.release_bot.outputs.token }}
        run: |
          set -euo pipefail
          if gh api "repos/$REPO/git/ref/tags/$TAG" >/dev/null 2>&1; then
            TS="$(gh api "repos/$REPO/git/ref/tags/$TAG" --jq '.object.sha')"
            [ "$TS" = "$STABLE_SHA" ] || { echo "::error::tag $TAG aponta para outro sha ($TS)"; exit 1; }
          else
            gh api -X POST "repos/$REPO/git/refs" -f ref="refs/tags/$TAG" -f sha="$STABLE_SHA" >/dev/null
            echo "tag $TAG criada em $STABLE_SHA"
          fi

      - name: "Publicar release no canal do updater (latest=true, prerelease=false) — 6 assets — via Release Bot"
        env:
          GH_TOKEN: ${{ steps.release_bot.outputs.token }}
        run: |
          set -euo pipefail
          gh release create "$TAG" -R "$REPO" \
            --target "$STABLE_SHA" --latest=true \
            --title "$TITLE" --notes-file notas.md \
            "pacote/$EXE_NAME" "pacote/$MSI_NAME" "pacote/$LY_NAME" "pacote/$BM_NAME" "pacote/SHA256SUMS" "pacote/VERSAO-DESKTOP.txt"
          echo "publicado: $TAG (canal do electron-updater: release Latest com latest.yml)"

      - name: "Pos-verificacao (Latest=1.0.228; 1.0.227/1.0.226/1.0.225/1.0.224/1.0.223 preservadas; rollbacks 1.0.218/1.0.216 preservados)"
        run: |
          set -euo pipefail
          FLAGS="$(gh release view "$TAG" -R "$REPO" --json isPrerelease,isDraft --jq '(.isPrerelease|tostring)+"|"+(.isDraft|tostring)')"
          [ "$FLAGS" = "false|false" ] || { echo "::error::flags incorretas: $FLAGS"; exit 1; }
          N="$(gh release view "$TAG" -R "$REPO" --json assets --jq '.assets|length')"
          [ "$N" = "6" ] || { echo "::error::esperado 6 assets, encontrados $N"; exit 1; }
          for a in "$EXE_NAME" "$MSI_NAME" "$LY_NAME" "$BM_NAME" SHA256SUMS VERSAO-DESKTOP.txt; do
            gh release view "$TAG" -R "$REPO" --json assets --jq '.assets[].name' | grep -qx "$a" || { echo "::error::asset $a ausente"; exit 1; }
          done
          if gh release view "$TAG" -R "$REPO" --json assets --jq '.assets[].name' | grep -qx "app-update.yml"; then echo "::error::app-update.yml nao pode ser asset"; exit 1; fi
          TAGSHA="$(gh api "repos/$REPO/git/ref/tags/$TAG" --jq '.object.sha')"
          [ "$TAGSHA" = "$STABLE_SHA" ] || { echo "::error::tag != STABLE_SHA"; exit 1; }
          LATEST_TAG="$(gh api "repos/$REPO/releases/latest" --jq '.tag_name')"
          [ "$LATEST_TAG" = "$TAG" ] || { echo "::error::Latest do repo != $TAG (got $LATEST_TAG)"; exit 1; }
          for t in "$PREV_LATEST_TAG" "$PRES_226_TAG" "$PRES_225_TAG" "$PRES_224_TAG" "$PRES_223_TAG" "$PROD_ROLLBACK_TAG" "$PROD_ROLLBACK2_TAG"; do
            gh release view "$t" -R "$REPO" --json tagName --jq '.tagName' | grep -qx "$t" || { echo "::error::$t (preservada/rollback) sumiu"; exit 1; }
          done
          echo "===== GO: 1.0.228 no canal do updater (Latest); ROLLBACK IMEDIATO 1.0.226 (baseline fisica); adicional 1.0.224; fisico de seguranca 1.0.223; 1.0.227/1.0.225 preservadas; extras 1.0.218/1.0.216 ====="
