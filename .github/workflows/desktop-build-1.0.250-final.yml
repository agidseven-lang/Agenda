# =====================================================================
# [manual] FINAL 1.0.250 — Desktop Build Canonico + Installed-App QA (NADA e publicado)
# ---------------------------------------------------------------------
# Espelha o mecanismo canonico auditado (desktop-build-1.0.249-final.yml):
# build UNICO da 1.0.250 a partir do FINAL SOURCE ff762ac7 (I7.10 Task Card
# aprovado + bump), com hard gates de proveniencia, GATE ASAR (versao/renderer/
# flags do updater), contrato do latest.yml (== NSIS do MESMO run), SHA256SUMS +
# VERSAO-DESKTOP.txt + manifest, e — NO MESMO RUN — a INSTALLED-APP QA REAL:
#   SESSAO A — 1.0.249 PUBLICA -> 1.0.250 (assets deste build): update SILENCIOSO
#     /S, REQUIRED_USER_INSTALL_ACTIONS=0, app relanca como 1.0.250.
#   SESSAO B — 1.0.250 instalada: versao==1.0.250, Light UI ON, smoke funcional
#     real + REGRESSAO DO TASK CARD APROVADO (I7.10: status topo .kbv2-due,
#     metadata grid Setor/Prazo, prazo curto .kbv2-date-s, inset de progresso +
#     %, footer coeso, Mover no topo em hover, surface Cliente com card cron) +
#     screenshots 1920/1536(Win125)/1366 + crops card/hover/cliente (evidencia).
# Diff de produto vs v1.0.249 (28ed90ef) = renderer + package.json (bump) +
# QA tooling (visual-qa-i710-task-card.mjs) — NADA de main/updater/builder.
# Sem tag, sem release, sem publish, sem Latest, sem RC_RELEASE_GO. Manual apenas.
# =====================================================================
name: "[manual] FINAL 1.0.250 — Desktop Build + Installed-App QA"

"on":
  workflow_dispatch: {}

permissions:
  contents: read

env:
  PRODUCT_SOURCE_SHA: "ff762ac7211cc9623a8e18992c0f3fefd0a8c80e"
  V249_TAG_SHA: "28ed90ef3f8a3573550e2c09a46b19fc98fe11fc"   # v1.0.249 (producao; base do candidato)
  EXPECTED_VERSION: "1.0.250"
  EXPECTED_RENDERER_SHA256: "51cc17d8daaf92d0d395ef1a4919d207cec5e06afa65af97845fcb9c5ab9e7b0"
  EXPECTED_LOCKFILE_SHA256: "047711f1968a7c9f32a7a854bcb437e5f10d168613b47eb7b030107c46c40ced"
  BASELINE_TAG: "v1.0.249"
  FEED_PORT: "8879"

jobs:
  final-build:
    runs-on: windows-latest
    timeout-minutes: 90
    defaults:
      run:
        shell: bash
    steps:
      - name: "Git sem conversao CRLF"
        run: git config --global core.autocrlf false

      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: "PIN do FINAL SOURCE (ff762ac7) + PROVENANCE hard gates"
        run: |
          set -euo pipefail
          git checkout --detach "$PRODUCT_SOURCE_SHA"
          [ "$(git rev-parse HEAD)" = "$PRODUCT_SOURCE_SHA" ] || { echo "::error::pin do FINAL SOURCE falhou"; exit 1; }
          R=$(git show "$PRODUCT_SOURCE_SHA":desktop/src/renderer/index.html | sha256sum | awk '{print $1}')
          [ "$R" = "$EXPECTED_RENDERER_SHA256" ] || { echo "::error::renderer difere do congelado ($R)"; exit 1; }
          L=$(git show "$PRODUCT_SOURCE_SHA":desktop/package-lock.json | sha256sum | awk '{print $1}')
          [ "$L" = "$EXPECTED_LOCKFILE_SHA256" ] || { echo "::error::lockfile difere do pinado ($L)"; exit 1; }
          node -e "const p=require('./desktop/package.json'); if(p.version!=='$EXPECTED_VERSION'){console.error('version '+p.version);process.exit(1);} console.log('version OK '+p.version);"
          # DIFF DE PRODUTO vs v1.0.249 (28ed90ef) = renderer + bump + QA tooling. NADA de
          # backend/updater/electron-builder/main. Prova a tese do candidato (Task Card only).
          D=$(git diff --name-only "$V249_TAG_SHA" "$PRODUCT_SOURCE_SHA" -- desktop | sort | tr '\n' ' ')
          echo "diff desktop vs v1.0.249: $D"
          [ "$D" = "desktop/package.json desktop/scripts/visual-qa-i710-task-card.mjs desktop/src/renderer/index.html " ] || { echo "::error::diff de produto inesperado vs v1.0.249"; exit 1; }
          U=$(git diff --name-only "$V249_TAG_SHA" "$PRODUCT_SOURCE_SHA" -- desktop/src/main desktop/electron-builder.yml | tr '\n' ' ')
          [ -z "$U" ] || { echo "::error::main/updater/electron-builder alterados vs v1.0.249: $U"; exit 1; }
          echo "PROVENANCE OK: source=$PRODUCT_SOURCE_SHA renderer=$R lockfile=$L version=$EXPECTED_VERSION"

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: "BUILD FINAL UNICO — npm ci + build + NSIS x64 + MSI x64 + GATE ASAR + IMMUTABILITY"
        working-directory: desktop
        run: |
          set -euo pipefail
          npm ci --no-audit --no-fund
          npm run build
          npx electron-builder --win nsis msi --publish never
          EX="$RUNNER_TEMP/asarx"; rm -rf "$EX"; mkdir -p "$EX"
          npx --yes @electron/asar extract dist-installer/win-unpacked/resources/app.asar "$EX"
          PKG="$EX/package.json" node -e "const p=JSON.parse(require('fs').readFileSync(process.env.PKG,'utf8')); if(p.version!=='$EXPECTED_VERSION')process.exit(1); console.log('asar version OK '+p.version);"
          git show "$PRODUCT_SOURCE_SHA":desktop/src/renderer/index.html > "$RUNNER_TEMP/src-index.html"
          A="$EX/src/renderer/index.html" B="$RUNNER_TEMP/src-index.html" node -e '
            const fs=require("fs");const n=s=>String(s).replace(/\r\n/g,"\n");
            if(n(fs.readFileSync(process.env.A,"utf8"))!==n(fs.readFileSync(process.env.B,"utf8"))){console.error("renderer empacotado != fonte");process.exit(1);}
            console.log("renderer empacotado == fonte (byte-identico)");'
          US="$EX/dist/main/updaterService.js"
          grep -Eq 'autoUpdater\.allowPrerelease[[:space:]]*=[[:space:]]*false' "$US" || { echo "::error::allowPrerelease!=false no asar"; exit 1; }
          grep -Eq 'autoUpdater\.channel[[:space:]]*=[[:space:]]*"latest"' "$US" || { echo "::error::channel!=latest no asar"; exit 1; }
          grep -Eq 'autoUpdater\.allowDowngrade[[:space:]]*=[[:space:]]*false' "$US" || { echo "::error::allowDowngrade!=false no asar"; exit 1; }
          grep -Fq 'quitAndInstall(true, true)' "$US" || { echo "::error::fix do updater ausente no asar"; exit 1; }
          if grep -Fq 'quitAndInstall(false' "$US"; then echo "::error::chamada assistida no asar"; exit 1; fi
          ST=$(git status --porcelain -- desktop/src desktop/package.json desktop/package-lock.json desktop/electron-builder.yml)
          [ -z "$ST" ] || { echo "::error::build alterou source: $ST"; exit 1; }
          echo "GATE ASAR + IMMUTABILITY OK"

      - name: "ASSETS + SHA256SUMS + VERSAO-DESKTOP.txt + MANIFEST + CONTRATO latest.yml (mesmo build)"
        working-directory: desktop/dist-installer
        run: |
          set -euo pipefail
          EXE="Agenda-ID-Seven-Desktop-$EXPECTED_VERSION-x64.exe"
          MSI="Agenda-ID-Seven-Desktop-$EXPECTED_VERSION-x64.msi"
          BMAP="$EXE.blockmap"
          for f in "$EXE" "$MSI" "$BMAP" latest.yml; do [ -f "$f" ] || { echo "::error::asset ausente: $f"; exit 1; }; done
          sha256sum "$EXE" "$MSI" "$BMAP" latest.yml > SHA256SUMS
          echo "$EXPECTED_VERSION" > VERSAO-DESKTOP.txt
          grep -q "^version: $EXPECTED_VERSION$" latest.yml || { echo "::error::latest.yml version"; exit 1; }
          grep -q "url: $EXE" latest.yml || { echo "::error::latest.yml nao referencia o exe deste build"; exit 1; }
          SHA512=$(openssl dgst -sha512 -binary "$EXE" | base64 -w0)
          grep -q "$SHA512" latest.yml || { echo "::error::sha512 do exe nao bate com latest.yml"; exit 1; }
          SIZE=$(stat -c%s "$EXE")
          grep -q "size: $SIZE" latest.yml || { echo "::error::size do exe nao bate com latest.yml"; exit 1; }
          echo "CONTRATO latest.yml OK (version/url/sha512/size == NSIS deste run)"
          {
            echo "== FINAL 1.0.250 ARTIFACT MANIFEST =="
            echo "PRODUCT_SOURCE_SHA: $PRODUCT_SOURCE_SHA"
            echo "VERSION: $EXPECTED_VERSION"
            for f in "$EXE" "$MSI" "$BMAP" latest.yml SHA256SUMS VERSAO-DESKTOP.txt; do
              H=$(sha256sum "$f" | awk '{print $1}'); B=$(stat -c%s "$f")
              echo "asset: $f | bytes: $B | sha256: $H | arch: x64 | signing: unsigned-by-design | source: $PRODUCT_SOURCE_SHA"
            done
          } | tee "$RUNNER_TEMP/FINAL-250-MANIFEST.txt"
          cat "$RUNNER_TEMP/FINAL-250-MANIFEST.txt" >> "$GITHUB_STEP_SUMMARY"

      - name: "BASELINE PUBLICA v1.0.249 — download + verificacao SHA256SUMS"
        run: |
          set -euo pipefail
          mkdir -p "$RUNNER_TEMP/baseline"; cd "$RUNNER_TEMP/baseline"
          BASE="https://github.com/agidseven-lang/Agenda/releases/download/$BASELINE_TAG"
          curl -sSL -o baseline.exe "$BASE/Agenda-ID-Seven-Desktop-1.0.249-x64.exe"
          curl -sSL -o SHA256SUMS "$BASE/SHA256SUMS"
          EXP=$(grep "Agenda-ID-Seven-Desktop-1.0.249-x64.exe$" SHA256SUMS | awk '{print $1}')
          GOT=$(sha256sum baseline.exe | awk '{print $1}')
          [ "$EXP" = "$GOT" ] || { echo "::error::baseline 1.0.249 nao confere"; exit 1; }
          echo "BASELINE 1.0.249 OK ($GOT)"

      - name: "Instalar baseline 1.0.249 (/S) — setup de QA"
        shell: powershell
        run: |
          $p = Start-Process -FilePath "$env:RUNNER_TEMP\baseline\baseline.exe" -ArgumentList "/S" -Wait -PassThru
          if ($p.ExitCode -ne 0) { Write-Error "baseline install exit $($p.ExitCode)"; exit 1 }
          Start-Sleep -Seconds 3
          $dir = Get-ChildItem "$env:LOCALAPPDATA\Programs" -Directory | Where-Object { Test-Path (Join-Path $_.FullName "Agenda ID Seven Desktop.exe") } | Select-Object -First 1
          if (-not $dir) { Write-Error "baseline nao instalada"; exit 1 }
          "INSTALL_DIR=$($dir.FullName)" | Out-File -FilePath $env:GITHUB_ENV -Append -Encoding utf8
          Write-Host "baseline 1.0.249 instalada em: $($dir.FullName)"

      - name: "FEED local com os ASSETS 1.0.250 (127.0.0.1:8879; producao intocada)"
        shell: powershell
        run: |
          $feed = "$env:RUNNER_TEMP\feed"
          New-Item -ItemType Directory -Force -Path $feed | Out-Null
          Copy-Item "desktop\dist-installer\latest.yml" $feed
          Copy-Item "desktop\dist-installer\Agenda-ID-Seven-Desktop-1.0.250-x64.exe" $feed
          Copy-Item "desktop\dist-installer\Agenda-ID-Seven-Desktop-1.0.250-x64.exe.blockmap" $feed
          Start-Process python -ArgumentList "-m","http.server",$env:FEED_PORT,"--bind","127.0.0.1","--directory",$feed -WindowStyle Hidden
          $ok = $false
          foreach ($i in 1..15) { Start-Sleep -Seconds 2; try { $r = Invoke-WebRequest -UseBasicParsing "http://127.0.0.1:$env:FEED_PORT/latest.yml" -TimeoutSec 5; if ($r.StatusCode -eq 200) { $ok = $true; break } } catch { } }
          if (-not $ok) { Write-Error "feed local nao respondeu em 30s"; exit 1 }
          $au = Join-Path $env:INSTALL_DIR "resources\app-update.yml"
          Copy-Item $au "$au.bak"
          @"
          provider: generic
          url: http://127.0.0.1:$env:FEED_PORT
          channel: latest
          updaterCacheDirName: agenda-id-seven-desktop-updater
          "@ | Out-File -FilePath $au -Encoding utf8
          Write-Host "feed 1.0.250 OK; instancia baseline 1.0.249 apontada ao feed local"

      - name: "SESSAO A — UPDATE 1.0.249 -> 1.0.250 (silencioso /S; REQUIRED_USER_INSTALL_ACTIONS=0)"
        timeout-minutes: 15
        working-directory: desktop
        env:
          PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD: "1"
        run: |
          set -euo pipefail
          npm i --no-save playwright@1.47.2 --no-audit --no-fund
          cat > au-a.mjs <<'EOF'
          import { _electron } from 'playwright';
          import fs from 'fs';
          const exe = process.env.INSTALL_DIR + '\\Agenda ID Seven Desktop.exe';
          const out = { g: {} }; const g = out.g;
          const sleep = ms => new Promise(r => setTimeout(r, ms));
          const app = await _electron.launch({ executablePath: exe, args: [] });
          const pg = await app.firstWindow({ timeout: 45000 });
          await pg.waitForLoadState('load'); await pg.waitForTimeout(4000);
          const before = await pg.evaluate(() => ({ ver: (typeof APP_VER !== 'undefined' && APP_VER.desktop) || null, upd: !!(window.desktopAPI && window.desktopAPI.updater) }));
          out.before = before;
          g.a1_baselineIs249 = before.ver === '1.0.249';
          g.a2_updaterApi = before.upd === true;
          const st = () => pg.evaluate(() => window.desktopAPI.updater.getState());
          const poll = async (want, ms) => { const t0 = Date.now(); for (;;) { const s = await st(); if (want.includes(s.status)) return s; if (Date.now() - t0 > ms) return s; await sleep(500); } };
          await pg.evaluate(() => { window.desktopAPI.updater.check(); });
          let s = await poll(['update_available'], 30000);
          out.avail = { status: s.status, version: s.availableVersion };
          g.b1_250Available = s.status === 'update_available' && s.availableVersion === '1.0.250';
          await pg.evaluate(() => { window.desktopAPI.updater.download(); });
          { const t0 = Date.now(); for (;;) { const x = await st(); if (x.status === 'downloaded' || x.status === 'error') { s = x; break; } if (Date.now() - t0 > 120000) { s = x; break; } await sleep(400); } }
          out.dl = s.status;
          g.b2_downloadedSha512Valid = s.status === 'downloaded';
          pg.evaluate(() => { window.desktopAPI.updater.installAndRestart(); }).catch(() => {});
          let closed = false;
          try { await Promise.race([new Promise(res => app.on('close', () => { closed = true; res(); })), sleep(30000)]); } catch (_) {}
          if (!closed) { try { const x = await st(); closed = (x.status === 'installing'); } catch (_) { closed = true; } }
          out.appClosedByUpdater = closed;
          g.c1_installTriggered = closed === true;
          const fails = Object.entries(g).filter(([k, v]) => !v).map(([k]) => k);
          out.fails = fails;
          fs.writeFileSync(process.env.RUNNER_TEMP + '/au-sessionA.json', JSON.stringify(out));
          console.log(JSON.stringify(out, null, 1));
          try { await Promise.race([app.close(), sleep(4000)]); } catch (_) {}
          if (fails.length) { console.error('FAILS A:', fails); }
          process.exit(fails.length ? 1 : 0);
          EOF
          node au-a.mjs

      - name: "Confirmar upgrade instalado == 1.0.250 (asar) + sem janela de instalador residual"
        shell: powershell
        run: |
          $target = (Get-FileHash "desktop\dist-installer\win-unpacked\resources\app.asar" -Algorithm SHA256).Hash
          $deadline = (Get-Date).AddSeconds(180); $done = $false
          while ((Get-Date) -lt $deadline) {
            Start-Sleep -Seconds 3
            $asar = Join-Path $env:INSTALL_DIR "resources\app.asar"
            if (Test-Path $asar) { try { if ((Get-FileHash $asar -Algorithm SHA256).Hash -eq $target) { $done = $true; break } } catch { } }
          }
          if (-not $done) { Write-Error "FAIL: asar instalado != 1.0.250 (upgrade silencioso nao concluiu)"; exit 1 }
          Start-Sleep -Seconds 3
          $setup = @(Get-Process -ErrorAction SilentlyContinue | Where-Object { $_.MainWindowTitle -like "*Setup*" -or $_.MainWindowTitle -like "*Instal*" }).Count
          if ($setup -ne 0) { Write-Error "FAIL: janela de instalador residual"; exit 1 }
          Get-Process | Where-Object { $_.ProcessName -like "*Agenda*" } | Stop-Process -Force -ErrorAction SilentlyContinue
          Start-Sleep -Seconds 2
          Write-Host "UPGRADE 249->250 SILENCIOSO OK (asar==1.0.250; sem setup residual)"

      - name: "SESSAO B — 1.0.250 instalada: versao + Light UI + smoke + REGRESSAO TASK CARD + evidencias"
        timeout-minutes: 20
        working-directory: desktop
        env:
          PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD: "1"
        run: |
          set -euo pipefail
          cat > au-b.mjs <<'EOF'
          import { _electron } from 'playwright';
          import fs from 'fs';
          const exe = process.env.INSTALL_DIR + '\\Agenda ID Seven Desktop.exe';
          const out = { g: {} }; const g = out.g;
          const sleep = ms => new Promise(r => setTimeout(r, ms));
          const app = await _electron.launch({ executablePath: exe, args: [] });
          const pg = await app.firstWindow({ timeout: 45000 });
          const perr = []; pg.on('pageerror', e => perr.push(String(e).slice(0, 160)));
          await pg.waitForLoadState('load'); await pg.waitForTimeout(4000);
          const a = await pg.evaluate(() => ({
            ver: (typeof APP_VER !== 'undefined' && APP_VER.desktop) || null,
            title: document.title,
            white: (document.body.innerText || '').trim().length < 2,
            loginPwd: !!document.querySelector('input[type="password"]')
          }));
          out.after = a;
          g.d1_versionIs250 = a.ver === '1.0.250' && a.title.includes('1.0.250');
          g.d2_noWhiteScreen = a.white === false;
          g.d0_loginSurface = a.loginPwd === true;
          // viewport desktop ANTES do seed (isDesktop() depende de largura >=1024 no app empacotado)
          try { await app.evaluate(({ BrowserWindow }) => { const w = BrowserWindow.getAllWindows()[0]; if (w) w.setContentSize(1920, 1080); }); } catch (_) {}
          await pg.waitForTimeout(800);
          const D = n => { const d = new Date(Date.now() + n * 86400000); return d.toISOString().slice(0, 10); };
          const smoke = await pg.evaluate(async (D3) => {
            const r = {};
            try {
              state.user = { id: 'carlos', name: 'Carlos Eduardo', role: 'Social Media', admin: true };
              state.users = [state.user, { id: 'marina', name: 'Marina Klein', role: 'Designer' }];
              document.body.classList.add('desktop', 'authed', 'light-ui');
              const asp = document.getElementById('authSplash'); if (asp) asp.style.display = 'none';
              const lg = document.getElementById('login'); if (lg) lg.classList.add('hidden');
              const ap = document.getElementById('app'); if (ap) ap.style.display = 'flex';
              state.tasks = [
                { id: 'k1', title: 'Post de Agenda Aberta - histeroscopias', client: 'Dra. Helita Freitas', sector: 'edicao_cards', by: 'carlos', assigneeId: 'carlos', status: 'afazer', dueDate: '2026-08-20', dueTime: '19:05', cardLegenda: 'Agenda aberta', cardObs: 'Arte aprovada' },
                { id: 'k2', title: 'Edicao de videos - Agosto', client: 'Hospital Visao', sector: 'edicao_midia', by: 'carlos', assigneeId: 'carlos', status: 'andamento', dueDate: D3.d1, dueTime: '17:00', priority: true, videos: [{ tema: 'v1' }, { tema: 'v2' }, { tema: 'v3' }], checklist: [{ t: '1' }, { t: '2' }, { t: '3' }, { t: '4' }, { t: '5' }] },
                { id: 'k3', title: 'Reels de lancamento', client: 'Sunset Wear', sector: 'edicao_cards', by: 'marina', assigneeId: 'carlos', status: 'revisao', dueDate: D3.d0, dueTime: '23:00', cardLegenda: 'Colecao' },
                { id: 'k4', title: 'Cronograma Setembro', client: 'Sunset Wear', sector: 'edicao_cards', by: 'carlos', assigneeId: 'carlos', status: 'concluido', dueDate: '2026-08-22' }
              ];
              state.tab = 'tarefas'; state.personBoard = 'carlos'; render(); if (typeof afterBoard === 'function') afterBoard();
            } catch (e) { r.seedErr = String(e); }
            const q = s => document.querySelector(s);
            r.isDesktop = (typeof isDesktop === 'function') ? isDesktop() : null;
            r.innerW = window.innerWidth;
            r.lightUi = document.body.classList.contains('light-ui');
            r.sidebar = !!q('.nav .lui-brand2') && document.querySelectorAll('.nav .sb-item').length >= 5;
            r.hasMore = !!q('.nav .sb-more>summary');
            r.newTask = !!q('.new-task-btn');
            r.lanes = document.querySelectorAll('.kbv2-column').length;
            r.cards = document.querySelectorAll('.kbv2-card').length;
            // ---- REGRESSAO TASK CARD I7.10 (candidato aprovado 86c5abce) ----
            const card2 = [...document.querySelectorAll('.kbv2-card')].find(c => c.querySelector('[data-cardmenu="k2"], [data-detail="k2"]'));
            const cs = card2 ? getComputedStyle(card2) : null;
            r.tcGrid = cs ? cs.display === 'grid' : false;
            r.tcDueVisible = card2 ? (card2.querySelector('.kbv2-due') && getComputedStyle(card2.querySelector('.kbv2-due')).display !== 'none') : false;
            r.tcMetaLabel = card2 ? getComputedStyle(card2.querySelector('.kbv2-card-chips'), '::before').content.includes('Setor') : false;
            r.tcShortDate = card2 ? (() => { const b = card2.querySelector('.kbv2-date-s'); return !!b && getComputedStyle(b).display !== 'none' && /ago|set|out/.test(b.textContent); })() : false;
            r.tcInset = card2 ? (() => { const rl = card2.querySelector('.kbv2-card-rail'); return !!rl && getComputedStyle(rl).backgroundColor === 'rgb(245, 248, 251)'; })() : false;
            r.tcPct = card2 ? (() => { const p = card2.querySelector('.kbv2-pct'); return !!p && getComputedStyle(p).display !== 'none' && /%$/.test(p.textContent.trim()); })() : false;
            r.tcPriorityQuiet = card2 ? (() => { const pr = [...card2.querySelectorAll('.kbv2-chip')].find(x => /Prioridade/i.test(x.textContent)); if (!pr) return false; const pcs = getComputedStyle(pr); return pcs.backgroundColor === 'rgba(0, 0, 0, 0)' && parseFloat(pcs.fontSize) < 11; })() : false;
            r.tcNoLeftBar = card2 ? getComputedStyle(card2, '::before').display === 'none' : false;
            r.tcFooterCounts = card2 ? (card2.querySelector('.kbv2-card-chk') && /5/.test(card2.querySelector('.kbv2-card-chk').textContent)) : false;
            r.tcTwoAvatars = (() => { const c3 = [...document.querySelectorAll('.kbv2-card')].find(c => c.querySelector('[data-cardmenu="k3"], [data-detail="k3"]')); return c3 ? !!c3.querySelector('.kbv2-av-by') : false; })();
            r.tcDoneBadge = (() => { const c4 = document.querySelector('.kbv2-column:last-child .kbv2-card'); return c4 ? getComputedStyle(c4, '::before').display !== 'none' : false; })();
            return r;
          }, { d0: D(0), d1: D(1) });
          out.smoke = smoke;
          g.e1_lightUiOn = smoke.lightUi === true;
          g.e2_sidebar = smoke.sidebar === true;
          g.e3_more = smoke.hasMore === true;
          g.e4_newTask = smoke.newTask === true;
          g.e5_lanes4 = smoke.lanes === 4;
          g.e6_cards4 = smoke.cards === 4;
          g.t1_grid = smoke.tcGrid === true;
          g.t2_dueTop = smoke.tcDueVisible === true;
          g.t3_metaLabel = smoke.tcMetaLabel === true;
          g.t4_shortDate = smoke.tcShortDate === true;
          g.t5_inset = smoke.tcInset === true;
          g.t6_pct = smoke.tcPct === true;
          g.t7_priorityQuiet = smoke.tcPriorityQuiet === true;
          g.t8_noLeftBar = smoke.tcNoLeftBar === true;
          g.t9_footerCounts = smoke.tcFooterCounts === true;
          g.t10_twoAvatars = smoke.tcTwoAvatars === true;
          g.t11_doneBadge = smoke.tcDoneBadge === true;
          // handlers reais + surface Cliente com card cron (seed de QA; nada persiste)
          const clk = async sel => { await pg.evaluate(s => { const e = document.querySelector(s); if (e) e.click(); }, sel); await pg.waitForTimeout(250); };
          await clk('[data-detail="k2"]'); g.f1_detalhes = (await pg.evaluate(() => ((document.getElementById('modalRoot') || {}).innerHTML || '').length)) > 50;
          await pg.evaluate(() => { const m = document.getElementById('modalRoot'); if (m) m.innerHTML = ''; });
          await clk('[data-move="k2"]'); g.f2_mover = await pg.evaluate(() => /Mover tarefa/.test((document.getElementById('modalRoot') || {}).innerHTML || ''));
          await pg.evaluate(() => { const m = document.getElementById('modalRoot'); if (m) m.innerHTML = ''; });
          // hover: Mover ancora no TOPO (right ~44) e nao cobre footer
          const hov = await pg.evaluate(() => { const c = [...document.querySelectorAll('.kbv2-card')].find(x => x.querySelector('[data-move="k2"]')); if (!c) return null; const b = c.querySelector('[data-move="k2"]'); const cb = c.getBoundingClientRect(); const bb = b.getBoundingClientRect(); return { topOff: Math.round(bb.top - cb.top) }; });
          g.f3_moverTop = !!hov && hov.topOff < 40;
          // screenshots de evidencia (board 3 larguras + crops)
          const shot = async (w, h, name) => { try { await app.evaluate(({ BrowserWindow }, s) => { const win = BrowserWindow.getAllWindows()[0]; if (win) win.setContentSize(s.w, s.h); }, { w, h }); } catch (_) {} await sleep(700); await pg.screenshot({ path: process.env.RUNNER_TEMP + '/' + name }); };
          await shot(1920, 1080, 'RC250-BOARD-1920.png');
          g.h1_no_hscroll_1920 = await pg.evaluate(() => document.documentElement.scrollWidth <= window.innerWidth + 2);
          const cardEl = await pg.$('.kbv2-column:nth-child(2) .kbv2-card');
          if (cardEl) await cardEl.screenshot({ path: process.env.RUNNER_TEMP + '/RC250-CARD-CROP.png' });
          if (cardEl) { await cardEl.hover(); await pg.waitForTimeout(300); await cardEl.screenshot({ path: process.env.RUNNER_TEMP + '/RC250-CARD-HOVER.png' }); }
          await shot(1536, 864, 'RC250-BOARD-WIN125.png');
          g.h2_no_hscroll_win125 = await pg.evaluate(() => document.documentElement.scrollWidth <= window.innerWidth + 2);
          await shot(1366, 768, 'RC250-BOARD-1366.png');
          g.h3_no_hscroll_1366 = await pg.evaluate(() => document.documentElement.scrollWidth <= window.innerWidth + 2);
          g.h4_lanes_after_resize = (await pg.evaluate(() => document.querySelectorAll('.kbv2-column').length)) === 4;
          // surface Cliente com card cron
          try { await app.evaluate(({ BrowserWindow }) => { const w = BrowserWindow.getAllWindows()[0]; if (w) w.setContentSize(1920, 1080); }); } catch (_) {}
          await sleep(500);
          await pg.evaluate(() => { state.tasks.push({ id: 'c1', title: 'Cronograma Setembro - Sunset Wear', client: 'Sunset Wear', sector: 'cronograma', by: 'carlos', assigneeId: 'carlos', status: 'afazer', dueDate: '2026-08-29', dueTime: '10:00', cronContents: [{ tema: 'Tema 1' }, { tema: 'Tema 2' }, { tema: 'Tema 3' }] }); render(); });
          await sleep(300);
          await clk('.nav [data-flowclient]');
          g.f4_ctxCliente = (await pg.evaluate(() => state.flowView)) === 'client';
          g.f5_clientCard = await pg.evaluate(() => { const c = document.querySelector('.scr-client .kbv2-card'); if (!c) return false; const t = c.querySelector('.kbv2-title'); return getComputedStyle(c).display === 'grid' && !!t; });
          await sleep(300); await pg.screenshot({ path: process.env.RUNNER_TEMP + '/RC250-CLIENT-1920.png' });
          const ccEl = await pg.$('.scr-client .kbv2-card');
          if (ccEl) await ccEl.screenshot({ path: process.env.RUNNER_TEMP + '/RC250-CLIENT-CARD.png' });
          await clk('.nav [data-myboard]');
          g.p1_noFatalPageError = perr.length === 0;
          out.pageErrors = perr;
          const fails = Object.entries(g).filter(([k, v]) => !v).map(([k]) => k);
          out.fails = fails;
          fs.writeFileSync(process.env.RUNNER_TEMP + '/au-sessionB.json', JSON.stringify(out));
          console.log(JSON.stringify(out, null, 1));
          try { await Promise.race([app.close(), sleep(4000)]); } catch (_) {}
          if (fails.length) { console.error('FAILS B:', fails); }
          process.exit(fails.length ? 1 : 0);
          EOF
          node au-b.mjs
          echo "INSTALLED-APP QA 1.0.250: PASS (update 249->250 silencioso + smoke + regressao Task Card + evidencias)" >> "$GITHUB_STEP_SUMMARY"

      - name: "Upload BUNDLE FINAL 1.0.250 (assets do release)"
        uses: actions/upload-artifact@v4
        with:
          name: agenda-id-seven-desktop-1.0.250-final-bundle
          path: |
            desktop/dist-installer/Agenda-ID-Seven-Desktop-1.0.250-x64.exe
            desktop/dist-installer/Agenda-ID-Seven-Desktop-1.0.250-x64.exe.blockmap
            desktop/dist-installer/Agenda-ID-Seven-Desktop-1.0.250-x64.msi
            desktop/dist-installer/latest.yml
            desktop/dist-installer/SHA256SUMS
            desktop/dist-installer/VERSAO-DESKTOP.txt
          if-no-files-found: error
          retention-days: 30

      - name: "Upload evidencias (manifest + sessoes + screenshots RC250)"
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: agenda-id-seven-desktop-1.0.250-final-evidence
          path: |
            ${{ runner.temp }}/FINAL-250-MANIFEST.txt
            ${{ runner.temp }}/au-sessionA.json
            ${{ runner.temp }}/au-sessionB.json
            ${{ runner.temp }}/RC250-BOARD-1920.png
            ${{ runner.temp }}/RC250-BOARD-WIN125.png
            ${{ runner.temp }}/RC250-BOARD-1366.png
            ${{ runner.temp }}/RC250-CARD-CROP.png
            ${{ runner.temp }}/RC250-CARD-HOVER.png
            ${{ runner.temp }}/RC250-CLIENT-1920.png
            ${{ runner.temp }}/RC250-CLIENT-CARD.png
          if-no-files-found: warn
          retention-days: 30
