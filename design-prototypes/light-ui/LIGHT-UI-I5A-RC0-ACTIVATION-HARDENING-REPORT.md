# LIGHT UI — I5A / RC-0 · CONTROLLED ACTIVATION GATE + RELEASE DEBT CLOSURE

**Base:** `899862a2` (F13 congelada; md5 do renderer base `0a3c3d7f…` idêntico ao da I4) · **Branch:** `rc/light-ui-activation-hardening-1.0.246` · **Versão:** 1.0.246 (inalterada) · **Natureza:** feature flag interna + correções de dívida autorizadas + QA. Sem build, sem installer, sem package, sem tag, sem release, sem deploy, sem merge, sem PR, sem bump. **Light UI permanece DESLIGADA por padrão para 100% dos usuários.**

**Veredito: I5A = GO.** Resposta literal à pergunta do mandato ao final.

---

## 1 · PREFLIGHT
Branch criado DIRETO de `899862a2` (verificado por hash e md5 — mesma base auditada pela I4). Worktree limpa antes dos edits. Versão 1.0.246 confirmada. Diff final: **1 arquivo** (`desktop/src/renderer/index.html`), 34 inserções / 11 deleções, 4 grupos cirúrgicos (bloco 8).

## 2 · ACTIVATION GATE (PARTE A) — mecanismo criado e provado
**Design (reutiliza o padrão de preferências REAL):** campo namespaced `luiPreview` DENTRO de `idseven.desktop.appearance` (localStorage; mesmo mecanismo do tema claro/HC/zoom — client-side puro, sem backend, sem UI pública nesta build). `applyAppearance()` aplica `body.classList.toggle('light-ui', a.luiPreview===true && body.classList.contains('desktop'))` — **guard rail `.desktop` fechado (achado B1 da I4)**; reavaliado também na mudança da `DESKTOP_MQ` (single edit na linha do listener). Opt-in: `appearSet({luiPreview:true})`; kill: `appearSet({luiPreview:false})`. Todas as provas abaixo usam o **mecanismo real** (chave gravada ANTES do load + boot completo via `authSelf`; a classe **nunca** foi injetada manualmente nas provas da Parte A).

| Gate | Prova | Resultado |
|---|---|---|
| **A1 default OFF** | 3 variantes de máquina: chave AUSENTE · chave real atual (`{theme,font,hc}` sem o campo) · chave CORROMPIDA (`{{{`) → em todas: sem classe, `appearLoad().luiPreview===false`, boot **não grava** a chave, zero writes | **15/15 PASS** |
| **A2 flag ON completa** | boot → par `desktop`+`light-ui` presente; skin viva (KPI computed `rgb(255,255,255)`); reload → persiste; MQ 1920→900→1920 → par sai/entra **JUNTO** (nunca `light-ui` órfã); **pixel:** mecanismo real × injeção das 13 fases sob o mesmo tipo de paint = **0px byte-idêntico** | **PASS** |
| **A3 kill switch** | `appearSet({luiPreview:false})` → classe removida imediatamente + persistido (1 write, chave única) + re-opt-in ok; **pixel: pós-kill × máquina nunca-ON = 0px IDENTICAL** | **PASS** |
| **A4 isolamento** | Escopo = **per-machine** (localStorage do renderer), EXATAMENTE como o tema claro/HC/zoom reais do produto (padrão aprovado; documentado: outro usuário na mesma máquina herda a aparência da máquina, como herda o tema hoje; outra máquina nunca vê a flag; nenhum canal novo: única chave tocada = `idseven.desktop.appearance`, zero backend) | **PASS (padrão do produto)** |
| **A5 logout** | `logout()` real com flag ON → login visível, sessão limpa (`wp_uid` etc.), aparência mantida (idêntico ao comportamento do tema light/HC hoje — login é superfície coberta F12) | **PASS** |
| **A6 first paint** | 1º frame renderizável capturado por rAF: **apenas o splash** (não-temável) visível — `#app`/`#login` ocultos; classe presente no `DOMContentLoaded` (aplicada durante o parse, linha do boot de aparência, muito antes do reveal async) ⇒ **nenhum frame com superfície temável sem a skin — zero flash** | **PASS** |
| **Guard desktop-only** | viewport 375 (sem `window.api`) + flag ON → `desktop` ausente E `light-ui` ausente (flag existe, guard nega a classe) | **3/3 PASS** |

*Nota A2 (registro técnico):* comparando shot "classe desde o parse" × "classe aplicada depois" existe um artefato **determinístico de rasterização do compositor** de 40 subpixels Δ≤2/255 na borda dos chips do header — provado ortogonal ao mecanismo (ocorre DENTRO do próprio mecanismo ao alternar off→on; sob paint igual os caminhos são 0px; computed styles idênticos). Não é diferença de estilo nem de conteúdo.

## 3 · RELEASE DEBT CLOSURE (PARTE B)
- **B1 · F10/F11 "Tarefas críticas" (HARD GATE) — CORRIGIDO (copy cirúrgica, 5 pontos).** Auditoria literal da fonte: KPI conta **só** `d.critical` (grace POR SETOR — 10/20 min); a lista dos cards contém **todos** os overdue (linha `if(d.state==='overdue')criticas.push`), ordenados critical-first (sort literal). Correções para eliminar as afirmações falsas: banner F10 `(atraso > 10 min)`→`(acima da tolerância do setor)`; KPI F10/F11 hint→`acima da tolerância`; cards F10/F11 `Tarefas críticas (recentes)`→`Atrasos recentes` (F11 sub→`críticas primeiro`, verdadeiro pelo sort). Golden intocado (mesmos elementos/estilos); fórmulas intocadas. Fixture boundary re-provada nos smokes (b1=N−599999 não-crít · b2=N−600000 crít · setores com grace distinto). **Nenhuma afirmação falsa restante nas superfícies F10/F11.** Registro fora de escopo: painel operacional do designer (linha ~5331) tem "10 minutos" hardcoded no estado overdue-não-crítico (impreciso p/ `edicao_midia`; o contador real ao lado usa o grace do setor; pushes usam texto por setor) — dívida pré-existente registrada, não tocada.
- **B2 · F13 async/double-submit (correção de confiabilidade) — CORRIGIDO.** `saveProduction` agora: guard de reentrância + busy acessível no padrão real do produto (`disabled` + `aria-busy` + "Salvando…" nos dois botões) + **`await` do update ANTES de fechar o modal**; falha → botões restaurados + toast honesto ("Não foi possível salvar agora…") + modal ABERTO com o digitado intacto (retry integral); sucesso → fluxo real inalterado (otimista pós-confirmação, resend→token→modal de envio). 1 write lógico por acionamento; sem novo workflow/payload/token/histórico. **Provas: 19/19** (busy durante voo com update em hold; cliques extras inertes; falha→retry preservando edição; retry grava o conteúdo EDITADO; Enter/Space; fechamento só pós-confirmação). Smokes F13 21/21 + chooser 1/1/1 continuam PASS.
- **B3 · F13 Escape — SEM MUDANÇA (correto).** Padrão real auditado: NENHUMA sheet do `#modalRoot` fecha com Escape (só ts-auth/wfDrawer/menus). Adaptar só o F13 criaria padrão novo — proibido pelo mandato. Dívida permanece registrada como comportamento uniforme do produto.
- **B4 · F12 recovery (HARD GATE) — STOP formal → RC-D02.** Auditoria de infra: login server-side próprio (IPC `desktopAPI.authLogin`→Cloud Run), **sem Firebase Auth client, nenhum endpoint de reset existe**. Não há API oficial disponível para implementar recovery real sem backend novo (fora do mandato). **Nada foi implementado nem simulado**; fluxo atual intocado, aguardando decisão do owner (bloco 9).
- **B5 · M1 CDN (prova empírica, 3 cenários) — FALLBACK SEGURO EXISTE → risco aceito documentado; vendorização NÃO aplicada.** O produto JÁ TEM guarda própria de boot offline (F3.5.5C-H1, script separado que sobrevive à morte do script principal). Provas: **(1) rede ok** → boot completo até o app (2/2). **(2) CDN bloqueada (sem stub de firebase; requests gstatic abortados)** → splash honesto "Aguardando conexão…" + hint "Sem conexão com o servidor — reconectando automaticamente. Sua sessão está preservada." + **reload automático observado** (backoff 4s; listener `online`) + login NUNCA revelado falsamente + morte do script principal documentada (5/5). **(3) SDK ok, backend fora** → splash persiste (nunca login automático), 3ª tentativa oferece "Tentar novamente / Entrar manualmente", escape manual leva ao login real, sem falso `authed` (4/4). Conclusão: **boot NÃO é irrecuperável** — degradação honesta e auto-recuperável por design. RC-D03 não aberto; vendorizar continua como opção de robustez para decisão do owner (registro M1 mantido).

## 4 · FULL QA (PARTE C)
- **C1 smokes:** **265/265** no commit final (f6 42 · f7 35 · f8 32 · f9 28 · f10 32 · f11 48 · f12 27 · f13 21) + chooser upload mouse/Enter/Space 1/1/1 + **novos gates I5A: 62** (A1 15 · A2/A3/A4/A5/A6 17 · mobile 3 · B2 19 · B5 11 já contando ok/nofs/cdn − sobreposição zero) ⇒ **327+ gates PASS, 0 fail**.
- **C2 navegação global (mecanismo real):** 2 voltas × 8 tabs + modais cruzados (Central, Legendas e artes) + deep-link F9→F6 + bloqueio H16 + filtros exec persistindo + badge=fonte única — **15/15**, incluindo gate novo `i12_pairAlways` (par `desktop+light-ui` presente em TODA a navegação) e `i15` sem `unhandledrejection`; sem pageerror/console-error próprios (únicos console: CDN gstatic bloqueada pelo harness — M1 conhecido).
- **C3 write map global:** Firestore/Functions/Worker/API = **zero writes em TODAS as baterias**; downloads = nenhum novo; GET-CDN = inalterado (2 scripts gstatic pré-existentes); localStorage: **única escrita nova do release = campo `luiPreview` dentro de `idseven.desktop.appearance`** (motivo: flag opt-in; payload: boolean; lifecycle: per-machine, sobrevive logout como o tema real; rollback: `appearSet({luiPreview:false})` ou remover a chave) — gravada **exclusivamente** em `appearSet` explícito (boot não grava: provado). Demais chaves: `wp_uid`/`wp_name` (login real, pré-existentes).
- **C4 responsivo (mecanismo real):** 1920×1080 · 1366×768 · 1536×864 — **15/15 cada**, com gate por tab `scrollWidth ≤ viewport` nas 2 voltas (zero scroll horizontal).
- **C5 a11y sweep:** nested interactive = **0** nas 8 tabs × 2 voltas × 3 viewports; F9-D01 sem regressão (gate dedicado no smoke F9 28/28); busy do B2 acessível (`aria-busy` + `disabled` + texto).

## 5 · LEGACY OFF (PARTE D) — base `899862a2` × HEAD, flag OFF
16 cenários (9 da matriz de inércia da I4: hoje/tarefas/agenda/exec/relatorios/notificacoes/login em dark + hoje HC + hoje legacy-light; + 7 legacy deep-states por fase F7–F12): **12 × 0px estrito**; **exec/relatórios dark (4 cenários: 2 tabs + 2 deep-states) com diff EXCLUSIVAMENTE das 5 strings B1 — provado por decomposição** (base+copy-only × HEAD = **0px** nos 4). Ou seja: fora da copy intencional e autorizada, **o legado é byte-idêntico**. Flake do sino: nenhum nos cenários D.

## 6 · F1–F13 REGRESSION, flag ON (PARTE E) — base × HEAD
31 superfícies (F1 shell · F2–F5 main+panel · F6 · F7 s1/s2 · F8 month/list/detail · F9 main/detail · F10 main/empty · F11 main/filtered/empty · F12 main/error/loading/focus/forgot · F13 main/empty/focus/host): **24 × 0px estrito**; **F10 main / F11 main / F11 filtered = diff só das strings B1 (decomposição 0px)**; 4 ocorrências do flake do sino tratadas pela **política A–E per-superfície**: re-runs → f3main/f5main/f5panel = 0px; f4main provado não-determinístico pelo próprio lado base (`base×base` flutua na bbox conhecida (1443,29,1485,71); zero pixels fora dela). **F13 em estados parados = 0px** (o busy do B2 é transitório e não altera render parado). **Zero regressão visual real; únicas mudanças visíveis do release = as 5 strings B1.**

## 7 · WRITE MAP (síntese)
Nenhuma escrita de rede nova. Nenhum listener novo. Nenhuma chave nova de storage — **um campo novo** (`luiPreview`) dentro da chave de aparência existente, escrito só por ação explícita. Assinatura completa no bloco C3.

## 8 · DIFF AUDIT (1 arquivo, 4 grupos — 100% mapeados ao mandato)
1. **Linha ~4396** — listener da `DESKTOP_MQ` ganha `try{applyAppearance()}catch(_){}` (reavaliação do guard na transição desktop/mobile — "sem ativação parcial"). 
2. **B1 copy** — 5 literais em `renderExecPanel`/`renderReports` (banner, 2 KPI hints, 2 card headers). Zero mudança estrutural/CSS. 
3. **B2** — corpo do `saveProduction` (guard de reentrância; busy acessível; `await` antes de fechar; caminho de falha com restauração+toast+return; otimista movido para pós-confirmação). Patch/payload byte-idênticos aos de antes. 
4. **Flag** — bloco de aparência: `appearLoad`/`appearSave` com `luiPreview` (default false, catch seguro) + `applyAppearance` com o toggle gated `.desktop` + comentário RC-0. 
Nada além disso: `git diff` = 34+/11− no `index.html` apenas.

## 9 · DEFECTS / DECISÕES DO OWNER
- **RC-D02 · Recovery de senha não implementável nesta base (STOP do hard gate B4).** Sem Firebase Auth client e sem endpoint de reset no backend atual, qualquer botão de recovery seria FALSO. Fluxo atual permanece exatamente como congelado na F12 (stub registrado no Golden). **Opções ao owner:** (a) copy honesta no link ("solicite ao administrador"); (b) ocultar o link nesta release; (c) mandato futuro com endpoint server-side real. Nenhuma foi aplicada sem decisão.
- **M1 (CDN)** — risco aceito com fallback provado (bloco B5); vendorização disponível como melhoria futura.
- **Registros:** texto hardcoded "10 minutos" no painel operacional do designer (~5331, fora do escopo F10/F11); artefato de rasterização paint-inicial×re-paint (Δ≤2/255, ortogonal ao mecanismo); flake do sino (política A–E, inalterada).

## 10 · CHECKPOINT
Commit único no branch `rc/light-ui-activation-hardening-1.0.246`: **`ae6fc7b0` · `feat(rc): add controlled light-ui activation and release hardening`** (899862a2 → ae6fc7b0; 1 arquivo, 34+/11−) + push. Sem PR (conforme mandato). Docs: este relatório + roadmap atualizado no branch de design.

## 11 · RECOMENDAÇÃO
**I5A = GO.** Ativação controlada existe, é opt-in, reversível, isolada, desktop-only, sem flash e sem backend; dívidas de release fechadas (B1/B2) ou formalmente decididas (B3 padrão real · B4 RC-D02 · B5 risco aceito com prova); QA integral verde; legado byte-idêntico fora da copy autorizada; F1–F13 sem regressão real.

**"Está autorizado tecnicamente iniciar a geração do Release Candidate?" — SIM**, com duas ressalvas de escopo já registradas: a decisão do owner sobre RC-D02 (recovery — como apresentar o link nesta release) e o aceite formal do risco M1 (CDN com fallback provado). Nenhum passo de build/package/release/deploy foi iniciado — **HARD STOP nesta entrega; I5B/RC Build só com mandato explícito do owner.**
