# F4.2C — Integração Canônica Firebase Auth (base funcional F4.2B)

> **STATUS:** BASE FUNCIONAL CANÔNICA INTEGRADA — PRONTA PARA MIGRAÇÃO DE IDENTIDADE
> (validação técnica por CI; **não** é GO de produção — cutover de identidade/Rules é fase à parte).

| Campo | Valor |
|---|---|
| Branch | `android/f42c-canonical-firebase-auth-integration` |
| Base (exata) | `72367c8145d06a710796998a9cdf5e7060adcbea` (módulo `android-native-beta/`) |
| Módulo | `android-native-beta/` **exclusivamente** (base funcional canônica F4.2B) |
| applicationId | `br.com.idseven.agenda.nativebeta` **(preservado nesta fase — não alterar)** |
| versionCode / versionName | `128` / `1.0.148-beta-firebase-auth-canonical` |
| Identidade final de produção | `br.com.idseven.agenda` (fase exclusiva posterior de identidade) |

Congelados e intactos (nenhum trabalho apagado): `android-native/` (legado);
branch `android/f42c-firebase-custom-token-production-auth` @ `11bcb6d`;
branch `android/f42b-server-auth-secure-session`; commit `72367c8`;
run F4.2B `29969282102` e evidências históricas.

---

## 1. Delta F4.2C integrado (somente o necessário)

Fluxo final de autenticação (obrigatório):

```
loginUser oficial
 → sessão server-side protegida no Keystore EXISTENTE (F4.2B)
 → issueFirebaseAuthToken (Bearer da sessão)
 → FirebaseAuth.signInWithCustomToken (custom token só em memória)
 → validar currentUser
 → validar UID Firebase == UID da sessão   (divergência ⇒ wipe total + login)
 → getUserSelf (bootstrap) → perfil real
 → SOMENTE então liberar telas (listeners Firestore)
```

Itens do mandato → onde:

| # | Avanço F4.2C | Implementação |
|---|---|---|
| 1 | `FirebaseAuth.signInWithCustomToken` | `data/auth/FirebaseBridge.kt` (`RealFirebaseBridge`) |
| 2 | endpoint `issueFirebaseAuthToken` | `ServerAuthRepository.issueFirebaseAuthToken` + `AuthApi/AuthApiClient.postFirebaseToken` |
| 3 | validação UID Firebase == sessão | `data/session/FirebaseSessionGate.kt` (`ensure`) |
| 4 | logout com `FirebaseAuth.signOut` | `SessionManager.logout()` (via `firebaseGate.signOut()`) |
| 5 | recuperação segura de identidade | `SessionManager.bootstrap()` + `FirebaseSessionGate.ensure` (re-cunho só se `currentUid != uid`) |
| 6 | FCM por `registerFcmToken` | `ServerAuthRepository.registerFcmToken` + `Fcm.kt` + `AppFirebaseMessagingService.kt` |
| 7 | remoção do write direto de FCM | removidos `collection("users")…update(fcmTokens…)` de `Fcm.kt` e `AppFirebaseMessagingService.kt` |
| 8 | bloqueio de Firestore sem FirebaseAuth | `Authenticated` só é emitido após `FirebaseSessionGate.Result.Ok`; listeners montam pós-`Authenticated` |
| 9 | redução de leituras inseguras de `users` | 2 writes diretos de FCM eliminados; demais operações passam a exigir `request.auth` real |
| 10 | preparação p/ Rules de menor privilégio | matriz Firestore (§5) + plano (§9); **Rules não alteradas** nesta fase |

**Arquitetura de blindagem:** a ÚNICA referência a `com.google.firebase.auth`/
`com.google.android.gms.tasks` no módulo é `FirebaseBridge.kt` (fronteira testável).
Os arquivos blindados do login (`ServerAuthRepository`, `AuthApiClient`, `AuthApi`,
`AuthContract`, `AuthErrorMapper`, `SessionManager`, `SessionBootstrapper`, `LoginViewModel`)
falam com a **interface** `FirebaseBridge`, nunca com o SDK — mantendo o gate de blindagem verde.

---

## 2. Diff (arquivos)

**Novos (main):**
- `data/auth/FirebaseBridge.kt` — interface `FirebaseBridge` + `RealFirebaseBridge` (SDK boundary).
- `data/session/FirebaseSessionGate.kt` — orquestrador issue→signIn→valida UID (sem tocar o cofre).

**Novos (test):**
- `auth/FirebaseTokenFcmContractTest.kt` — contrato de `issueFirebaseAuthToken` + `registerFcmToken`.
- `auth/FirebaseSessionFlowTest.kt` — login/bootstrap/logout com identidade Firebase.

**Modificados (main):**
- `data/auth/AuthApi.kt` — `postFirebaseToken`, `postRegisterFcm`.
- `data/auth/AuthApiClient.kt` — 2 URLs + 2 overrides (reusa `post()` HTTPS-only/Bearer/sem-log).
- `data/auth/AuthContract.kt` — `FirebaseTokenOutcome`, `FcmRegisterOutcome`, `AuthError.IDENTITY_MISMATCH`.
- `data/auth/AuthErrorMapper.kt` — mensagem humana de `IDENTITY_MISMATCH`.
- `data/auth/ServerAuthRepository.kt` — `issueFirebaseAuthToken`, `registerFcmToken` + URLs no ctor default.
- `data/session/SessionManager.kt` — gate em login (atômico), bootstrap (pré-listeners), logout (signOut), `create()`.
- `core/Fcm.kt` — registro via endpoint (remove write direto em `users`).
- `core/AppFirebaseMessagingService.kt` — `onNewToken` via endpoint (remove write direto em `users`).
- `app/build.gradle` — `firebase-auth`; 2 buildConfig URLs; versionCode 128 / versionName.

**Modificados (test):**
- `auth/AuthTestFixtures.kt` — `FakeAuthApi` (2 overrides + fixtures) + `FakeFirebaseBridge`.
- `auth/SessionFlowTest.kt` — injeta gate no helper; `t22` (login U2 assina Firebase como U2); `t23` (API inline + gate).

**Modificado (workflow):** `.github/workflows/build-android-native-beta.yml` — branch no push; gate de versão 128; strings de relatório/artefato.

Escopo garantido: `git status` só mostra `android-native-beta/**` + o workflow beta (gate region-invariance satisfeito).

---

## 3. Matriz de PARIDADE (F4.2B @72367c8 × Candidata canônica × 11bcb6d)

Legenda: ✅ preservada · ⬆️ melhorada · 🔄 substituída (transporte) · ➕ adicionada · N/A não aplicável

| Função | F4.2B @72367c8 | Candidata canônica (esta) | 11bcb6d (legado) | Resultado |
|---|---|---|---|---|
| loginUser | ✅ server-side | ✅ **idêntico** (inalterado) | ✅ (arquitetura Backend própria) | **preservada** |
| getUserSelf | ✅ | ✅ **idêntico** | ✅ | **preservada** |
| changePassword | ✅ | ✅ **idêntico** | N/A | **preservada** |
| Mensagens humanas de erro | ✅ `AuthErrorMapper` | ✅ + `IDENTITY_MISMATCH` | ✅ `AuthMessages` | **preservada + 1 msg** |
| Sessão Keystore AES-GCM | ✅ `SecureSessionStore`/`SessionCipher` | ✅ **idêntico** (reusado) | (impl. própria) | **preservada** |
| Bootstrap/restore | ✅ `SessionBootstrapper` | ✅ inalterado + gate Firebase no `SessionManager` | ✅ `AuthManager.restore` | **preservada + gate** |
| Sessão persistente | ✅ | ✅ | ✅ | **preservada** |
| Usuário inativo | ✅ (login/self) | ✅ + bloqueio no issue (403) | ✅ | **preservada ⬆️** |
| Logout | ✅ wipe local | ⬆️ + `FirebaseAuth.signOut` | ✅ signOut | **melhorada** |
| Single-flight login | ✅ | ✅ **idêntico** | (executor) | **preservada** |
| FirebaseAuth (Custom Token) | ❌ ausente | ➕ `FirebaseBridge`+`FirebaseSessionGate` | ✅ `FirebaseBridge`/`AuthManager` | **adicionada** |
| Validação UID == sessão | ❌ | ➕ (wipe em divergência) | ✅ | **adicionada** |
| FCM (transporte) | Firestore `users.fcmTokens` (direto) | 🔄 `registerFcmToken` (endpoint) | 🔄 endpoint | **substituída (transporte)** |
| FirebaseMessagingService | ✅ (onMessageReceived) | ✅ **preservado** (só `onNewToken` troca transporte) | ✅ | **preservada** |
| Notificações/deep-link/lembretes | ✅ | ✅ **inalterado** | ✅ | **preservada** |
| Telas/tarefas/eventos/chats/nav/visual/permissões | ✅ | ✅ **inalterado** (0 telas tocadas) | (telas legadas próprias) | **preservada** |
| 33 testes F4.2B + gate de blindagem | ✅ | ✅ **preservados/verdes** (fixtures estendidas) | (suíte própria) | **preservada** |
| Keystore/sessão duplicados? | — | **NÃO** (reusa F4.2B) | (impl. própria) | **sem duplicação** |
| Dependência do `android-native/` legado | — | **ZERO** (§7) | é o próprio legado | **zero** |

**Resultado obrigatório:** zero funcionalidade da F4.2B perdida; FirebaseAuth + FCM seguro
adicionados; nenhuma duplicação de sessão/Keystore; nenhuma dependência do módulo legado.

---

## 4. Reutilizado seletivamente do 11bcb6d (adaptado à F4.2B madura)

| Do 11bcb6d | Como entrou na candidata |
|---|---|
| `FirebaseBridge` (interface + impl real) | **portado e re-namespaced** p/ `nativebeta`; `signInWithCustomToken` virou `suspend` (coroutines da F4.2B) |
| Padrão do `AuthManager` (issue→signIn→valida UID→wipe atômico) | **adaptado** ao `SessionManager`/`FirebaseSessionGate` coroutine-first (não copiado) |
| Contrato do `Backend` (`issueFirebaseAuthToken`, `registerFcmToken`) | **espelhado** no `ServerAuthRepository` sobre o `AuthApi`/`post()` da F4.2B (payload FCM = `token`/`deviceId`/`platform`; issue = POST vazio + Bearer; resposta `firebaseToken`/`count`) |
| Tratamento de UID divergente (bloqueio total) | **portado** (wipe + `signOut` + login; `AuthError.IDENTITY_MISMATCH`) |
| Testes de Custom Token / UID | **reescritos** sobre fakes da F4.2B (`FakeFirebaseBridge`, `FakeAuthApi`) |
| Testes de FCM server-side | **reescritos** como contrato do `registerFcmToken` |

## 5-bis. Rejeitado do 11bcb6d (não portado — F4.2B já é superior/madura)

- Nova implementação de **Keystore** (F4.2B `SecureSessionStore`/`SessionCipher` preservada).
- Nova implementação de **sessão** (F4.2B `SecureSession`/`SessionManager`/`SessionBootstrapper`).
- **Telas** e **navegação** do módulo legado (`android-native/`).
- **`AuthRepo` legado** / login client-side.
- Camada `Backend`/`HttpClient`/`ApiResult`/`ApiError` própria do legado (F4.2B usa `AuthApi`/`AuthApiClient`/`HttpReply`/`Transport`).
- Modelo executor+callback do `AuthManager` (F4.2B é coroutines/`StateFlow`).
- Qualquer código que duplicasse função madura da F4.2B.

---

## 5. Matriz FIRESTORE da candidata (Rules NÃO alteradas nesta fase)

| Local | Coleção | Operação | Estado na candidata | Exige `request.auth` (pós-Rules) |
|---|---|---|---|---|
| `Fcm.kt` (antes :38) | `users/{uid}` | write `fcmTokens`/`fcmTokenMeta` | **REMOVIDO** → `registerFcmToken` (endpoint, Bearer) | n/a (não é mais client-side) |
| `AppFirebaseMessagingService.onNewToken` (antes :21) | `users/{uid}` | write `fcmTokens` | **REMOVIDO** → `registerFcmToken` | n/a |
| `UsersRepo.kt:14` | `users` | listener (equipe) | mantido; roda pós-`Authenticated` (FirebaseAuth ativo) | **sim** |
| `ReminderAlarmActivity.kt:71` | `users/{respId}` | read (foto) | mantido (best-effort no alarme) | **sim** (SDK persiste sessão Firebase) |
| `AuthRepo.kt:104` | `users` | `add` (cadastro) | mantido (único write client-side; função administrativa — não removida sem prova de desuso) | **sim** (a restringir nas Rules) |
| `EventRepo/TaskRepo/ChatRepo` | events/tasks/chat | listeners | mantidos; pós-`Authenticated` | **sim** |
| `usersPublic` | — | — | **não usado** no beta | — |

Nenhum download da coleção `users` para autenticar; nenhuma verificação de senha no
aparelho; nenhum hash/salt no fluxo de login (gate de blindagem verde).

---

## 6. Endpoints (produção real, `-P` overridable no CI)

| Endpoint | URL default (canônica Gen2) |
|---|---|
| loginUser | `https://loginuser-de36pi7vza-uc.a.run.app` |
| getUserSelf | `https://getuserself-de36pi7vza-uc.a.run.app` |
| changePassword | `https://changepassword-de36pi7vza-uc.a.run.app` |
| **issueFirebaseAuthToken** | `https://us-central1-agenda-id-seven.cloudfunctions.net/issueFirebaseAuthToken` |
| **registerFcmToken** | `https://us-central1-agenda-id-seven.cloudfunctions.net/registerFcmToken` |

Forma `cloudfunctions.net` = a mesma de `functions/index.js` (determinística, sem hash presumido);
sobreponível por `-PAUTH_FIREBASE_TOKEN_URL` / `-PAUTH_REGISTER_FCM_URL`. Nunca token/credencial em BuildConfig.

---

## 7. Prova de ZERO dependência do `android-native/` legado

- Imports em `android-native-beta/**/*.kt` sob `br.com.idseven.agenda.` que **não** sejam
  `.nativebeta`: **NENHUM**.
- Referências de caminho a `android-native/` em fontes/gradle do beta: **NENHUMA**.
- Referência ao SDK `com.google.firebase.auth` no beta: **apenas** `data/auth/FirebaseBridge.kt`
  (fronteira única).
- `git status` restrito a `android-native-beta/**` + workflow beta (gate region-invariance).

## 8. Inventário de testes (`br.com.idseven.agenda.nativebeta.auth.*`)

| Suíte | Conteúdo |
|---|---|
| `ServerAuthContractTest` (13) | F4.2B — login/self/changePassword/erros/anti-enumeração — **inalterada** |
| `SessionFlowTest` (8) | F4.2B — bootstrap/logout/troca-usuário/single-flight — **atualizada p/ gate** |
| `SecureSessionTest` (5) | F4.2B — Keystore AES-GCM — **inalterada** |
| `AuthLoginShieldTest` (7) | F4.2B — gate de blindagem (login sem users/Crypto/hash/salt; sem log/Intent) — **inalterada** |
| `FirebaseTokenFcmContractTest` (9) | **F4.2C** — issue (token/401/403/rede/5xx/malformado) + FCM (payload+Bearer/401/rede+5xx) |
| `FirebaseSessionFlowTest` (11) | **F4.2C** — Custom Token válido/inválido, currentUser nulo, UID divergente, inativo, dormant, recuperação, perda/retorno de rede, logout |

Validação por CI: workflow `Build Android Nativo Beta (SaaS)` (push no branch) —
gate de versão (128) + region-invariance + testes JVM + gate de blindagem + `assembleRelease` (APK privado).
**Nenhum teste automatizado constitui GO de produção.**

---

## 9. Plano da migração final de identidade (fase exclusiva posterior)

Pré-condições (gates do owner) antes do cutover:
1. Android autenticado no Firebase (esta fase) **e** Desktop autenticada silenciosamente;
2. `request.auth` comprovado nos dois clientes;
3. Rollback de Rules preparado.

Passos da fase de identidade (NÃO nesta fase):
1. `applicationId`/`namespace` → `br.com.idseven.agenda` (remover sufixo `nativebeta`).
2. `google-services.json` **oficial** do app de produção.
3. Certificado oficial + **Play App Signing**.
4. `versionCode` monotônico oficial.
5. Renomear pacotes-fonte `…nativebeta` → oficiais (refactor mecânico) e ajustar o gate de blindagem/versão.
6. Publicar Rules de menor privilégio (matriz §5) com rollback armado.
7. Prova física com contas reais (login → Firestore autenticado → FCM real) antes do GO.

Proibições desta fase (respeitadas): não alterar `applicationId`; não gerar chave oficial;
não alterar Rules/Functions/Cloud Run; não publicar Desktop; não distribuir APK/AAB; não tocar dados reais.
