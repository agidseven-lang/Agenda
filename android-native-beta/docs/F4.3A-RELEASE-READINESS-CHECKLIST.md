# F4.3A — PRONTIDÃO PARA PUBLICAÇÃO OFICIAL NA GOOGLE PLAY (STOP GATE)

**App:** Agenda ID Seven — `br.com.idseven.agenda`
**versionCode:** 129 · **versionName:** 1.0.0 · **minSdk 26 / targetSdk 34 / compileSdk 34**
**Base congelada:** branch `android/f42e-official-identity-signed-build` (HEAD `4d33357`), build oficial no run CI **30205814202** (head `75e98cb`, success).
**Natureza desta entrega:** SOMENTE documentação (Markdown) + prova de invariância. **Nada foi publicado. Nada foi recompilado. Nenhum componente congelado foi tocado.**

> Este documento é o índice + relatório STOP-GATE de F4.3A. Os artefatos de preparação da loja estão nos docs irmãos `F4.3A-*.md` deste diretório. A publicação na Google Play é uma **ação manual do owner** no Play Console e só pode ocorrer após o comando literal do owner (ver §12).

---

## 0. Onde vive F4.3A (branch e por quê)

- F4.3A é entregue no branch Android dedicado **`android/f43a-play-release-readiness`**, criado a partir do HEAD congelado de f42e (`4d33357`).
- **Por que NÃO `main`:** um `push` em `main` dispara `.github/workflows/build-android-native-beta-manual.yml` (build descartável do módulo legado). Evitado.
- **Por que NÃO `android/f42e-official-identity-signed-build`:** `build-android-official.yml` tem trigger `push` nesse branch com `paths: ['android-native-beta/**']` — e `android-native-beta/docs/**` cai nesse filtro. Um commit de doc em f42e **recompilaria o AAB** e quebraria o SHA-256 pinado. Evitado.
- O branch `android/f43a-*` **não casa nenhum trigger de `push`** de nenhum workflow ⇒ zero CI, zero build, AAB/APK oficiais com SHA-256 idênticos. (Prova detalhada em `F4.3A-INVARIANCE-SCOPE.md` §4 e `F4.3A-IMPACT-ZERO-MATRIX.md`.)

---

## 1. Confirmação EXATA do artefato oficial (AAB) — a publicar (REUTILIZADO, não recompilado)

| Campo | Valor |
|---|---|
| Pacote | `br.com.idseven.agenda` |
| versionCode / versionName | 129 / 1.0.0 |
| **AAB SHA-256** | `1c14d39188d28997bdedf53e828eefcb96ba913cae3e0786da48e0aa65320a83` |
| APK SHA-256 (referência) | `dca77db...` (registrado em `F4.2E-OFFICIAL-IDENTITY-SIGNED-BUILD.md`) |
| Certificado de UPLOAD SHA-256 | `0c16dc20...` · SHA-1 `0e6ef79b...` |
| Alias de assinatura | `idseven-agenda-upload` (PKCS12) |
| Run CI que produziu o AAB | **30205814202** (head `75e98cb`, conclusão success) |
| Assinatura final na loja | **Play App Signing** (chave de app gerida pelo Google; a chave acima é a de *upload*) |

- O AAB oficial é o **único artefato de produção**. F4.3A **reutiliza** esse artefato pinado; **não** o recompila (builds assinados não são bit-a-bit reprodutíveis → recompilar mudaria o SHA-256). Detalhe do dado por trás de cada campo em `F4.3A-DATA-FLOW-AUDIT.md` / `F4.2E-*.md`.

## 2. Verificação de hash (integridade)

- Valor esperado do AAB (owner + F4.2E): `1c14d39188d28997bdedf53e828eefcb96ba913cae3e0786da48e0aa65320a83`.
- **CONFIRMADO** idêntico ao registrado em `F4.2E-OFFICIAL-IDENTITY-SIGNED-BUILD.md` e ao valor informado pelo owner na autorização de F4.3A.
- Nesta sessão **nenhum build Android foi disparado** (ver §10), portanto o binário e seu SHA-256 permanecem intactos. Quando o owner baixar o artefato do run 30205814202 para enviar à Play, deve reconferir `sha256sum` == valor acima antes do upload.

## 3. Situação do Play Console

- Esta sessão **não tem acesso ao Google Play Console** (nenhuma service-account de Play no repo; sem `fastlane/`; sem publishing automatizado). A publicação é **100% ação manual do owner**.
- Ações que o owner executa no Console (todas manuais, fora deste repo):
  1. App já criado / criar app `Agenda ID Seven` (produção, não teste).
  2. Configurar a ficha (usar `F4.3A-STORE-LISTING.md`).
  3. Preencher Data Safety (usar `F4.3A-DATA-SAFETY-FORM.md`).
  4. Publicar a política de privacidade e informar a URL (texto em `F4.3A-PRIVACY-POLICY.md`).
  5. Preencher o questionário de classificação (usar `F4.3A-CONTENT-RATING.md`).
  6. Declarar Alarme Exato + Notificação em Tela Cheia (textos em `F4.3A-PERMISSIONS-JUSTIFICATION.md`).
  7. Fornecer conta de teste ao Google (login obrigatório) na aba "Acesso ao app".
  8. Criar a release de **Production** com o **mesmo AAB** (SHA-256 §1) e configurar staged rollout (usar `F4.3A-ROLLOUT-HALT-UPDATE-PLANS.md`).
- **O owner NÃO deve clicar em "Enviar para revisão"/"Publicar" até dar o comando literal de §12.** (Criar rascunho no Console com o mesmo AAB oficial é permitido; **submeter/lançar não**.)

## 4. Ficha da Play Store — PREPARADA

→ `F4.3A-STORE-LISTING.md`: nome, descrição curta (≤80) e completa (≤4000) em PT-BR, categoria (Produtividade), notas da versão 1.0.0, atributos (sem anúncios, sem compras in-app, com interação usuário-a-usuário) e **especificação** dos assets gráficos (ícone 512×512, feature 1024×500, screenshots reais). Todas as imagens são produzidas pelo owner a partir da arte/telas REAIS — **nada fabricado aqui**.

## 5. Data Safety — PREPARADO

→ `F4.3A-DATA-SAFETY-FORM.md`: respostas prontas para o assistente, derivadas do comportamento REAL do app (A1/A3). Declara coleta e (por mandato do owner) **compartilhamento com o Google** de e-mail, nome, telefone, User IDs, foto, mensagens de chat, conteúdo de tarefas/eventos, FCM token e ANDROID_ID; encryption-in-transit = SIM; exclusão via canal externo (sem self-service in-app). Categorias NÃO coletadas (localização, financeiro, saúde, contatos, calendário do sistema, analytics, advertising ID) listadas com "No".

## 6. Política de Privacidade — PREPARADA

→ `F4.3A-PRIVACY-POLICY.md`: texto completo em PT-BR, coerente com a Data Safety e com o código (Firebase Auth/Firestore/FCM/Cloud Run; sem analytics/ads). Campos externos ao código (controlador, contato, datas, URL) ficam como `[OWNER: preencher]`. **O owner deve publicar em uma URL pública e informá-la na ficha e na Data Safety.**

## 7. Permissões — AUDITADAS

→ `F4.3A-PERMISSIONS-JUSTIFICATION.md`: as **7** permissões do `AndroidManifest.xml` mapeadas a uso REAL no código (file:line). Sinaliza as declarações sensíveis exigidas pela Play (Alarme Exato: `USE_EXACT_ALARM` + `SCHEDULE_EXACT_ALARM`; Tela Cheia: `USE_FULL_SCREEN_INTENT`; isenção de bateria) e fornece o texto de justificativa pronto para colar. Nenhuma permissão de localização/câmera/microfone/contatos/armazenamento.

## 8. Planos — Rollout, Halt e Atualização

→ `F4.3A-ROLLOUT-HALT-UPDATE-PLANS.md`:
- **Rollout controlado** (staged 10%→20%→50%→100%) com critérios objetivos (crash < 1,09%; ANR < 0,47%) e observação de login/sessão/lembrete/FCM/boot.
- **Halt/pause**: como deter/pausar; premissa crítica — **não existe versão anterior publicada**, logo não há "rollback para versão anterior"; correção é sempre **roll-forward** com `versionCode ≥ 130` (nunca reusar 129).
- **Atualização automática** pela Play (Play App Signing garante continuidade de assinatura; `BootReceiver` reancora os lembretes após `MY_PACKAGE_REPLACED`); toda versão futura repete o MESMO processo gated.

## 9. Prova de invariância (LIVE, read-only) — componentes congelados INTACTOS

Sonda ao vivo `f351-card-live-probe.yml`, run **30263330643** (2026-07-27, branch main, read-only, sem secrets, conclusão success):

- **Worker VIVO** = `{"ok":true,"service":"idseven-push","version":"V64.59-c20-failopen-f350-client-progress","build":"f350-client-progress-tracking"}` — mesmo build f350 congelado, **inalterado**.
- **Imagens OG** (ambas linhagens): `/og/wa-card-v64-39.jpg` → 200 image/jpeg 144941 B; `/og/wa-card-v64-38.jpg` → 200 144941 B; `/og/wa-card-roteiro-v64-60.jpg` → 200 75415 B (magic `ffd8ff`).
- **`/share/cronograma/<diag>`** → HTTP 200 com OG completo; `og:image` 1200×630 baixável pelo crawler; `x-id7-worker-build: f350-client-progress-tracking`; fail-open genérico (`x-share-task: not_found` para token diagnóstico — esperado).

Escopo formal de invariância + racional dos 4 invariantes (I1 sem código compartilhado alterado · I2 sem deploy · I3 sem recompilar AAB · I4 sem endpoint/segredo/dado tocado) em `F4.3A-INVARIANCE-SCOPE.md`.

## 10. Confirmação — NENHUM componente congelado foi alterado

Ver a matriz completa (23 componentes = **IMPACTO: ZERO**) em `F4.3A-IMPACT-ZERO-MATRIX.md`. Resumo:

- Superfície de escrita de F4.3A = **apenas** arquivos `.md` novos em `android-native-beta/docs/`, em branch dedicado (§0). Zero bytes de runtime.
- **Zero** alteração em `android-native-beta/app/**`, `desktop/**`, `cloudflare-worker.js`, `functions/**`, Firestore Rules, `.github/workflows/**`.
- **Zero** deploy: os pipelines de produção (`deploy-worker.yml`, `firestore-rules-deploy.yml`, `firebase-functions-deploy.yml`, releases Desktop) são `workflow_dispatch` e não foram invocados.
- **Zero** recompilação do AAB/APK; SHA-256 §1 preservado.
- Desktop 1.0.186, Card Premium/OG, Cronograma, Roteiro, aprovação 2-etapas, links, notificações amarela/vermelha, SLA, Agenda, Kanban, tarefas, eventos, chats, tray, X-close, updater, Worker J6, portal, F3.5.0, Rules, Functions, Cloud Run, dados reais → **IMPACTO: ZERO**.

## 11. Confirmação — NADA foi publicado aos usuários

- **Google Play:** nada enviado, nada lançado, nenhum rollout iniciado (sessão sem acesso ao Console). O AAB oficial permanece o único artefato de produção, intocado.
- **Desktop:** nenhuma release nova; 1.0.186 permanece Latest.
- **Worker / Rules / Functions / backend:** nenhum deploy.
- Esta entrega adiciona **somente documentação** ao repositório, em branch dedicado sem trigger de CI.

## 12. Portão de publicação (o que falta para publicar)

A publicação na Google Play requer, cumulativamente:
1. Owner preencher os itens `[OWNER: preencher/confirmar]` dos docs (§13).
2. Owner concluir os passos manuais do Play Console (§3) e criar a release de Production com o AAB oficial (§1) — **em rascunho, sem submeter**.
3. Owner emitir o comando literal **`PUBLICAR-ANDROID-1.0.0-GOOGLE-PLAY-PRODUCTION`**.

Sem esse comando literal, **não** submeter/lançar. (Publicação é ação manual do owner; esta sessão não publica.)

## 13. Pendências do OWNER (dados externos ao código — não inventados)

- URL pública da Política de Privacidade (publicar o texto de `F4.3A-PRIVACY-POLICY.md`).
- E-mail de suporte / contato da ficha; site e telefone (opcionais).
- Nome/endereço/razão social do desenvolvedor (conta Play / Data Safety).
- Classificação única Data Safety: "Shared" (mandato do owner) vs. "Collected/service provider" (definição literal da Play) — decidir e aplicar uniformemente.
- URL/e-mail e procedimento de solicitação de exclusão de conta/dados (política de exclusão de contas do Google — o app permite auto-registro).
- Host das imagens carregadas por Coil (provável Firebase Storage) — confirmar.
- Conta de teste para a revisão do Google (login obrigatório).
- Assets gráficos REAIS: ícone 512×512, feature 1024×500 e screenshots (spec em `F4.3A-STORE-LISTING.md`).
- Declarações de Alarme Exato e Tela Cheia no Console (textos prontos em `F4.3A-PERMISSIONS-JUSTIFICATION.md`).

---

## STATUS FINAL

**ANDROID OFICIAL PRONTO PARA PUBLICAÇÃO CONTROLADA NA GOOGLE PLAY — GO condicionado.**

Condicionado a: (a) o owner preencher os itens de §13; (b) a publicação ocorrer manualmente no Play Console com o AAB oficial (SHA-256 §1, **sem recompilar**); (c) o branch permanecer dedicado (`android/f43a-*`, ≠ main, ≠ f42e); (d) o comando literal `PUBLICAR-ANDROID-1.0.0-GOOGLE-PLAY-PRODUCTION` de §12.

Sob os quatro invariantes (I1–I4), **todos os 23 componentes congelados = IMPACTO: ZERO** e a prova de invariância ao vivo (§9) confirma Worker/Card/OG intactos. **Nada foi publicado; nada congelado foi alterado; o AAB não foi recompilado.**

**Condição de PARAR (NO-GO)** — se qualquer uma ocorrer, interromper e apresentar arquitetura alternativa isolada (detalhe em `F4.3A-IMPACT-ZERO-MATRIX.md` §3): alteração fora de `android-native-beta/docs/**`; branch = `main` ou `f42e`; qualquer recompilação/divergência do SHA-256 do AAB; qualquer doc com endpoint/token/chave executável ou dado/usuário/tarefa/screenshot fabricado; Data Safety que não reflita a produção real (Firebase Auth/Firestore/FCM).
